# Запись звука: что мы обещаем, что бывает, и где сегодняшняя логика врёт

Собрано 2026-07-29, когда выяснилось, что откат на захват всего экрана — не защита,
а заплатка, которая маскирует то, что мы не умеем попадать в приложение. Документ
нужен, чтобы посмотреть на слой захвата целиком, а не чинить его по одному вопросу
за раз.

Замеры, на которые он опирается: [ECHO_AND_MIX_EXPERIMENTS.md](ECHO_AND_MIX_EXPERIMENTS.md)
(эхо, сдвиг стемов, некогерентность) и [SYSTEM_AUDIO_CAPTURE_EXPERIMENTS.md](SYSTEM_AUDIO_CAPTURE_EXPERIMENTS.md)
(история с Process Tap и аудит стемов).

---

## 1. Что мы обещаем

| # | Обещание | Почему именно так |
|---|---|---|
| О1 | **Обе стороны в записи** — микрофон и то, что слышно из динамика | Без дальней стороны запись бессмысленна: саммари не о чем |
| О2 | **Не портить сам звонок** | VPIO уже сделал тестировщика неслышным для собеседников. Записывающий инструмент не имеет права ухудшать то, что записывает |
| О3 | **Писать встречу, а не машину** | Приватность (чужая музыка, уведомления, параллельный звонок) и шум в транскрипте |
| О4 | **Ничего не спрашивать** | Целевое состояние — install & forget: в приложение заходят забрать саммари |
| О5 | **Дать материал для «кто говорил»** без библиотеки голосов | Владелец должен быть определим структурно, а не эвристикой |
| О6 | **Не требовать наушников** | Люди слушают через динамики, и это норма, а не ошибка пользователя |
| О7 | **Пережить длинную встречу и смену устройств** | Восемь часов, AirPods посреди звонка, отключённый монитор |
| О8 | **Быть пригодным для лайв-транскрайба** | Буферы и общие часы нужны заранее; задним числом их не завести |
| О9 | **Всё локально** | Ни один килобайт звука не уходит наружу |

### Чего мы **не** обещаем

Список нужен не меньше первого — он держит границы: телефонные звонки через
Continuity, запись без разрешения на экран, звук приложений, которые не являются
встречей, встречи, начатые до старта записи (пишем с момента старта), и любая
попытка «починить» плохой микрофон пользователя.

**Встречи в браузере — вне области (решено 2026-07-29).** Толк и Meet во вкладке
играют через процесс браузера, и отличить вкладку встречи от вкладки с ютубом
нечем: ни ScreenCaptureKit, ни Core Audio process taps такого разреза не дают.
Пока целимся в десктопные приложения; браузер — отдельное решение потом, и
принимать его имеет смысл, когда десктопный случай работает.

---

## 2. Юзкейсы

Колонка «сегодня»: ✅ работает · ⚠️ работает случайно или частично · ❌ не работает.

### 2.1 Где живёт звук встречи

| Случай | Сегодня | Что происходит |
|---|---|---|
| Zoom, идёт звонок | ✅ | Тап целится в процессы платформы, чей звонок идёт (`TapTargetPolicy`) — само приложение и его хелперы |
| Zoom, звук из процесса-хелпера (`CptHost`) | ✅ | Раньше это делал SCK молча, отдавая приложение целиком. Тапу хелпер надо назвать: ловится префиксом bundle id (`us.zoom.xos.CptHost`) и по имени процесса из таблицы платформ. Под тестами (`TapTargetTests`) |
| Контур.Толк, идёт звонок | ⚠️ | Звук снимется, если звонок засечён. Но **засечь его теперь нечем**: единственным сигналом был заголовок окна, а заголовки мы не читаем. Нужна строка `callHelperProcesses` в таблице платформ, подтверждённая на живом установленном Толке |
| Приложение встречи запущено, но звонка нет (запись подкаста, вебинара) | ✅ | Пишем машину целиком — глобальный тап, исключающий нас самих. Область решается по идущему звонку, а не по факту запуска: иначе висящий весь день Zoom превратил бы такую запись в тишину |
| Толк / Zoom / Meet в браузере | 🚫 | **Вне области.** Звук идёт через процесс браузера, отличить вкладку встречи от вкладки с ютубом process taps не дают. С 2026-07-29 невозможно и автоопределение: заголовков окон мы больше не читаем. Браузерный случай вернём отдельным решением — понадобятся другие способы, не заголовки |
| Telegram, WhatsApp, Discord, FaceTime | ❌ | Не в таблице платформ вовсе |
| Звонок в наушниках | ✅ | Захват выхода не зависит от устройства вывода |

### 2.2 Аудио-тракт

| Случай | Сегодня | Что происходит |
|---|---|---|
| Динамики ноутбука | ⚠️ | Микрофон по-прежнему ловит дальнюю сторону, но дорожки теперь когерентны (0.91–0.97), то есть эхоподавитель на них наконец сойдётся (Э5). Задвоение в миксе ушло вместе со сдвигом стемов |
| Проводные наушники | ✅ | Чистый случай |
| AirPods подключены **до** старта | ✅ | — |
| Смена устройства вывода **посреди** записи | ⚠️ | **Слушатель добавлен 2026-07-29** (`ProcessTapCapture`): вход, выход и исчезновение устройства из состава дают одну пересборку тапа и агрегата, дыра вписывается тишиной. На живой записи не проверено — переключать не на чем: виртуальная карта основной не становится, а второго настоящего устройства нет (M3) |
| Bluetooth-гарнитура в моно-профиле | ⚠️ | Микрофон уходит в узкую полосу, ASR деградирует; мы этого не видим и не сообщаем |
| Внешняя карта / HDMI-монитор | ⚠️ | Не проверялось |

### 2.3 Кто говорит

| Случай | Сегодня | Что происходит |
|---|---|---|
| Один на один | ✅ | — |
| Пользователь только слушает | ⚠️ | Транскрипт цел, но имя владельца может сесть на собеседника (энергия микрофона выше из-за эха) |
| Пользователь только говорит | ✅ | — |
| Все замьючены полчаса, потом говорят | ✅ | Гейт микса смотрит на пик по файлу, одно слово его проходит |
| Вебинар: говорит один, остальные молчат весь час | ✅ | То же |
| Перебивания (обе стороны разом) | ⚠️ | В миксе складываются; раздельные стемы это решают, но мы их пока не транскрибируем |
| Шаринг экрана с видео и звуком | ⚠️ | При тапе на приложение встречи попадёт (играет оно), при глобальном тапе попадёт всё |

### 2.4 Время

| Случай | Сегодня | Что происходит |
|---|---|---|
| Запись начата до начала встречи | ⚠️ | Цель тапа выбирается **один раз на старте**: если звонок ещё не идёт, всю запись пишем машину целиком. Пересматривать цель на лету теперьтехнически можно (пересборка вписывает паузу тишиной), но это не сделано |
| Запись начата в середине | ✅ | Пишем с момента старта |
| Две встречи подряд в одной записи | ⚠️ | Одна запись, одно саммари — разделения нет |
| Встреча длиннее восьми часов | ✅ | Авто-стоп и обычный пайплайн (G5) |
| Сон / блокировка экрана посреди встречи | ❓ | Не проверялось |
| Приложение упало во время записи | ✅ | Восстановление на старте, стемы пересобираются в микс |

### 2.5 Система и разрешения

| Случай | Сегодня | Что происходит |
|---|---|---|
| Нет разрешения на экран | ✅ | Захвату оно не нужно вовсе, и с 2026-07-29 **не запрашивается**: ключа нет в бандле, шага в онбординге нет. Цена — автоопределение встречи держится только на процессе звонка: Zoom (`CptHost`) ловится, **Контур.Толк нет** (в таблице у него нет хелпера), браузер тоже нет. Ручная запись и её качество не затронуты |
| Разрешение протухло после пересборки | ⚠️ | Захват не поднимется, запись станет микрофонной и будет так помечена. Отличить «отказали» от «собеседники молчат» нечем: у разрешения на захват звука нет API статуса |
| Микрофон занят другим приложением | ⚠️ | Сторож микрофона поймает отсутствие прогресса |
| Второй рекордер (OBS) параллельно | ❓ | Не проверялось |
| macOS Voice Isolation включён системно | ⚠️ | Микрофон обрабатывается нелинейно; для AEC это приговор, мы этого не видим |
| Диск кончился | ⚠️ | Порог проверяется на старте; периодическая проверка во время записи не сделана |

---

## 3. Свежий взгляд: где логика врёт

**1. ~~Два списка платформ разъехались.~~ Исправлено 2026-07-29** — во что целиться, решает `TapTargetPolicy` в `PropellerPure` по той же таблице `MeetingPlatform`, что и детектор; под тестами. (`CaptureScopePolicy`, делавший это для SCK, удалён вместе с ним.) Было так: `MeetingPlatform.all` знает Zoom и
Контур.Толк, включая веб-версию. `SystemAudioCapture.meetingAppBundleIDs` — это
`["us.zoom.xos"]`, зашитая строка. То есть для Толка app-scoped даже не пробуется:
захват сразу пишет весь экран. Один и тот же вопрос «какое приложение является
встречей» отвечается в двух местах по-разному, и одно из них — константа.

**2. ~~Мы целимся в приложение с окном, а звук издаёт процесс.~~ Опровергнуто
2026-07-29.** Гипотеза была: список источников строится с `onScreenWindowsOnly: true`,
процесс звонка окон не имеет, значит app-scoped фильтр слушает тишину. Живой звонок
показал обратное — приложение записало звонок именно app-scoped фильтром, стем
громкий (rms 0.061, пик 0.70), откат не срабатывал. **SCK отдаёт звук приложения
целиком, включая его хелперы**, хотя в списке источников они не появляются.

Что из этой проверки всё же вышло: процесс звонка у нынешнего Zoom называется
**`CptHost`**, а не `aomhost` — он поднялся ровно в секунду входа в звонок, тогда
как `caphost` висел с утра. В таблице платформ стоял мёртвый идентификатор, то есть
сигнал «хелпер запущен» у детектора не работал вовсе. Починено, плюс сравнение имён
процессов теперь регистронезависимое, как и остальные правила таблицы.

**3. ~~Откат по тишине.~~ Удалён 2026-07-29** вместе с перезапуском потока и флагом одноразовости. Весь экран остался ровно для одного честного случая: ни одного знакомого приложения не запущено, а записать попросили. Было так: Триггер «четыре
секунды без слышимого звука» описывает нормальное начало почти любой встречи.
Срабатывая, он молча меняет смысл записи: вместо звонка мы пишем машину, вместе с
музыкой, уведомлениями и параллельными звонками — прямое нарушение О3. Пользователю
об этом не сообщается никак.

И теперь известно, что компенсировать ему нечего: прицел работает. Раз app-scoped
ловит звонок, откат нужен ровно для одного случая — «мы нацелились не на то
приложение», — а тишина в первые четыре секунды об этом не говорит ничего.

**4. ~~Три порога на один вопрос.~~ Осталось ноль (2026-07-29).** Порог в стороже
удалён вместе с откатом; порог при миксе тоже — тишину микшировать не вредно
(`MixGain` на тихом стеме возвращает единицу), а выбросить тихую, но настоящую
дальнюю сторону он мог. Осталась одна проверка, и она не про громкость: есть файл
стема больше заголовка — микшуем. Заодно ушло чтение всего стема ради строки в лог.

**5. ~~Микрофон — чёрный ящик.~~ Исправлено 2026-07-29.** `AVAudioRecorder`
остался только на запасном пути. Основной — `ProcessTapCapture`: микрофон и
системный звук приходят одной IOProc одного агрегатного устройства, кадрами и с
`mSampleTime`. Сдвиг измерять больше нечем и незачем — его нет по построению
(когерентность 0.97 против 0.04).

**6. ~~Смену устройства никто не слушает~~ — слушатель есть с 2026-07-29**,
но на живой записи не проверен: переключать не на чем (см. 2.2).

---

## 4. Куда идти

**Сначала научиться попадать, потом убирать откат.** Порядок важен: если снять
display-wide сегодня, мы потеряем звук ровно там, где он сейчас единственно и
спасает — Толк, браузер, хелперы Zoom.

**Сделано 2026-07-29:** пункты 1–4 ниже закрыты — прицел по таблице платформ,
откат по тишине удалён, пороги громкости из решений убраны. Осталось 5 и 6.

0. **Браузер не трогаем** — см. «чего не обещаем». Всё ниже про десктопные приложения.
1. ~~Целиться в процессы — не требуется, process taps остаются запасным вариантом~~
   — **перевёрнуто 2026-07-29 и окончательно 2026-08-06:** тап и есть единственный
   путь, SCK удалён вместе с ним и `CaptureScopePolicy`. Целиться в процессы
   требуется именно потому, что Core Audio фильтруется процессами:
   `TapTargetPolicy` называет и приложение, и хелперы звонка.
2. **Один источник правды.** Захват читает `MeetingPlatform.all`, как и детектор.
   Новая платформа — строка в таблице, а не правка двух файлов.
3. **«Не смог поймать приложение» — это состояние, а не повод писать всё.** Честный
   отказ лучше молчаливой подмены смысла записи. С 2026-08 это состояние —
   mic-only (`CapturePathPolicy`), и оно **не показывается ни одной строкой**:
   `micOnlyCaptured` пишется в запись, но ни одна вьюха его не читает. По
   `design/principles.md` §3 деградация обязана быть видимой — дырка открытая.
4. **Решать по фактам, а не по громкости.** Есть процесс — есть поток; тишина в
   потоке это тишина, а не отказ.
5. ~~**Слушать смену устройств** (M3)~~ — код есть, замера нет.
6. ~~**Готовить общие часы**~~ — **сделано 2026-07-29**: `ProcessTapCapture`.
   Лайв теперь возможен (кадры отдаются через `onLiveFrames`), AEC — тоже
   (когерентность 0.97 при пороге 0.7).

## 5. Чем мерить

**Захват на общих часах — `--tap-probe`** (добавлен 2026-07-29):

```bash
osascript -e 'quit app "Propeller"'   # иначе open -a просто активирует уже
open -a Propeller --args --tap-probe  # запущенный экземпляр, без флага
cat ~/Library/Application\ Support/Meeting\ Recorder/tap-probe.txt
```

Шесть ступеней снизу вверх: сырое устройство → агрегат из одного микрофона →
все варианты состава с тапом (с пиком **по каждому каналу отдельно**) →
настоящий `ProcessTapCapture` → сквозной прогон через `AudioRecorder` → смена
устройств на живой записи. Отчёт дописывается построчно: Core Audio умеет
встать намертво, и тогда нужна ровно последняя строка перед тем, как всё
замерло.

Пока проба идёт, нужно, чтобы **звучало и то, и другое** — иначе по пикам
каналов не отличить микрофон от системы, и вся ступень с составами
бессмысленна.

Что смотреть в отчёте **до** того, как делать выводы:

- `maxSys=1.00000` — динамик гнали в клиппинг. Когерентность на таком прогоне
  падает до 0.67 не от захвата, а от нелинейности акустического пути; при
  некритичной громкости она 0.91–0.97.
- «замер недействителен: устройство переключить не удалось» — виртуальные карты
  основными не становятся, система возвращает `noErr` и оставляет прежнее.
  Первый прогон на этом дал ложное «захват пережил смену устройства».
- Первый в жизни приложения прогон ждёт решения TCC около минуты. Это не
  зависание.

Когерентность считается отдельно, по стемам из пробы:
`tools/echo-probe/coherence.py`. Приёмка — **> 0.7** в речевой полосе.

**Область захвата — `--capture-probe`** (прежняя проба). Меряется во время
звонка; отчёт ложится в `~/Library/Application Support/Meeting Recorder/capture-probe.txt`.
Проба сама ждёт, пока появится звук, и сравнивает три фильтра — прод-путь (только
приложения с окнами), тот же плюс процессы без окон, и захват всего экрана.

- ~~Виден ли хелпер в `SCShareableContent`~~ — **измерено 2026-07-29:** не виден
  (ни `CptHost`, ни `caphost`), и это не мешает: app-scoped фильтр по `us.zoom.xos`
  пишет звонок громко и чисто.
- Сколько раз откат срабатывает на реальных встречах — теперь видно по
  `systemCaptureAppScoped` в архиве. Первая запись с новым полем: `true`.
- Как часто app-scoped вообще выживает: пишется в запись как
  `RecordingEntry.systemCaptureAppScoped` начиная с 1.14.
- ~~Что отдают process taps на macOS 14.4+ сегодня~~ — **измерено 2026-07-29:**
  прошлый вердикт «пустые стемы» устарел. Тап отдаёт звук, и на нём построен
  основной путь захвата (`--tap-probe` выше).
- **Что происходит со стемом при смене устройства вывода на живой записи —
  всё ещё открыто.** Не на чем проверить: одно настоящее устройство вывода и
  одно ввода, а виртуальная карта основной не становится. Нужны наушники.
