# Релиз 1.16.5 — саммари-lite

_Бывший план «Релиз 1.17 — улучшенное саммари». Сокращён до lite решением
владельца 2026-08-14: ансамбль читателю хуже базы (JUDGE-3: читаемость 2,10
против 3,25 при выигранном покрытии), парадигма v3 отвергнута пробой (доска,
раздел B0). Едут только бесспорные победы; выделенного номера не получают —
идут следующим регулярным релизом 1.16.5 (`build.sh` уже стоит на нём, CHANGELOG
копит транскрипционные фичи)._

Основание — замеры в [tools/recap-lab/OPTIMIZATION.md](tools/recap-lab/OPTIMIZATION.md):
t=0 детерминирован побайтово на двух встречах (A4), механическая сборка спасает
3–5 пунктов на схлопнувшихся сводах (A5.1) и не может схлопнуться сама.

## Что едет

| изменение | что даёт |
|---|---|
| `temperature 0` + один ретрай при t=0,3, если ответ схлопнулся | пол поднимается с 4 до 8 пунктов, результат воспроизводим. Пол держится **только вместе с ретраем** (риск №1 ниже) |
| нарезка на фрагменты + авторский свод из фактов, со страховкой сборкой (встречи, не влезающие в окно) | документ пишет единый автор — «Итог» и связность на месте (решение владельца 2026-08-15, OPTIMIZATION.md, «Финал»); механическая сборка считается на тех же фактах и забирает документ, только если свод схлопнулся или потерял половину пунктов (`RecapDigestGuard`) |
| версия генератора в метаданных конспекта | телеметрия по версиям, безопасный бэкфилл, откат |
| телеметрия генерации | reply_tokens, retried/collapsed, длительность, число пунктов, версия, RAM-когорта; на пути нарезки — ещё и `author` (свод/сборка), то есть частота срыва свода в проде |
| индикация активности на пути нарезки | долгая генерация не выглядит зависанием (счётчик «фрагмент N из M» до панели не доносится — владелец счёл ненужным, живёт в деталях лога; Г3, 15.08) |
| **Developer ID + hardened runtime + нотаризация** (сделано 2026-08-15) | первый запуск — двойной клик. «ПКМ → Открыть» уходит из инструкции коллегам, а вместе с ним — шаг, на котором раздача теряла людей молча |

Подпись едет в этом релизе не потому, что относится к саммари, а потому что
1.16.5 — первый образ, который увидит человек, не читавший COLLEAGUES.md.
Устройство и порядок — в [COLLEAGUES.md](COLLEAGUES.md) «Для разработчиков»;
короткая версия: подпись изнутри наружу без `--deep`, нотаризуются **оба**
артефакта — `.app` (тикет едет в бандле, который распаковывает Sparkle) и `.dmg`.

## Что НЕ едет (решено, не забыто)

- **Ансамбль — срезан целиком** (решение владельца 2026-08-14) и вернулся на
  доску. Условие возврата любой многовызовной конструкции в релиз: покрытие
  выше базы **и** читаемость у независимого судьи не ниже базы. Матчерное
  покрытие без читателя один раз уже обмануло — этим правилом урок зафиксирован.
  Лид «свободные окна, 4 вызова» (доска, B0/«Что дальше» п. 4) живёт там же
  и меряется только под это условие.
- Порог 15 минут и маршрутизация по RAM — не нужны без ансамбля: путь один для
  всех машин.
- Облачный провайдер как opt-in тир качества — **решение владельцем не принято**,
  вне этого релиза.
- Старые конспекты не перегенерируются **никогда** — ни бэкфиллом, ни вручную.
  Бэкфилл догоняет только встречи без конспекта.
- Кнопки «Сгенерировать заново» нет и не появляется: при t=0 повтор — тот же текст.
- Выдумки не чиним — только считаем в телеметрии.
- 9B не тащим: +0,7 пункта в честном замере (p = 28 %) не стоят 3,2 ГБ
  (OPTIMIZATION.md, пункт «б»).

## Правила маршрутизации

| встреча | путь |
|---|---|
| влезает в окно | одиночный проход, t=0 + ретрай |
| не влезает в окно | нарезка на фрагменты → факты → свод одним вызовом, t=0 + ретрай; сборка кодом — страховка гварда |
| notes пользователя | текст заметок не переписывается (правило наследуется); одиночный путь — поведение 1.16 не меняется |

## Решения до старта порта

- [x] Выдумки: только детектим в телеметрии, не чиним (решено 2026-08-13).
- [x] **Дубль лучше пропуска — почти всегда** (решено владельцем 2026-08-14).
      Пропуск невидим и нечиним читателем; дубль виден и стоит лёгкого
      раздражения. В lite живёт в механической сборке: при неуверенности
      «дубль или потеря» — дубль. Граница — бюджет буллетов: внутри бюджета
      жадный отбор предпочитает новое повторному.
- [x] Ретраев у одиночного пути — **один** (замер A5.2 показал, что порог берут
      2 сэмпла из 5, но растить хвост попыток вслепую не будем): телеметрия
      `collapsed` покажет масштаб на проде, решение о втором ретрае — по ней.
- [x] Notes на пути нарезки: свод механический, вставлять заметки генерацией
      некуда. Сделано по дефолту (2026-08-14): блок кладёт `RecapNotes.placed`
      (8 тестов, текст дословный). Глазами на живой встрече — Г3.

## Риски, известные до старта

**1. У `t=0` есть режим конфабуляции, и детерминизм его запирает.** На
`20260810_094722` детерминированный ответ придумывает участника («Ильяс» →
«Илья Сафронов», пять вхождений, ему назначаются задачи); повтор при t=0 вернёт
то же слово в слово. Строка «пол 4 → 8» держится только вместе с ретраем.

**2. Дыра, которая есть в проде уже сегодня.** Одиночный путь имена не
фильтрует: выдуманная фамилия доезжает до пользователя как есть. В lite это
осознанно не чинится (см. «Что НЕ едет») — только телеметрия.

**3. Смена подписанта под Sparkle.** Установленные 1.16.x подписаны ad-hoc или
self-signed, новая сборка — Developer ID. Sparkle сверяет подпись обновления с
подписью установленного приложения, и переход между разными подписантами —
известное место, где апдейт молча отказывается ставиться. Проверяется только на
**реально установленной старой сборке**, не на свежей (гейт Г3). Если сломано —
у людей на руках 1.16.5 без пути наверх, и лечится это перезаливкой DMG руками.
Побочно: TCC (микрофон, календарь) спросят заново — идентичность сменилась.

**4. Кандидат в телеметрию:** доля имён конспекта без кандидата ближе θ = 0,343
(нормализованное редакционное расстояние до токенов транскрипта и шапки).
Считается без модели; как относительный риск-сигнал откалиброван пробой C-2
(`tools/recap-lab/probes/PROBES.md`). В гейты не входит.

## Гейты

Г0 старого плана снят: стендовая конструкция ансамбля вернулась на доску вместе
с самим вопросом.

### Г1 · Swift-порт с паритетом — **закрыт 2026-08-14**

- [x] Fixture-тесты: живой вход стенда (`gate2-fixed m3/code-1`) собирается
      `RecapAssembly.assemble` **байт в байт** как python-эталон —
      `Tests/Fixtures/recap-assembly/` (README с командой снятия эталона)
- [x] Все четыре формы вывода экстрактора + пятая неизвестная метка (текст
      сохраняется, ярлык не едет) — `RecapAssemblyTests`, перенос из
      `tools/recap-lab/test_parse.py`
- [x] Промпты не тронуты ни байтом (extract / конспект / редактура). Свод —
      это тот же конспектный промпт над фактами вместо транскрипта; вернулся
      2026-08-15 дословно из `26e0504^`, вместе со своей обвязкой user-сообщения

### Г2 · Юнит-тесты новой логики

- [x] Переполнение окна → нарезка + свод; влезает → одиночный. Решение целиком
      (включая «облако не режется никогда») вынуто из `RecapService` в
      `TranscriptChunking.needed(backend:promptCharacters:)` —
      `testНарезкаТолькоДляЛокальногоПути` (2026-08-15)
- [x] Гвард свода: схлопнутый после ретрая → сборка; свод, потерявший половину
      пунктов → сборка; здоровый → свод — `RecapDigestGuardTests`, 7 шт.,
      схлопывание входит живым ответом Ollama через границу
      (`Tests/Fixtures/boundaries/chat-collapsed-digest.json`)
- [x] Ретрай: только при схлопывании, только один, только t=0,3; при t=0
      повторного вызова с t=0 не бывает — `RecapGenerationPolicyTests`, 13 шт.
- [x] Notes: блок кладёт `RecapNotes.placed` — `RecapNotesTests`, 8 шт.
- [x] Версия генератора пишется в метаданные нового конспекта — glue в
      `AppState.runRecap`, юнитом не достижим (Sources); живая проверка — Г3/Г5
- [x] Бэкфилл пропускает встречу с конспектом любой версии — цепочка из трёх
      звеньев, все чистые и покрытые: конспект+метаданные ⟺ `.summarized`
      (`SummaryStageReconciler`, `RecordingStageTests`), `.summarized` ничего не
      должен (`testAFinishedArchiveSchedulesNothing`), версия генератора не
      входит ни в одно из двух решений по коду (проверено 2026-08-15)
- [x] `Task.checkCancellation` между вызовами чанков — в цикле фрагментов,
      поведение с 1.16 не менялось
- [x] Упавший чанк → конспект из оставшихся, не ошибка — catch/continue в цикле,
      дыра называется в логе
- [x] Полный сьют проекта зелёный: 747 тестов, 5 пропущено, 0 падений +
      `./build.sh` установлен (2026-08-14)

### Г3 · Сценарная прогонка руками (живое приложение)

Любой fail = will-hurt. Дополняет [dogfood-checklist.md](dogfood-checklist.md).

- [x] Обычная встреча → t=0, два прогона на одном входе дают один текст —
      детерминизм закрыт стендом (`tools/recap-lab/out/v2-compare/`, тот же
      вход → тот же текст); 104 обычных конспекта сгенерены стендами 15.08 на
      установленном билде без единого failed
- [x] Встреча не влезает в окно → документ пишет свод: «Итог» на месте, текст
      связный. В логе — строка «документ пишет свод модели»; если вместо неё
      «документ отдан сборке», конспект всё равно целый, но без «Итога», и это
      сработавшая страховка, а не дефект

      Прогнано на стенде 15.08, обе длинные встречи архива, обе `chunked=1`, и
      **авторы разные** (выдача — `/Users/Shared/propeller-stand/g3-long-meetings/`):

      | встреча | символов | время | автор | буллетов | «Итог» |
      |---|---|---|---|---|---|
      | `20260803_180142` Агентный дизайн | 132 840 | 291,6 с | `digest` | 25 | есть |
      | `20260729_155956` Обед | 127 676 | 208,3 с | `assembly` | 32 | нет |

      У «Обеда» гвард отобрал документ **не по схлопыванию**: `collapsed=0`,
      `retried=0`, ответ 960 токенов — выше пола 800. Сработало второе условие
      (`RecapDigestGuard.swift:76`): в своде меньше половины пунктов сборки.
      Пара 1:1 не даёт числа (n = 2), но говорит, что страховка живёт не в
      углу распределения — доля `assembly` в проде теперь и есть тот вопрос.

      **Чтение глазами за владельцем** — связность и «авторскость» текста
      здесь никем не проверены.

      Лог-строка гварда — **починено и подтверждено** 15.08 вечером: четыре
      решения пути длинной встречи переведены с `NSLog` на `debugLog`
      (подсистема `app.propeller`), «документ пишет свод модели» / «документ
      отдан сборке» видны живым `log stream` на приёмочном прогоне Г4.
      Оба документа владелец смотрел глазами: «Обед»-сборка 15.08 —
      «нормально», «Агентный дизайн»-свод 15.08 — «ок».

      Закрыто живой прогонкой 15.08 поздно вечером (владелец за машиной,
      стенд g3-final): «прогресс по чанкам» из пункта вычеркнут — счётчик
      «фрагмент N из M» до панели, куда смотрит человек, **не доносится**
      (владелец кликал по считающейся встрече), и владелец счёл его ненужным:
      потребность пункта — «долгая генерация не выглядит зависанием» — закрыта
      обычной индикацией активности, которую владелец видел. Счётчик остаётся
      деталью лога; выкинуть или донести до UI — решение после релиза.

      Попутное наблюдение того же прогона: на «Агентном дизайне» гвард на этот
      раз отдал документ **сборке** (свод 12 пунктов против 44 у сборки) —
      та же встреча, что днём дала свод на 25 пунктов, но уже на новом билде
      (7 фрагментов после исправления байты/символы). Автор пути нарезки на
      одной встрече не детерминирован между билдами; доля `assembly` в проде —
      телеметрия `author`, смотреть первые дни
- [x] Встреча с notes → заметки в документе, текст не переписан — стенд 15.08
      (вечер), две настоящие встречи архива (`20260810_134523` «Камуфляж»,
      `20260808_173043`): в перегенерированных конспектах блок «## Заметки»
      стоит после «Итога», текст **байт-в-байт** равен `notes` из индекса
      (110 и 331 байт), остальные разделы на месте (7 заголовков)
- [x] Тишина / пустая встреча → честный отказ, не мусор — стенд 15.08 (вечер),
      вход синтетический (встреч без речи в архиве нет): запись с пустым
      `transcript` получила terminal `noSpeech` («в записи не нашлось речи»,
      `AppState.runSummarize`) за 67 мс, без конспекта, одна попытка,
      `stopped: finished`; запись с одной репликой «Салам» — не отказ по коду
      (транскрипт не пуст), модель честно написала «Встреча не состоялась…»,
      выдумки нет
- [x] Kill приложения посреди генерации → рестарт → бэкфилл догнал, дублей и
      битых файлов нет — стенд 15.08 (вечер), 4 средних встречи без конспектов:
      `pkill -9` на 57-й секунде генерации первой (битого файла не осталось),
      рестарт → «launch: starting, 4 owed» → 4/4 конспекта за 5,5 мин,
      `stopped: finished`, по одному файлу на встречу (106 всего, дублей 0),
      в индексе все `summarized`, `lastFailure` пуст, 0 failed/cancelled
- [x] Живая запись стартует во время бэкфилла → запись не страдает, генерация
      встала и догналась после — живая прогонка 15.08 (владелец, ⌘R посреди
      генерации бэкфилла): `stopped: cancelled` в момент старта записи,
      запись `20260815_231944` получила транскрипт и конспект **вне очереди**
      за 38 с (23:20:19 transcribing → 23:20:22 summarizing), бэкфилл догнался
      сам (23:20:37 → …), финал `stopped: finished`, 3/3 конспекта стенда
- [x] Модель не скачана → приложение находит или скачивает её само
      (`AppState.ensureSummaryModel`, «нет LLM» — незаконное состояние;
      формулировка «honest empty state» была протухшей — она старше
      ModelProvisioning, поправлено 15.08 по слову владельца). Проверено
      живьём 15.08: `ollama rm qwen3.5:4b` при закрытом приложении → запуск →
      без единого действия человека модель снова в сторе (3,4 ГБ, ~6 мин);
      лог-строк провижининг не пишет — если захочется видеть это в
      `log stream`, это одна строка debugLog
- [x] Провайдер openai/claude → путь вообще не задет — по коду: каждое
      изменение релиза под `backend == "ollama"` (ретрай и пороги — только
      `callOllamaTracked`; облако не режется никогда —
      `testНарезкаТолькоДляЛокальногоПути`; облачная ветка `callBackend`
      не менялась ни в одном коммите релиза)
- [x] Апдейт через Sparkle на архиве → старые конспекты нетронуты — живой
      апдейт 16.08 00:30: ни один из 106 recap-файлов не менялся в окно
      апдейта (`find -newermt`, 0 файлов; хэш-слепок в этот раз не снимался,
      доказательство — mtime + счёт)
- [x] Тот же апдейт — с **реально установленной ad-hoc сборки**: 16.08,
      владелец за машиной. Старая собрана из тега `v1.16.4` (ad-hoc,
      `TeamIdentifier=not set`), фид локальный с финальным нотаризованным DMG;
      Sparkle предложил 1.16.5, поставил, после перезапуска —
      `TeamIdentifier=9T455555U3`, `flags=runtime`, `spctl accepted`.
      **Риск №3 снят**: переход ad-hoc → Developer ID проходит
- [x] Скачанный DMG на чистом Mac — **закрыт заменителем** (15.08, слово
      владельца: чистого Mac нет ни у кого — Propeller уже стоит у всех
      доступных людей). Заменитель: копия DMG с карантином Safari — `spctl` →
      `accepted`, `source=Notarized Developer ID`, то есть Gatekeeper-путь
      чистой машины пройден его же механизмом. Натурный тест случится сам на
      первом новом коллеге; если двойной клик там не сработает — это will-hurt
      немедленно, канал жалобы короткий
- [x] Ollama распаковывается и стартует под hardened runtime — 104 генерации
      стендов 15.08 на установленном нотаризованном билде (Developer ID +
      runtime), живой лог раннера читался («prompt cache is disabled»),
      ноль failed

### Г4 · Нагрузка

Стенд 2026-08-15: копия архива в `/Users/Shared/propeller-stand/.meeting-recorder`
(APFS `cp -c`, конспекты удалены **в копии**), установленное 1.16.5, ollama
`qwen3.5:4b`, M5 / 16 ГБ, **от батареи**. Настоящий `~/.meeting-recorder` только
читался; после прогона пути в настройках возвращены, приложение закрыто.
Отдельного macOS-пользователя не заводили — изоляцию дали `recordingsPath` /
`meetingsPath`, архив-источник это защищает так же, а учётку и системные
настройки трогать не пришлось.

- [x] Бэкфилл 52 встреч подряд (16:56:07 → 18:18:17, `stopped: finished`),
      52 конспекта из 52, ни одного `failed` / `cancelled`:
      - **время:** 82 мин на 52 встречи; на встречу — медиана 90 с, среднее 94 с,
        min 14 с, max 297 с
      - **память приложения:** пик 123 МБ, среднее 100 МБ — не фактор
      - **память ollama** (`serve` + `llama-server`, сумма): медиана 6209 МБ,
        p95 11 310 МБ, **пик 12 349 МБ**; суммарный пик с приложением
        **12 448 МБ** в 17:35:44. Выше 8 ГБ — 20 % сэмплов (189 из 942)
      - **форма кривой:** 5,0 ГБ на старте → монотонный рост до 12,3 ГБ за
        ~40 мин → сброс до 5,8 ГБ → качели 5,5–7,5 ГБ до конца. Пик снят на
        встрече с транскриптом 604 байта, то есть он **не** объясняется
        текущей встречей: это накопленный резидент раннера. Причина роста и
        причина сброса **не выяснены** — мерили снаружи, внутрь не смотрели
      - **батарея:** 100 % → 32 % за 81 мин (~50 % в час)
      - **термалка:** ноль. `pmset -g thermlog` пуст, ни одного сэмпла с
        `CPU_Speed_Limit`, ни одной строки `[pipeline] thermal` за весь прогон
      - **сеть:** прогон целиком от батареи. **От сети не мерено** — зарядку
        втыкает человек
- [ ] **8 ГБ — этой машиной не закрыть, нужен Mac тестера.** Здесь 16 ГБ, и
      пик 12,4 ГБ уложился только поэтому. На 8 ГБ те же 12,4 ГБ — своп, и это
      уже не «пик не вырос», а другой продукт. Мерить надо там же, где живёт
      этот вопрос
- [ ] Сравнение пика с 1.16.4 — **не сделано, кита нет.** В `dist/` только
      1.16.5, тег `v1.16.4` есть, но собранного артефакта нет. Числа выше
      записаны **как база**; когда появится старый кит, сравнивать с ними
- [ ] Приоритет живой записи над бэкфиллом за Ollama — **не проверен.** Живую
      запись во время бэкфилла из сессии не запустить: у `osascript` нет права
      слать события ввода (ошибка 1002), выдача этого права — системная
      настройка. Нужен один ⌘R человеком на новом прогоне бэкфилла

Мерили скриптами в `/Users/Shared/propeller-stand/`: `stand-build.sh` (клон
архива), `stand-watch.sh` (сэмплер), `stand-report.sh` (сводка). Две ловушки,
каждая из которых давала неверные числа и обе записаны в шапке `stand-watch.sh`:
исполняемый файл называется `MeetingRecorder`, а не `Propeller`; модель держит
**не** `ollama serve`, а дочерний `llama-server` — сэмплер по одному `ollama`
показывал 27 МБ при резидентных 3,4 ГБ.

**Причина роста и сброса найдена, фикс уехал в 1.16.5 (2026-08-15, вторым
заходом).** Виновник — prompt cache llama-server (`--cache-ram`, по умолчанию
8192 MiB): KV-стейт каждого отработанного промпта остаётся резидентным —
354 MiB на 8107 токенов (~44 КБ/токен), а конвейер конспекта промпты не
повторяет, так что кэш — чистый балласт. Замер по PID (`stand-watch-pids.sh`,
сокращённый прогон 12 встреч, run-phase1): **один** llama-server, RSS
5,2 → 10,2 ГБ за 14,5 мин при `ollama ps` неизменных 3,6 GB; в его логе кэш
дорос до 22 промптов / 5483 MiB за 69 вызовов. Сброс базовой кривой до
5,8 ГБ — перезагрузка раннера на 32k-встрече (footprint 32k-раннера как раз
5,7–5,8 ГБ), качели — 11 32k-встреч, каждая перезагружает раннер дважды
(конспект 16k→32k, метадата 32k→16k) и сбрасывает кэш. Параллельные раннеры
за весь прогон жили один раз и 10 секунд — гипотеза «чехарда бакетов держит
несколько моделей» отпала замером.

Фикс: `LLAMA_ARG_CACHE_RAM=0` в окружении серва, который поднимает приложение
(`PropellerPure/OllamaServeTuning.swift` с тестами; llama-server наследует env,
своего `--cache-ram` Ollama в аргументы не пишет — проверено по командной
строке раннера и строке `prompt cache is disabled` в его логе). Чужой
адоптированный Ollama на :11434 остаётся со своим кэшем — его окружение не
наше. Живой путь не платит: холодных загрузок не добавилось, кэш окупался
только на ретрае схлопнувшегося ответа (~9 с переоценки промпта, 859 ток/с).

Приёмка на полном стенде (52 встречи, 19:19–20:35; серв — свой, приложения:
homebrew-серв на время прогона остановлен и после возвращён, иначе фикс не в
деле):

| метрика | база (16:56) | фикс (19:19) |
|---|---|---|
| пик ollama, рабочая фаза | 12 349 МБ | **5 910 МБ** |
| сэмплов выше 8 ГБ | 20 % (189/942) | **0** |
| форма кривой | монотонный рост ~40 мин | плоско 4,8–5,9 ГБ; старт 5321, финал 5316 |
| время на 52 встречи | 82 мин | 75 мин |
| конспекты | 52/52 | 52/52, ноль failed |
| термалка | ноль | ноль |

Оговорка к пику: сырой максимум серии — 17 166 МБ **одним тиком на +10 с, до
первой встречи**. Это старт свежего серва Ollama 0.32.x: GPU-discovery-проба
(отдельный короткоживущий llama-server, 6 с) внахлёст с загрузкой рабочего
раннера, и сумма phys_footprint двоит замапленные обоими веса. База этого
тика не имела только потому, что её серв был запущен за трое суток до
прогона. После первой минуты выше 5 910 МБ нет ни одного сэмпла из 869.

Оговорка к Г5: стенд шлёт телеметрию в прод. Все `Propeller.Recap.generated`
от 15.08 начиная с 16:56 — это бэкфилл стенда, не живые встречи. Прогоны
18:55–19:15 (run-phase1) и 19:19–20:35 (run-accept) шли с
`analyticsEnabled=false` (после возвращено как было) — в проде их нет.

### Г5 · Телеметрия работает до релиза

- [x] События с моей машины видны в TelemetryDeck: живой
      `Propeller.Recap.generated` от 14.08 20:19 несёт все поля —
      reply_tokens 190, retried 1, collapsed 1, bullets 2, generator 2,
      ram 16, 20,7 с во floatValue (сверено запросом 2026-08-15). Поле
      `author` **приехало и различает пути** (стенд 15.08): `chunked=1`
      встретился дважды, `author=digest` и `author=assembly` соответственно —
      Г3. Но эти два события, как и остальные 50, отправлены стендом, а не
      живыми встречами.
      Оговорка к чтению: `collapsed=1` на записях короче ~10 минут — ложная
      тревога по построению (пол 800 токенов рассчитан на настоящую встречу),
      долю схлопываний считать только по встречам от ~10 минут
- [ ] Это же — критерий отката после релиза: доля схлопываний и время генерации
      в проде против стендовых. Смотрим первые дни, не ждём жалоб

### Г6 · Откат

- [x] Прошлая версия приложения поверх новых данных — даунгрейд руками 16.08
      (тот же прогон, что Sparkle-тест): v1.16.4 из тега поверх свежего
      архива, владелец глазами — список полный, конспекты читаются, ничего не
      пересчитывается, не падает. Оговорка честности: устойчивость к **полю
      версии** в индексе на реальных данных не экзерсировалась — поля там не
      оказалось вовсе (см. находку ниже); держится декодером (незнакомые поля
      игнорируются) и инвариантом I6.

      **Находка того же вечера, не блокер:** телеметрия 14.08 слала
      `generator=2`, а в реальном индексе (52 записи) ключа версии нет ни у
      одной — штамп до реального `recordings.json` не доезжает. Бэкфилл
      версию не читает, откат от неё не зависит; разобраться после релиза.

## Порядок работ

1. Порт: t=0 + ретрай, версия генератора, телеметрия — один кусок; нарезка +
   механическая сборка — второй. Каждый коммитится по зелёному сьюту.
2. Г2–Г3 по мере готовности кусков; Г4–Г6 на собранном ките.
3. Релиз одним апдейтом Sparkle вместе с накопленным в CHANGELOG
   («Как готовить релиз» там же); после релиза этот файл → `archive/`.

## Definition of Done

Все гейты зелёные. После релиза: телеметрия первых дней без аномалий против
стенда + разговоры с юзерами про качество саммари — кроме них, «стало ли лучше»
не скажет никто.
