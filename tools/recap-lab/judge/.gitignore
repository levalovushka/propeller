# Слепые копии — те же 48 recap.md из out/gate, а out/ репозиторий не хранит
# («прогоны — артефакт замера, а не исходник»). Соответствие ячеек и прогонов
# зафиксировано в mapping.json, сами копии восстанавливаются blind.py.
blind/
