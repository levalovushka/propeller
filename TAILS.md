# Хвосты

Мелочи, которые остаются после работы и теряются: не цель роадмапа и не дефект
компонента, а «осталось» размером в один вечер. Новое — в конец, снял — вычеркнул
галочкой и удалил через месяц.

**Что сюда не идёт.** Цели и горизонты — `ROADMAP.md`. Дефекты компонентов —
`STATE.md`, Часть II. Продуктовые решения — `product-ideas.md`. Что сделано —
`WORKLOG.md`. Если хвост уже описан в одном из них, здесь его нет: только ссылка,
если она вообще нужна.

**Откуда берётся.** Работа кончилась, в записи `WORKLOG.md` появился блок
«Осталось» — то из него, что является чьей-то будущей работой, переезжает сюда
строкой. Свойства сделанного («тестам недоступен `Sources/`») остаются в логе:
это не работа, а ограничение.

Собрано 2026-08-19 выгребкой 62 блоков «Осталось» из `WORKLOG.md`.

## Открыто

- [x] **На созвоне 1×1 имён нет вовсе.** Снято в тот же вечер: реализован ход из
  этого же хвоста — `CallWindowJournal.soleCompanion` (f5faffa) подписывает чужие
  реплики второй плиткой по якорю `Preferences.ownerZoomName`; правило проверено
  по трассе `213214` (218 опросов постоянной пары). Числа — `DIARIZATION.md`,
  третий замер. ← снято 20 авг
- [ ] **Пробы «до вывода метод исчерпан» на 1×1.** Осталось из снятого хвоста:
  полный набор AX-атрибутов плитки на 1×1 (рамку говорящего Zoom может держать
  в нечитаемом пробой атрибуте); вид «Динамик» и мини-окно на 1×1; панель
  участников (`AXRow`) как источник «говорит». Одна встреча с телефоном. ← 20 авг
- [ ] **Микрофон, выключенный в звонилке, всё равно попадает в расшифровку.**
  Замерено на той же встрече: из 23 реплик владельца в ленте **21 сказана при
  замьюченной плитке Мака** (мьют с 7-й секунды, 246 опросов из 287). Человек
  ждёт обратного: на встрече он в этот момент молчал. Сигнал для Zoom богатый и
  уже разбирается (`Tuning.mutedMarkers`), но применяется только как вето для
  чужой плитки (`CallWindowJournal.swift:227`). Чего не хватает: **своя плитка не
  опознаётся** — Zoom её не помечает, в хвостах описаний только
  `звук компьютера включен/выключен`, `video on/off` и третий, невнесённый в
  словарь маркер **`звуковое устройство не подключено`** (3 опроса). Якорь
  предлагается тот же, что выше: запоминать zoom-имя владельца, когда замок его
  вычислил на обычной встрече. Решение по поведению: **помечать реплику и не
  пускать её в саммари, а не вырезать** — ложный мьют стоит потерянной настоящей
  речи, а вред не в строке ленты, а в том, что несказанное становится решением
  или задачей. Ложится туда, где уже стоит `noteOwnerTurn`: мьют известен с шагом
  0,4 с, реплика финализируется позже, значит показанное переписывать не
  придётся. Для не-Zoom сигнала нет: остаётся пауза горячей клавишей (цель D
  роадмапа) плюс один замер `kAudioProcessPropertyIsRunningInput` готовой пробой
  `tools/shadow-mic-probe/` — он либо даёт универсальный сигнал, либо закрывает
  направление. ← 20 авг
- [x] **Отсутствие права на «Универсальный доступ» ничем не заметно.** Снято
  (f5faffa): `CallWindowObserver.start` при отказе пишет строку в лог и шлёт
  `CallJournal.noPermission` — по сигналу на запись. ← снято 20 авг
- [ ] **`og.png` и `preview.png` несут старый знак.** Скриншоты окна от 10 августа,
  знак сменился 17-го; favicon и apple-touch тогда пересобраны, эти два нет. Это
  картинки, которые видит человек, впервые получивший ссылку. Подтверждено по
  mtime 21 авг: `preview.png` 10 авг 13:34, `og.png` 10 авг 13:47 против
  `mark.svg`, `favicon.*`, `apple-touch-icon.png` — 17 авг. ← 17 авг
- [x] **Судьба `site/assets/wordmark.svg`.** Удалён 21 авг по решению владельца:
  ссылок ноль, шапка рисует `assets/mark.svg` (`site/index.html:320`). Локап
  живёт в Фигме, оттуда и вернётся, если понадобится. ← снято 21 авг
- [x] **Шесть мёртвых токенов типографики.** Снято 21 авг (e270c24), и их
  оказалось семь: `Theme.pageHeader` — не токен, а невызываемое расширение `View`
  целиком. `ToastBar` описывал полосу, удалённую вместе со всей очередью тостов
  (`design/notifications.md`), то есть токены пережили то, чем мерили.
  `Heading.lg` оставлен: им рисует `MeetingsTitleBlock.swift:46`. ← снято 21 авг
- [ ] **Девять стилей в Фигме без образца-простыни.** Сравнить их глазами негде;
  сортируются по алфавиту, из-за чего `2xs` идёт первым внутри `Interface/`. ← 19 авг
- [x] **Снимков retention нет в галерее состояний.** Хвост неверен, снят
  проверкой 21 авг: галерея не молчит, а сознательно исключила это состояние —
  `design/state-gallery.md:166-168`: «„Аудио удалено“ как состояние удалено
  вовсе: удалил человек — встречи в списке нет, удалили мы ради места —
  транскрипт и саммари живут как обычно». Решение, а не недосмотр. ← снято 21 авг
- [ ] **Начать откладывать стемы.** Retention съел звук 53 встреч из 55; пока
  стемы не откладываются отдельно, следующий замер диаризации начинается с нуля.
  Срочность не в объёме работы, а в том, что материал исчезает каждый день. ← 18 авг
  **Проверено кодом 21 авг, и это не работа, а один переключатель.** Удаляются
  микс и оба стема разом (`RecordingStore.audioFileURLs:365-370`, цикл
  `applyAudioRetention:448-451`), рычага «сохранить стемы» нет нигде — искали
  `keepStems`, env-флаги, отладочные обходы. Но режим **«Месяц»**
  (`AudioRetentionMode.afterDays`, `AudioRetention.days = 30`) хранит звук
  тридцать дней от даты встречи (`AudioRetention.expired:147-150`), а по
  умолчанию стоит «До расшифровки». **Режима «Всегда» больше не существует** —
  `keep` удалён 17–18 августа (`AudioRetention.swift:70-72`), поэтому три хвоста
  ниже, которые ждут «одну встречу с хранением „Всегда“», ждут того, чего нет.
  Хватит «Месяца». **Владелец поставил его 20 августа** — проверено приборами
  21 авг: `defaults read com.simplyai.meeting-recorder audioRetentionMode` →
  `afterDays`, `audioRetentionReset` → `1`, то есть одноразовый сброс уже
  отработал и выбор больше не затрут. Материал копится; три хвоста ниже ждут
  теперь одной встречи, а не настройки. Сама работа «откладывать стемы отдельно»
  от этого не исчезла — но перестала быть срочной.
- [x] **Посмотреть телеметрию первых дней 1.16.5.** Снято 21 авг, TQL через MCP,
  окно 30 дней. `Recap.generated` — 159 событий, `collapsed=1` у **51 (32 %)**;
  `author=assembly` — **6 у трёх человек** на 1.16.5, ноль на 1.16.6.
  Разрез «от 10 минут» сделать нечем (см. хвост ниже), но косвенно он и не нужен:
  секунды генерации у здоровых медиана 120 с (p10 69), у схлопнутых 43 с (p90 80) —
  схлопывается почти исключительно короткое, ровно та поправка «на огрызках
  ложный». Побочно честная воронка на 1.16.6: `Recording.finished` 29 против
  `Recap.finished{ok, first=1}` 21 → **72 % встреч доходят до саммари**, и все 21
  `ok` пришли с `first=1` (перегенераций пока не было). Людей 57, было 42.
  **Имена сигналов на сервере с префиксом `Propeller.`** — запрос без него отдаёт
  пустой результат, это стоило одного запроса вслепую. ← снято 21 авг
- [ ] **Дашборд под `Claude.used` не заведён.** Сигнал шлётся, посмотреть негде;
  MCP-коннектор дашборды создавать не умеет, это руками в UI TelemetryDeck.
  Половина хвоста снята 21 авг: **живая отправка наблюдается** — 12 событий у
  одного человека за 30 дней. ← 15 авг
- [ ] **Калибровать `digestMinBulletShare` по проду.** Порог 0,5 выбран от A5.1 и
  14 буллетов сборки на фикстуре; сколько держит здоровый свод — не замерено.
  **«Считается после телеметрии» оказалось неверным (21 авг): существующими
  сигналами не считается вообще.** `RecapDigestGuard.Decision.reason` наружу не
  уходит, поэтому из шести срабатываний `assembly` не видно, сколько пришло от
  «свод схлопнут после ретрая», а сколько от порога по буллетам — а калибруется
  только второе. Нужен один параметр к `Recap.generated` (причина отбора), и это
  правка лексикона, который чистили сознательно 19 августа: решение владельца, не
  работа. Заодно тот же сигнал не несёт длительности встречи — отсюда невозможный
  разрез «от 10 минут». ← 15 авг
- [ ] **Поговорить с людьми, которые поставили 1.16.5.** Телеметрия покажет, что
  происходит, но не почему; DoD раздачи — «≥1 саммари без пинга автору». ← 16 авг
- [x] **Нет теста маршрутизации «влезает / не влезает»** на уровне `RecapService`.
  Снято 21 авг: ветка вынута в `RecapRoute.of(backend:promptCharacters:)`
  (`PropellerPure/RecapBackendChoice.swift`), три её случая и согласие с самим
  порогом покрыты `RecapBackendChoiceTests`. Прежний текст:
  Порог покрыт тестами `TranscriptChunking.needed`, а выбор пути — нет; Г2 ловил
  это руками. **Проверено 21 авг: сегодня такой тест не написать.** Ветка стоит в
  `Sources/RecapService.swift:316-318` и `:330/339/350`, `RecapService` живёт в
  executableTarget, а `MeetingRecorderTests` зависит только от `PropellerPure`,
  `PropellerUI`, `SpeakerMatchingCore`, `SnapshotTesting` — ни одного
  `@testable import MeetingRecorder` в дереве нет. Шов `RecapBackend`
  (`PipelineBoundaries.swift:40-77`) обёртывает вызов целиком и решения внутри не
  показывает. Значит либо таргет в зависимости тестов, либо вынести саму ветку
  в `PropellerPure` — второе дешевле и уже записано в
  `archive/plan-testing-metrics.md:97`. ← 14 авг
- [x] **Решить судьбу авто-снапшотов `swift/benchmarks/report-*.md`.** Решено
  21 авг: вход и рассуждение в гите, выход нет. `latest*.json` и авто-`report*.md`
  выведены из-под гита (`git rm --cached`, файлы на диске целы), `baseline*.json` и
  три разбора прозой остались. Заодно поправлен `DOCS.md`: глоб `report-*.md`
  относил к артефактам и `report-gate.md`, который цитируется как довод. ← снято 21 авг
- [ ] **`axprobe` под Толк: `--process` и ленивое AX-дерево Electron.** Фильтр
  процессов прибит к «zoom» (`tools/diarize-lab/axprobe/Sources/axprobe/main.swift:51`),
  нужен параметр. Толк — Electron: перед обходом дерева ставить
  `AXManualAccessibility` (это рукопожатие с чужим процессом, а не чистое чтение).
  Проверяется только на машине друга, в релизной версии; не начинать до гейта Г0 —
  Zoom это 89 % встреч архива. В `plan-speaker-tags.md` Толк как работа не описан,
  только как открытый вопрос §10.1. ← 19 авг
- [ ] **Замерить оставшиеся хвосты ревизии на живых встречах Zoom.** Осталось
  два вопроса: двухмониторный режим (Н9 — сейчас это вечное молчание) и
  наложение речи (Н10 — ставит ли Zoom две метки `active speaker` сразу).
  Мьют (Н8) замерен второй трассой 20 авг: «Звук компьютера выключен», дефолт
  в `Tuning`. Всё — `plan-speaker-tags.md` §2. ← 20 авг
- [x] **Вид ленты живёт в шести местах.** Дисковая половина снята 21 авг:
  форму `**Иван** · 12:34` держит writer (`MeetingMarkdown.diskHeadBody`,
  `diskHeadPattern`, `diskTurnLookahead`, `diskHead`), `TranscriptChunking` и
  `RecapDocument.participants` читают её оттуда, а тест
  `testWhatTheWriterEmitsIsWhatTheReadersMatch` сводит «как пишем» с «как
  читаем» (проверен на срыв: подмена `·` на `—` роняет и ростер в пустой
  список — ровно тот отказ, ради которого написан). **Проверка исправила
  запись:** вид в памяти общей константой уже жил, у дискового не было ни одной.
  Осталось два отдельных хвоста ниже. Прежний текст: Внутренний вид (`[Кто] [12:34]`) читают
  четверо: `Timecode.transcriptHeadPattern`, `RecapDocument.participants` (ветка 1),
  `TranscriptPresentation.parseRemarks:194` (дискового вида не знает — файл на входе
  молча схлопнет подписи в «Unrecognised line») и сам writer как источник; дисковый
  (`**Кто** · 12:34`) — двое: `MeetingMarkdown.transcriptBody:129` и приватный
  `TranscriptChunking.turnPattern`. Расхождение уже стоило инертного ростера в
  промпте (2026-08-20, чинено 4163e48). Закрыть оба списка одной именованной
  константой у writer'а. ← 20 авг
- [ ] **Минута «Динамика» на текущем Zoom — судьба ветки геометрии.** По 16 581
  боевому опросу геометрия не сработала ни разу (все — галерея/шер, плитки
  равны); живёт она ради вида «Динамик», замеренного 2026-08-19 (1080×600
  против 160×80, меток в дереве нет). Одна трасса `axprobe trace` в «Динамике»
  на двоих решит: размеры в дереве разные → ветка живёт; равная полоса → снести
  вместе с порогом. ← 20 авг
- [ ] **Лабораторная линейка мерит пролёты, продукт — ближайший образец.**
  `saer-journal.py` считает по накрывающему пролёту, а лента после упрощения
  подписывается правилом «ближайший образец ≤1 с» (полнее на стыках). Научить
  `CallWindowJournalLab` отдавать и по-образцовую разметку, чтобы линейка мерила
  отгруженное правило. ← 20 авг
- [ ] **Правило для запятой в display name (Н11).** «Иванов, Иван» режется в
  «Иванов», двое таких склеятся под одной подписью: `CallWindowJournal`
  `name(fromDescription:)` берёт `split(separator: ",", maxSplits: 1).first`
  (`CallWindowJournal.swift:123-131`). **Поправка 21 авг: «маркеры знаем» —
  неправда про код.** В `Tuning` лежит один — `mutedMarkers:
  ["звук компьютера выключен"]` (`:110-117`); «звук компьютера включен»,
  `video on/off` и «звуковое устройство не подключено» не встречаются в дереве
  ни разу. Они известны из трасс, а не из кода, поэтому разбор с конца начинается
  с внесения списка в словарь — иначе резать нечем. ← 20 авг
- [x] **Доснять контрольную трассу «Zoom открыт, встречи нет».** Снята 20 авг
  (`axprobe-trace-control-nomeeting.jsonl`): 132 опроса, ноль плиток, журнал
  пуст, всё молчание — `noTiles`. Все три трассы §11.1 закрыты. ← снято 20 авг
- [x] **Эталон к `20260820_100315` и первый SAER журнала.** Размечено владельцем
  (по памяти, 8 спорных исключены); после ревизии пересчитано честной линейкой
  (объявленные имена, дотянутые концы пролётов): журнал 9,4 % на названном
  (подмен людей — 0 с) против 49,6 % у диаризации — `DIARIZATION.md`. ← снято 20 авг
- [ ] **`claude-mcp-seen` в Application Support.** Файл от 19 августа 19:00 — похож
  на живое подключение, а не на мусор дымового прогона 15-го, но проверить может
  только владелец: хук не пускает агента в каталог пользовательских данных.
  **Уточнено 21 авг по коду:** метка ставится на `initialize` и обновляется на
  каждом следующем запуске клиента (`MCPServer/Handshake.swift:46-54`,
  `main.swift:72,84`), а строка складывается из неё и конфига Claude Desktop
  (`MCPCellState.swift:121-135`): нет конфига при живой метке — уже честное
  `.lost` («Больше не видит Propeller»). Врать строка может только в одном
  случае — конфиг цел, а Claude Desktop им больше не пользуются. Смотреть надо
  `claude_desktop_config.json`, а не саму метку. ← 15 авг

- **1.16.5 и 1.16.6 у всех юзеров без доступа к календарю.** Entitlement
  `com.apple.security.personal-information.calendars` появился в подписи только
  20 августа: у всех, кто обновился на нотаризованную сборку, tccd отказывает без
  окна, и названия встреч придумывает модель. Ни `tccutil`, ни галка в System
  Settings этого не лечат — нужен релиз с новой подписью. ← 20 авг

- [ ] **Владелец пропадает, когда говорит поверх собеседника — замерено, частота нет.**
  Микрофонный стем на колонках: речь в свои слоты распознаётся полностью (31/31 при
  эхе до +20 dB), речь поверх звучащей дальней стороны — 2/31, и теряется в ASR, до
  всякого снятия эха. Вычитание системного стема (NLMS, `tools/echo-probe/aec2.py`)
  возвращает 31/31 при ERLE +9.6 dB и чистый случай не портит. Числа и оговорки —
  `swift/benchmarks/report-owner-loss.md`, фикстура `ru-echo-loud-2spk`. Архив потерю
  **не подтверждает** (восемь подозрительных встреч оказались молчанием владельца),
  поэтому это не работа, а замер, ждущий повода: настоящее число даст одна догфуд-
  встреча, где сознательно говорить поверх собеседника, при хранении звука
  «Месяц» (режима «Всегда» больше нет — см. хвост про стемы). ← 20 авг
- [ ] **«Микрофон разрешён, но звучит тишина» приложение не отличает от «все молчали».**
  `capturedUsableStem` смотрит только системный стем, `maxMicLevel` уходит в строку
  лога, `micCaptureWarning` ставится и не читается ничем, `micOnlyCaptured` пишется в
  запись без бейджа (комментарий в `AppState.swift` про бейдж неверен), в
  `recordings.json` здоровья захвата нет вовсе. Поэтому жалобу внешнего тестировщика
  («ни одного моего слова») нечем было ни подтвердить, ни опровергнуть — ни у него, ни
  по своему архиву. Чинить, когда такой случай повторится у кого-то, до кого можно
  дотянуться; тогда же — четвёртая строка в плиту доступов. ← 20 авг

- [ ] **Потоковый микс не проверен на живой встрече.** Побайтовое совпадение с
  прежним алгоритмом доказано на фикстурах и на синтетической 50-минутной паре, но
  отгруженный код (`AudioRecorder.mix`) тестам недоступен, а отказ микса невидим:
  транскрипт идёт из стемов, длительность — с часов захвата, фолбэк молча копирует
  микрофонный стем. Две встречи 20 августа прошли на новом коде, но retention
  `afterTranscript` снёс аудио. Нужна одна запись, чей звук переживёт расшифровку —
  тогда сравнение того, что написало приложение, с независимо посчитанным миксом
  проверит именно отгруженный код. Режима «Всегда» нет с 18 августа: годится
  «Месяц» (`afterDays`, 30 дней), см. хвост про стемы. ← 20 авг
- [x] **Допуск `live.app_cpu_cores` лежит внутри собственного разброса.** Снято
  21 авг по решению владельца: `+20%` → `+0.004` в `Bench/LiveRun.swift` **и в
  обоих базлайнах** — `bench-diff` берёт допуск из базлайна (`tol = b.get(...) or
  l.get(...)`), одной правки кода не хватило бы. Проверено на тех самых двух
  числах: `+20%` даёт `FAIL (allow +0.00148)`, `+0.004` — `OK within tol`. ← снято 21 авг
- [ ] **Фикстура `ru-pauses-2spk` не переснята.** Базлайн ru-short-2spk обновлён
  20 августа обоими харнессами, у второй фикстуры свой базлайн и свой
  `Verdict: FAIL`, её живой слой не мерили. ← 20 авг
- [ ] **Причина +74 МБ пика офлайнового прохода — предположение, а не замер.**
  Транзиентный пик вырос, удержанная память нет, `diarize.rtf` −67 %, в том же
  окне пин FluidAudio 0.13.6 → 0.15.5. Подтверждается только откатом пина с
  перезамером, а откат тянет перепроверку `prepareModels` (макось-14 краш). ← 20 авг
- [ ] **Выигрыш по памяти от потокового микса нечем показать в бенчах.** `Bench`
  `produceFinalMix` не зовёт вовсе — он читает готовый `final.wav` фикстуры. Пока
  единственные числа (560,4 → 14,0 МиБ) сняты черновым харнессом вне репозитория.
  **Проверка 21 авг уточнила цену:** это не «должен режим харнесса», а переезд.
  `produceFinalMix` — статик на `AudioRecorder` в `Sources/` (executableTarget), а
  `Bench` зависит от `SpeakerMatchingCore`, `PropellerMetrics`, `PropellerPure`,
  `FluidAudio` (`Package.swift:81-89`); исполняемый таргет SPM не импортируется
  вообще. Сначала микс переезжает в библиотечный таргет, только потом появляется
  режим. ← 20 авг
- [x] **`resolveBackend` и `throwIfBadHTTP` остались в `Sources`.** Снято 21 авг:
  `RecapProviderKind`, `RecapSkipReason`, `RecapError` переехали в
  `PropellerPure/RecapBackendChoice.swift` вместе с самим выбором
  (`RecapBackendChoice.resolve`) и правилом «какой ответ считать отказом»
  (`httpFailure`). Снаружи остался один вопрос к сайдкару — пригодна ли Ollama, —
  и он входом. Прежний текст: Они держатся
  за `RecapProviderKind`, `RecapSkipReason` и `RecapError`; переезд этих типов в
  `PropellerPure` — свой шаг, после него решение «какая модель отвечает» станет
  проверяемым. Замерено 21 авг: пять файлов и около двадцати ссылок —
  `RecapService.swift` (там же `RecapResult`), `PipelineBoundaries.swift:57`,
  `Preferences.swift:181,191,194`, `SettingsPane.swift:295,325,334,445-446`. Тот
  же шаг разблокирует тест маршрутизации выше. ← 20 авг
- [x] **Три однострочные обёртки над `Timecode`.** Снято 21 авг, и диф оказался
  не косметическим: три теста этих обёрток дублировали `TimecodeTests` под именами
  «что увидит человек». Переписаны на то, о чём назывались — заметка через
  `MeetingNotes.blob`, вырезка через `render`. ← снято 21 авг
- [x] **Префикс `Temp` на пяти постоянных инструментах.** Снято 21 авг:
  `RailShotTests`, `AshFilmTests`, `SettingsShotTests`, `NotchShotTests`,
  `SwitcherShotTests`, плюс «Scratch tool» в доке заменено на «A tool, not a
  test». Заодно поправлена устаревшая строка `STATE.md` §«Сломано»: там стояло
  «один тест пропускается», а их пять. ← снято 21 авг
- [ ] **Терминов безопасности нет в подсказках распознавателя.** `passkey`, `totp`,
  `otp`, `2fa` — ноль вхождений в `vocab/hotwords-core.txt`, `hotwords-full.txt` и
  `BuiltinHotwords.swift`, а жалоба из поля именно про них. Работа не в коде, а в
  словаре: собрать блок, прогнать `vocab/mine.py --audit` и переклеить обе
  стороны, иначе `testBuiltinHotwordsMatchVocabFile` покраснеет. До тех пор ответ
  спрашивающему — «Личный словарь» в настройках (`SettingsPane.swift:390-401`).
  **Две поправки от 21 авг.** Пример «короткое „отп“ стоит на „отпуске“» не
  воспроизводится: `audit()` сверяет целиком совпадающую фонетическую подпись
  (`mine.py:112-146`), а `signature("отп")` = «атп» против «атпуск» — то есть
  скрипт этого не покажет; настоящий пример такого вреда другой и записан в коде
  («Кикс» превратил «диплинки» в «диплин Кикс», `BuiltinHotwords.swift:81`).
  И формат: оба `hotwords-*.txt` — **одна строка** через запятую, без секций и
  весов, а `testBuiltinHotwordsMatchVocabFile`
  (`PureFunctionTests.swift:199-216`) сравнивает **упорядоченные** массивы, так
  что порядок вставки обязан совпасть по обе стороны. ← 20 авг
- [ ] **OpenRouter не проверен живым ключом.** Провод подтверждён только отказом
  (`401 User not found` на фейковый ключ правильной формы); настоящего запроса и
  настоящего конспекта через него не было. Пара строк настроек глазами не снята —
  `osascript` в той сессии не имел прав Accessibility. Одна встреча с реальным
  ключом закрывает оба хвоста разом. ← 20 авг
- [ ] **Parakeet v3 не замерен, и это цена решения оставить один движок.** Сменные
  движки расшифровки отложены 20 августа (движок всегда gigastt), но FluidAudio
  0.15.5 уже в зависимостях и уже возит `parakeet-tdt-0.6b-v3-coreml` с русским.
  Если тема вернётся, начинать не с абстракции движка, а с прогона по стенду
  `docs/ASR-QUALITY.md` на тех же встречах, что GigaAM: выбор из двух моделей без
  замера — это выбор наугад. ← 20 авг

- [ ] **Числа телеметрии 21 августа не занесены в `STATE.md` §12.** Замер сделан
  (32 % схлопнутых, 6 `assembly`, 72 % воронки на 1.16.6, 57 человек), но §12
  по-прежнему стоит на числах 19 августа и на 42 людях. Не занёс сознательно:
  `STATE.md` в тот момент правила параллельная сессия. Один абзац. ← 21 авг
- [ ] **`TranscriptPresentation.parseRemarks` дискового вида не знает вовсе.**
  Найдено проверкой 21 авг, отдельно от снятого хвоста про формат ленты. Функция
  разбирает вид в памяти и две исторические формы (`TranscriptPresentation.swift:194,205-206`),
  а `**Кто** · 12:34` не матчит ни одна: такой блок падает в ветку `!matched`
  (`:221-222`) и становится `Remark(speaker: "", text: head, start: 0)` — подпись и
  время теряются. Сегодня это **латентно**: единственный вызов
  (`MainView.swift:859`) кормит её `entry.transcript`, то есть видом в памяти из
  индекса. Стреляет в тот день, когда кто-нибудь скормит ей файл с диска. Одна
  ветка и один тест. ← 21 авг
- [ ] **Вид ленты в памяти читают двумя разными строгостями.**
  `Timecode.transcriptHeadPattern` берёт имя как `(.+?)`,
  `RecapDocument.participants` — как `[^\]\n]{1,60}`. Свести в одну константу
  нельзя механически: `(.+?)` лаксее, и на нём стоит разбор старых архивов в
  `parseRemarks`. Нужно решение — что считать именем, — а не правка. ← 21 авг

- [ ] **Собственный ключ кадра главного окна мёртв на запись, и это принято.**
  Замер 2026-08-21: за один драг 813 → 1014 pt двадцать пять `saveFrame(usingName:)` не
  изменили `NSWindow Frame PropellerMain` ни разу — персистентность окна принадлежит
  `WindowGroup`. Восстановление размера теперь читает ключ SwiftUI, и это работает
  (проверено полным циклом). Хвост в другом: мы держим наблюдатель, который пишет в
  мёртвый ключ, «на случай системы, где запись доходит» — предположение, не замер.
  Либо проверить на другой macOS, либо снять наблюдатель и не делать вид, что у нас есть
  свой фолбэк. ← 21 авг

- [ ] **Ручной старт записи из меню-бара: выходит ли окно вперёд — не измерено.**
  Авто-старт больше не крадёт фокус (снято 21 авг, `bba7657`), но если запись
  начинают руками из меню-бара, окно может подняться другим механизмом: активация
  приложения поднимает его окна, а `MenuBarContentView` «Начать запись» ничего для
  этого не делает специально. Проверить не смог — `osascript` на этой машине без
  прав Упрощённого доступа, кликнуть меню-бар нечем. Замер на одну минуту: другое
  приложение вперёд, клик в меню-баре, `lsappinfo front` до и после. Пока не
  измерено — считать открытым: жалоба была про «стартует запись», а не «стартует
  авто-запись». ← 21 авг
- [ ] **Проверить, что `Notice.recordingStarted.window` ушёл в ноль.** Через неделю
  после 21 авг: `.window` должен исчезнуть, а сумма `.sound` + `.silent` — остаться
  той же, иначе повод потерялся целиком, а не сменил поверхность. Разрез по версии,
  чтобы не считать старые сборки. ← 21 авг

## Снято

- [x] **Версия в `build.sh` зашита как 1.16.5.** Бампнута до 1.16.6 при сборке
  релиза — новые сигналы отделяются на дашборде разрезом по версии. ← снято 20 авг
- [x] **Правило «доля встреч до саммари = два `eventCount`» в `STATE.md` §12
  неверно.** Поправлено в трёх местах §12 прямо в незакоммиченной правке другой
  сессии, как и было условлено: `{result=ok}` считает выдачи, честный счёт —
  `{result=ok, first=1}` начиная с 1.16.6. ← снято 20 авг
