# Propeller — единая карта состояния

_Собрано 2026-07-25, обновлено **2026-07-28** (рефакторинг состояния пайплайна · Контур.Толк · фикстуры границ). **Это источник правды.** Остальные доки (`plan-v2`, `plan-optimization`, `plan-testing-metrics`, `release-review`) остаются как история решений; при расхождении — верить этому файлу и коду._

**Как пользоваться:** документ организован **по компонентам**, а не по раундам планов — под режим «беру кусок приложения и довожу до здорового состояния изолированно». В каждом компоненте: где живёт · что здорово · что сломано · что значит «готово».

**Легенда проверки:**
- ✅ — сверено с кодом (дата в шапке / у пункта)
- 📋 — из доков/ревью, **в коде не сверено**
- ⚠️ — расхождение доков с кодом или между доками

**Тяжесть:** 🔴 потеря данных/доверия · 🟠 сильно бьёт при использовании · 🔵 позже

---

## Часть 0а — 2026-07-28: пайплайн, детект звонков, границы

Крупная перестройка. Полный разбор с обоснованиями — [`meeting-recorder/docs/REFACTOR-PIPELINE-STATE.md`](meeting-recorder/docs/REFACTOR-PIPELINE-STATE.md); ниже — что изменилось по существу.

**Состояние пайплайна: 4 источника правды → 1.** Было: строковый `status` на диске, три эфемерных шага на всё приложение, наличие файла рекапа, `topics == nil`. Стало: `RecordingStage` (что достигнуто, персистентно) + `PipelineActivity` (что идёт, одно на приложение) + `lastFailure` на записи. Удалены `PipelineStep`, `transcribeStep`/`saveStep`/`recapStep`, `busyRecordingID`, `statusMessage`, `statusIsTransient`, `micOnlyRecording`.

**Один воркер вместо трёх планировщиков.** `enqueueOrRunTranscribe`, `reconcilePendingPipeline` и `NSBackgroundActivityScheduler`-бэкфилл заменены очередью, выводимой из стадий (`nextJob` → `PipelineDrain`). Бэкфилл рекапов перестал быть подсистемой: встреча в `saved` — просто встреча, которой причитается работа. Хвостовая рекурсия на 4 async-кадра на встречу убрана.

**Приоритет:** самая новая незавершённая встреча доводится до саммари раньше бэклога.

**Пауза во время звонка** распространяется на весь пайплайн, а не только на бэкфилл: раньше ASR мог стартовать посреди созвона.

**Детект звонков — данные, а не код.** `MeetingPlatform.all` описывает Zoom и **Контур.Толк** (bundle id, helper-процессы, маркеры и холостые заголовки, веб-вкладки). ⚠️ Идентификаторы Толка **не проверены на живой установке** — снять реальные через `./tools/detect-meeting-signals.sh толк talk kontur` во время созвона и обновить строку + тесты.

**Границы наружу подменяемы** (`Transcriber`, `RecapBackend`), а их ответы разбираются чистыми функциями (`BoundaryResponses`) поверх записанных фикстур: 413 от gigastt, «модель ушла в thinking», пустой результат на 200, битый JSON. Раньше эти случаи требовали живого сайдкара и 40-минутной встречи.

**Презентация транскрипта вынута из вьюхи** (`TranscriptPresentation`): реплики, фразы, слова — с тестами на реальных данных.

**Тесты: 31 → 111**, все за ~0,3 с. Чистое ядро `PropellerPure`: 403 → 988 строк.

---

## Часть 0д — 2026-08-05: у записи появился экран, а у встречи — живой транскрипт

Разбор по существу; замеры и оговорки движка — в
[`PLAN-CAPTURE-AND-LIVE.md`](meeting-recorder/docs/PLAN-CAPTURE-AND-LIVE.md) §Э3.

**Запись перестала быть режимом окна.** Было: `isRecording` подменял всю
контентную панель, назад не пускало (`canGoBack` возвращал `false`), выбрать
другую встречу было нельзя. Стало: встреча создаётся в рельсе с первой секунды
(это было и раньше), а панель показывает экран записи **только когда выбрана
именно она** — `AppState.activeRecordingID`. Пока идёт запись, можно уйти читать
любую другую встречу; ⌘. останавливает откуда угодно; звонок, кончившийся сам,
больше не утаскивает с открытой страницы (`selectedRecordingID` меняется, только
если человек и так смотрел на запись). Удаление записываемой встречи из рельса —
это «не записывать её»: `removeRecording` уводит в `cancelRecording`, иначе
строка исчезала бы, а рекордер продолжал писать в файл встречи, к которой не
ведёт ни одна дверь.

**Живой транскрипт (Э3 плана захвата).** Две WS-сессии к gigastt из кадров
общего захвата — микрофон в одну, система в другую, отсюда же атрибуция:
владелец / «Собеседник». Склейка реплик — чистый `LiveTranscript` (11 тестов).
Две сессии стоят ~1 ядро и 547 МБ. Живой слой **ничего не должен встрече**: нет
сайдкара, нет сети, захват микрофонный — текста просто нет, и это глубина, а не
отказ (`design/no-dead-ends.md`).

**Порция звука решает качество — и это замер, а не вкус (2026-08-06).** Кусок,
которым кормят движок, режет речь на сегменты: по 200 мс (как отдаёт захват) —
1.8 слова на сегмент и 25 % WER против офлайнового прохода по тому же звуку, по
2 с — 2.8 слова и 9.7 %; на второй записи 33.6 % → 19.1 %. Оптимум именно на
двух секундах: 2.5 с дают 12.5 %, 3 с — 13.9 %. VAD не влияет вовсе. Приложение
шлёт по 2 с, замер повторяется `tools/live-bench/`.

**Колонка показывает самое глубокое из готового (2026-08-06).** Пока саммари
пишется, на его месте стоит расшифровка, а не «Саммари пока нет»: правило —
`SummaryColumnContent` (6 тестов), и оно же оставляет строку-объяснение там, где
ожидание длиннее минуты («докачается модель», «ждём сервиса»). Смена содержания
идёт по очереди — старое уходит за 0.16 с, новое приходит за 0.26 с, — потому
что крест-накрест два текста проступают друг сквозь друга; проверено покадрово.
Кегль реплики поднят до саммарного (14 / 22): один и тот же текст живёт живой
строкой во время встречи и расшифровкой после, и смена размера между ними
читалась как перерисовка.

**Догадок (`partial`) на экране нет.** Они приходят на ~0.6 с раньше решения и
через полсекунды становятся другим текстом: читающему разницы с решённым не
видно — видно только, что строка под глазами меняется.

**Пауза.** Кадры перестают браться, курсор снимается с якоря
(`CaptureCursor.detachClock`) — в файле паузы нет ни секундой тишины, ни сдвигом
дорожек, и счётчик «сколько раз поехали часы» её не считает. Таймер стоит вместе
с записью: `AudioRecorder.elapsed` вычитает простой, и время на экране совпадает
с тем, сколько звука в файле (замерено пробой).

**Свечение по краям окна снято.** Оно светилось уровнями двух дорожек, то есть
отвечало на вопрос «работает ли захват» тем, что красило края экрана всю встречу.
Теперь на этот вопрос отвечает сам транскрипт.

**Заметки во время записи — те же самые.** Была отдельная `TextEditor`-простыня,
писавшая в блоб `notes` (и однажды затиравшая заметку из оверлея ⌃⌥N). Стала та
же `MeetingNotesColumn`, что у готовой встречи, — `appendNote`, записи, никакого
второго пути к тем же данным. `RecordingInProgressView` удалён.

**Кадров в галерее: 29 → 31** (`rec-live`, `rec-paused`). Тестов прибавилось 14: живой транскрипт (11) и пауза (3).

---

## Часть 0г — 2026-08-05: онбординг стал одной плашкой

Было шесть экранов, стало один — `setup` (Figma 50:1223), плюс блок у подошвы рельса (91:929). Разбор по существу.

**Пять экранов из шести ничего не спрашивали у macOS.** Карусель на четыре слайда описывала приложение тому, кто его только что поставил и узнает больше из первого же звонка. Экран про модель саммари объявлял загрузку, которая идёт сама. «Готово» существовал, чтобы его закрыли. Имя и календарь — вопросы, без ответов на которые приложение работает полностью. Осталось то, без чего первый звонок не запишется: микрофон, уведомления, запуск при входе — и одна кнопка «Начать», которая **не блокируется** ничем: отказавшийся от микрофона получает приложение, которое говорит «Нет доступа к микрофону» на строке записи, а не стену.

**Имя и календарь переехали в рельс** (`SidebarPromptBlock` + `SetupPromptMachine`): один блок 276 pt у подошвы, «Подключите календарь» 1/2 → «Как вас зовут?» 2/2. Спрашиваются там, где видно, ради чего ответ, — рядом со списком встреч, который они улучшают. Кнопки «пропустить» нет и не будет: блок ничего не держит, поэтому отменять в нём нечего; он ждёт ответа. «Подключить» тратит шаг **нажатием**, а не грантом — что ответит система, дело человека и календаря.

**Список под блоком не закрывается, а гасится.** В макете там непрозрачная плита `#141414` на 166 pt; нарисованная буквально, она перестала бы быть окном ровно в том месте, где сидит блок. Вместо неё — градиентная маска на самом списке (`SidebarListFade`): чернила заканчиваются, стекло продолжается. 50 pt растушёвки, ниже — пусто; остальные 116 pt это сам блок и его поле, они измеряются, а не задаются числом.

**Модель саммари качается с нажатия на «Начать»**, молча и ничего не блокируя. Проверка на каждом запуске осталась (`design/no-dead-ends.md` §5) — ушёл экран, который её объявлял.

**Обновление с 1.14 блок не увидит.** Оба вопроса там уже задавали, и признак этого — сохранённое имя: старый онбординг всегда его писал. `Preferences.userName` теперь означает «человек ответил», а подстановка имени учётной записи живёт в `ownerName`, у места использования — иначе заглушка ответила бы за пользователя и блок не появился бы никогда.

**Светофор на плашке настоящий.** Был спрятан, и экран оставался единственной поверхностью без выхода, кроме ⌘Q. Закрыть — законный ответ: приложение живёт в меню-баре, настройка остаётся должной и вернётся при следующем запуске с уже проставленными галочками. В титуле — винт, против часовой, оборот за 12 с: это не спиннер, ждать здесь нечего.

**Удалено:** `OnboardingFlowView`, `OnboardingWelcomeView`, `OnboardingNameView`, `OnboardingCalendarView`, `OnboardingPermissionsView`, `OnboardingSummaryModelView`, `OnboardingEndView`, `OnboardingComponents` (вместе с `OnboardingCard`, `PillButton`, `OnboardingPager`, `CalendarProvider`), `Tokens.Card`, `OnboardText`. Кадров в галерее: 41 → 29.

**Тесты: +13** (`SetupPromptTests`).

---

## Часть 0в — 2026-07-30: догон, которого не видно

Цель: человек не должен знать, что в приложении есть бэкфилл. Полный разбор с обоснованиями — [`meeting-recorder/docs/REFACTOR-PIPELINE-STATE.md` §10](meeting-recorder/docs/REFACTOR-PIPELINE-STATE.md); ниже — что изменилось по существу.

**Отказ несёт свой план восстановления.** ⚠️ Этот абзац описывает состояние на 2026-07-30 и **перекрыт 2026-08-04** (см. компонент 4: `permanent` больше нет, попытки не кончаются, `needsAttention` → `isTerminal`, интерфейс читает `MeetingRest`). Оставлен как история решения. Было: любая ошибка парковала встречу до кнопки «Повторить» — то есть `ollama serve`, опоздавший на полсекунды, стоил встрече саммари, молча. Стало: `PipelineFailure` знает `kind` (`transient`/`permanent`), `attempt` и `nextAttemptAt`. Переживаемый отказ повторяется сам по лестнице 20 с → 1 мин → 5 мин → 20 мин, у ASR попыток меньше (его повтор стоит минут CPU). Неустранимый (нет аудио, пустой транскрипт, нет места) парковка на первой же попытке. **UI смотрит на `needsAttention`, а не на `lastFailure != nil`** — встреча, которая ждёт следующей попытки, выглядит как обычная встреча.

**Главная дырка была не в повторах, а в пробуждении.** Цикл останавливался четырьмя способами, и для трёх не существовало события, которое вернуло бы его к работе: перегрев, пауза на звонке, отсутствие провайдера. Теперь `PipelineOutlook` отвечает *что делать* **и** *когда посмотреть снова*, `PipelineDrain.plan` превращает любую остановку в дедлайн, а `AppState` слушает термальное состояние, пробуждение машины и активацию приложения. Инвариант **I12** («есть незакрытая работа ⇒ есть дедлайн») проверяется свойством на случайных архивах.

**Нагрузка.** Три находки: `ollamaUsable` поднимал сервер (и распаковывал рантайм), чтобы узнать, что модели нет — теперь сначала дешёвый вопрос про веса на диске; бэклог саммари убивал свой же сервер отложенным `stopAfterIdle` посреди генерации; архив 1.11 (саммари есть, тем нет) **перегенерировал саммари целиком** — теперь `SummaryWork` отличает «нужен рекап» от «нужны только темы» (один короткий промпт). Пустой архив не держит ни таймеров, ни сайдкаров.

**Молчание.** Уведомления и вывод окна на себя — только для встреч, записанных в этой сессии (`isAwaited`): двадцать встреч в бэклоге давали двадцать уведомлений и двадцать краж фокуса. Проверка диска в воркере больше не интерактивна — ожидание модалки останавливало воркер, и это единственный стопор, из которого не выводит никакой таймер.

**Прочее по пути:** отмена (звонок начался посреди ASR) больше не пишется как ошибка; ручное «Повторить»/«Завершить» идёт через очередь и **обгоняет** бэклог, а не теряется в re-entrancy-гарде; загрузка модели саммари, на которую человек уже согласился, возобновляется при блокировке пайплайна, а не только один раз через 20 с после старта; удалённый вручную транскрипт перезаписывается из индекса вместо отказа; `restartPipeline` (мёртвый) удалён.

**Тесты: 138 → 178.** Новые: `PipelineRetryTests` (классификация и лестница), `PipelineCatchUpTests` (сценарии: модель приехала позже, сеть упала, очередь из нескольких записей, звонок посреди догона, сон машины), `PipelineNeverStrandsWorkTests` (I12 как свойство).

---

## Часть 0б — Вечер 2026-07-27 (модель, загрузки, словарь)

Найдено прогоном «с нуля» на чистой машине: `serve` качал **885 МБ fp32-энкодера, который никогда не читает** — `download --prequantized` кладёт INT8-комплект и рапортует ready, а `serve` проверяет наличие по fp32-имени и лезет в сеть. На флаки-связи это и был «висит на 85 %».

| Что сделано | Где |
|---|---|
| **`qwen3.5:4b` вместо `qwen2.5:7b`** (3,4 ГБ против 4,7). Миграция префа с `qwen2.5:7b` / `llama3.2`; старая модель удаляется после переезда | `Preferences.defaultRecapModel`, `OllamaSidecar.reclaimSupersededModels` |
| **`num_ctx` по размеру промпта** (бакеты 16k/32k) + `temperature: 0.2`. Замер: конспект писался по 2050 токенам из 5538 | `PropellerPure.OllamaContext`, `RecapService.callOllama` |
| **`think: false`** — reasoning-модель уводила весь ответ в поле `thinking` и возвращала пустой `content` (замер: 484 с, 12794 токена, пусто). Ретрай без флага на 400 | `RecapService.callOllama`, `sendChat` |
| **GigaAM в бандле** (~247 МБ INT8), копируется в Application Support при первом старте; ASR работает офлайн, качать нечего | `build.sh`, `GigasttSidecar.seedModelsFromBundle` |
| **Маркер fp32 + `--offline` у serve** — обход рассогласования gigastt 2.14; любая загрузка теперь только в нашем шаге с прогрессом | `GigasttSidecar.ensureEncoderPresenceMarker` |
| **Встроенный словарь хотвордов** (319 терминов из `vocab/`), пользовательские добавляются сверху с дедупом | `PropellerPure.BuiltinHotwords`, `GigasttSidecar.writeHotwordsFile` |
| **Ретрай загрузки модели** 5с→10мин, только на транспортных ошибках; согласие персистится и докачка возобновляется после перезапуска | `OllamaSidecar.pullModelWithRetry`, `PropellerPure.OllamaRetry`, `Preferences.localRecapModelRequested` |
| **Метаданные отвязаны от заголовка** — в 1.11 темы/теги не генерились для встреч с календарным именем; плюс бэкфилл по `topics == nil` | `AppState.generateMeetingMetadata`, `backfillMissingMetadata` |
| Гейт диска разделён: 2 ГБ под ASR, 5 ГБ под модель саммари (был один на 4 ГБ, врал в обе стороны) | `AppState.ModelDownload` |
| Пустое саммари предлагает **«Скачать»**, а не «Сгенерировать», когда модели нет; «Настройки» убраны, когда всё уже работает | `RecordingDetailView`, `AppState.needsLocalRecapModel` |
| Тост: равный отступ 16 от обоих краёв (было 8/12); экран модели в онбординге не держит на прогресс-баре | `Tokens.Toast.cornerInset`, `OnboardingSummaryModelView` |

---

## Часть 0 — Changelog надёжности (2026-07-24 … 2026-07-27)

Инцидент: Zoom-встреча `20260724_182534` (~43 мин) — аудио сохранилось (mic ~79 МБ), транскрипта не было: gigastt default body-limit ~50 МБ → HTTP **413**. System stem пустой (~4 КБ header-only), UI при этом рисовал «живой» System-чип.

| Что сделано | Где |
|---|---|
| **SCK-primary** для системного звука; Process Tap оставлен в дереве, **не** на hot path | `AudioRecorder`, `SystemAudioCapture`, `ProcessTapAudioCapture` (dormant) |
| Баннер mid-recording только если захват **не стартовал**; mic-only судят на **stop** по реальному стему (`lastStopWasMicOnly`); health = level meter | `AudioRecorder`, `RecordingInProgressView` |
| Клиентский чанкинг ASR (~20 мин / под body+duration caps) + sidecar `--body-limit-bytes 67108864`, `--inference-timeout-secs 0` | `PropellerPure.GigasttChunking`, `GigasttClient`, `GigasttSidecar` |
| Спиннер строки = `status == .recording` **или** `activity.concerns(entry.id)` (не idle-стадии) | `MainView`, `MenuBarPanelView` |
| Заголовок: не латчить `titleManuallySet` на авто-«Recording …»; при старте — `CalendarService.suggestedRecordingTitle`; бэкфилл заголовков из уже существующих рекапов | `AppState`, `RecordingStore`, `RecordingInProgressView`, `CalendarService` |
| Онбординг: Mic + **Screen Recording** обязательны для Next; Notes / Launch at login опциональны; убран нерабочий Update-pill | `Onboarding*` |
| Glass tint `#0A0A0A` @ 80% поверх `.thickMaterial` (main + onboarding) | `PropellerUI/Tokens.Glass`, `Controls.GlassBackground` |
| Промпт конспекта: Итог → Решения → Задачи → Открытые вопросы → Ход обсуждения → Прочее; `languageLock` append; миграция старого дефолта из UserDefaults | `RecapService.defaultPrompt`, `Preferences.recapPrompt` |

---

## Часть I — Канонические решения (конфликты доков разрешены)

| Вопрос | **Канон** | Что противоречит | Действие |
|---|---|---|---|
| Захват системного звука | **ScreenCaptureKit — единственный путь.** Область — `CaptureScopePolicy`: сужаемся на приложение той платформы, чей звонок идёт; нет звонка (или он в браузере) — пишем машину целиком. Отката по тишине нет. **Process Tap удалён 2026-07-29** (см. ниже) | Старые пометки plan-v2 1.2 / E4 «Process Tap first» | Канон здесь; планы помечены откатом |
| Длинные встречи / ASR | Клиентский чанкинг + body-limit 64 MiB на сайдкаре | Старый «один POST на весь файл» → 413 | — |
| Авто-обновления | **Sparkle в R1** (GitHub Releases + EdDSA). Проверка **раз в час** (`SUScheduledCheckInterval=3600`, 2026-07-29) — дефолт Sparkle в сутки слишком долог для сборки с дефектом без обходного пути | plan-v2 5.3 | Developer ID позже; первый запуск всё ещё ПКМ→Открыть |
| Модель рекапа по умолчанию | **`qwen3.5:4b`** (решено и сделано 2026-07-27, `Preferences.defaultRecapModel`). Миграция с `qwen2.5:7b` и `llama3.2` — на чтении префа. Обязателен `think: false` | plan-v2 4.5 фиксировал 7B ⚠️ | Канон здесь |
| Бандл Ollama | **После** живых тестов и Developer ID | plan-v2 4.5a план готов | Порядок зафиксирован |
| TEST-онбординг (`showOnboarding = true`) | **Снят** — снова `!onboardingCompleted` | — | — |
| Потолок записи | **8 часов** → авто-стоп + обычный пайплайн | — | ✅ сделано (G5, 2026-07-25) |
| Подпись | Ad-hoc + «ПКМ→Открыть» для R1; Developer ID — после недели фидбека | — | TCC будет слетать между билдами, это известно и принято |
| **Язык интерфейса** | **Русский целиком** (решено 2026-07-25) | Ревью `ru-en` фиксировало раскол EN UI / RU уведомления ⚠️ | Онбординг переведён. Остальное — список поверхностей ниже |
| **Google Календарь** | Через macOS: аккаунт добавляется в Системных настройках → «Учётные записи», Propeller читает EventKit. **OAuth в приложении нет и не будет** | Онбординг-чип обещал подключение аккаунта ⚠️ (исправлено) | — |
| Порядок работ | **Живые тесты → затем фиксы** | — | Решение владельца 2026-07-25 |

**Русификация — что переведено и что осталось:** ✅ онбординг целиком (5 шагов). ❌ осталось: `MainView` (заголовок «Meetings», секции Today/Yesterday/This Week/Older, алерты), `RecordingDetailView` (табы, кнопки, статус-пилюли), `SettingsSheet` (все вкладки), `MenuBarPopover`, `SearchPalette`, `RecapService` (промпт уже RU), системные уведомления (уже RU).

---

## Часть II — Компоненты

### 1. Захват аудио

**Файлы:** `AudioRecorder.swift` · `ProcessTapCapture.swift` · `CoreAudioObjects.swift` · `SystemAudioCapture.swift` · `AudioPlayer.swift` · `TapProbe.swift` · `PropellerPure/{CaptureScope,CaptureChannelMap,CaptureCursor,CapturePath,CaptureBuffers,TapTarget,StemTimeline}.swift`

**Здорово:** ✅ **захват на общих часах** (`ProcessTapCapture`): одно агрегатное устройство, одна IOProc, микрофон и системный звук кадр в кадр — сдвига нет по построению, когерентность в речевой полосе **0.91–0.97** против 0.04 у прежнего пути; на захвате **0.6–2.3 % CPU и 94 МБ RSS** (замерено на живой записи 2026-07-29), сети не трогает вовсе; разрешение на захват звука спрашивать нечем — его первое получение стоит **60 секунд ровно** внутри Core Audio, поэтому оно оплачивается прогревом при запуске (`warmUpIfNeeded`) и запоминается в настройках, а не отъедает начало встречи; путь захвата теперь **один** — SCK удалён вместе с подъёмом минимума до **macOS 14.4**, лестница заканчивается честным mic-only (`CapturePathPolicy`); **разрешение «Запись экрана» больше не запрашивается вовсе** — ни в онбординге, ни в рантайме, ключа нет в бандле; mic-стем пишется непрерывно; при отвале — деградация в mic-only (`lastStopWasMicOnly` на stop), не падение; UI не врёт «System live» по speculative silence-watchdog (баннер только если capture не стартовал); watchdog целостности микрофона; `stop()` дожидается записи WAV; straggler-буфер не обрезает стем (C10 ✅).

**Сломано:**

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| M1 / mix-ram | Офлайн-микс грузит **оба стема целиком** в RAM (`readAndResample` читает `file.length` одним буфером) → ~ГБ пик на часовой встрече | 🟠 | ✅ |
| M4 | **Hard-clip** суммы mic+sys, хотя комментарий обещает soft clamp (`if v > 1.0 { v = 1.0 }`) — клиппинг ровно там, где оба говорят | 🟠 | ✅ |
| ~~M2~~ | **Исправлено 2026-07-29:** сдвиг снимается на захвате (`SystemAudioCapture.onFirstSample` → позиция часов микрофона), хранится в записи (`RecordingEntry.systemStemOffset`) и применяется в миксе через `StemTimeline`; пересборка микса после падения берёт его же. Меряем по таймстемпам, а не корреляцией: в наушниках коррелировать не с чем, а лайв-транскрайбу нужен сдвиг во время встречи, а не после. Ниже — замер, из-за которого чинили. **Было (замерено 2026-07-29):** реальный сдвиг ≈ **+484 мс**, стабильный (±3 мс за 43 минуты, тренд +1 мс/час). Без наушников микрофон ловит дальнюю сторону (корреляция огибающих 0.82, +27 dB над своим тихим полом), и микс кладёт две копии одной речи в полусекунде друг от друга. Цена: до gigastt доходит **52.9 %** слов дальней стороны против 95 % при выровненных стемах; уверенность 0.826 против 0.881; сегментов 112 против 96 — замер в [ECHO_AND_MIX_EXPERIMENTS.md](meeting-recorder/docs/ECHO_AND_MIX_EXPERIMENTS.md) | 🔴 | ✅ |
| ~~M6~~ | **Исправлено 2026-07-29** захватом на общих часах: обе дорожки снимаются одной IOProc одного агрегатного устройства, кадры кладутся по `mSampleTime` (`CaptureCursor`), а не дописываются в конец. Когерентность в речевой полосе **0.91–0.97** на четырёх прогонах (среднее по 200–3500 Гц), задержка mic↔sys +30…34 мс — это дорога звука от динамика до микрофона, а не часы. Порог приёмки был 0.7. Единственный прогон ниже порога (0.67) — тот, где системный стем упёрся в единицу: динамик гнали в клиппинг, и метрика ровно это и ловит (её же документация называет искажения динамика первой причиной). При некритичной громкости показатель воспроизводится. Ниже — замер, из-за которого чинили. **Было:** **Стемы не когерентны по сэмплам** (замерено 2026-07-29): когерентность выровненного sys с mic в речевой полосе **0.04** — линейной связи нет, хотя речь в микрофоне слышна и распознаётся. Контрольная синтетика с дрожанием ±3 мс даёт ровно ту же картину (0.70 → 0.01), то есть между дорожками болтается несколько миллисекунд: буферы SCK кладутся подряд, а не по своим presentation timestamp. **Следствие: эхоподавление на нынешних стемах невозможно** — свой канцеллер даёт +22 dB на синтетике и −1 dB на записи. Чинить таймлайн раньше, чем браться за AEC | 🔴 | ✅ |
| M3 | Смена/пропажа устройства → тихая потеря sys-звука. **Слушатель добавлен 2026-07-29** (`ProcessTapCapture`: вход, выход и исчезновение устройства из состава → одна пересборка тапа и агрегата, дыра вписывается тишиной, серия уведомлений склеивается окном 1.5 с). **Но на живой записи не проверено:** на машине разработки один настоящий вход и один выход, а виртуальная карта основной не становится — `AudioObjectSetPropertyData` возвращает `noErr` и оставляет прежнее устройство. Первый прогон на этом и дал ложный вывод «пересборка не нужна»; проба теперь читает устройство обратно и объявляет замер недействительным (`TapProbe`, ступени 5–6). Проверить с настоящими наушниками | 🟠 | ⚠️ |
| ~~M5~~ | **Снято 2026-07-28 вместе с VPIO.** Опция «Подавлять эхо динамика» удалена с кодом: Apple voice processing перенастраивает **общее** входное устройство, и у тестировщика он стал почти неслышен собеседникам. Записывающий инструмент не должен портить саму встречу | — | ✅ |
| ~~G5~~ | **Исправлено 2026-07-25:** потолок 8 ч в `startDisplayTimer` → обычный `stopRecording()`, запись сохраняется и уходит в пайплайн. _Периодическая проверка диска во время записи — осталась_ | 🔵 | ✅ |
| ~~—~~ | **`ProcessTapAudioCapture.swift` удалён 2026-07-29.** 465 строк, ноль ссылок. Держали «на будущее» как запасной механизм захвата, но живой звонок показал, что SCK по приложению отдаёт звук звонка вместе с хелперами (`CptHost`) — сценарий, ради которого его берегли, закрыт основным путём. Сам код был признан нерабочим ещё в 2026-07 (пустые стемы) и писался до нынешнего API процесс-тапов, так что ценности как заготовки не имел. Вернуть — `git log -- Sources/ProcessTapAudioCapture.swift` | — | ✅ |

**Здоров, когда:** микс стримится чанками (не ГБ в RAM), стемы выровнены по host-time, сумма не клиппится, смена наушников не убивает дорожку, запись сама останавливается на 8 часах.

---

### 2. ASR-сайдкар

**Файлы:** `GigasttSidecar.swift` · `GigasttClient.swift` · `PropellerPure.GigasttChunking`

**Здорово:** ✅ **модели GigaAM едут в бандле** (~247 МБ INT8, копия в Application Support при первом старте — ASR работает офлайн, качать нечего); `serve` c `--offline`, так что любая загрузка идёт только через наш шаг с прогрессом; ленивый спаун + idle-stop (E1); backoff с cap 5 (S3); проверка владения портом + PID-файл (S4/C8); атомарный `ensureReady` (C9); аплоад стримом с диска (S1); health не принимает `model=loading`; **клиентский чанкинг** длинных WAV (обход 413 / duration cap) + sidecar `--body-limit-bytes 67108864`, `--inference-timeout-secs 0` (2026-07-24).

**Сломано:**

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| R1 | `stop()` **не ждёт** реального выхода: `terminate()` + SIGKILL через 2 с асинхронно, возврат сразу → `restart()` может обогнать освобождение порта и словить ложный `portOccupied` | 🟠 | ✅ |
| R4 | Нет **таймаута** на загрузку модели (stderr-дренаж починен, зависшая загрузка висит вечно) | 🟠 | ✅ |
| R3 | ASR HTTP timeout зашит 600 с — не масштабируется на длинные встречи (чанкинг снижает риск, но per-request timeout всё ещё фиксирован) | 🟠 | ✅ |
| ~~build-soft-gigastt~~ | **Неверно, сверено 2026-07-27:** `build.sh` делает `exit 1` без `gigastt`, без Sparkle и без моделей GigaAM | — | ✅ |

**Здоров, когда:** `stop()` синхронно дожидается смерти процесса, у загрузки модели есть таймаут, ASR-таймаут пропорционален длительности аудио, сборка падает без `gigastt`.

---

### 3. Транскрипция и диаризация

**Файлы:** `TranscriptionService.swift` · `SpeakerMatchingCore/` · `PropellerPure/`

**Здорово:** ✅ двухфазный чекпоинт (`transcribed_raw`) — дорогой ASR не теряется; освобождение тяжёлых ресурсов после батча (E1/E2); кэш `gigasttReady` убран (R5); владелец-по-микрофону работает детерминированно.

**Сломано:**

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| P8 | `resolveOwnerName` перечитывает mic/sys стемы **N раз** (цикл по спикерам), буферы по 4096 кадров | 🟠 | ✅ |
| R5-хвост | Класс без изоляции (`class TranscriptionService`, не actor/@MainActor) | 🔵 | ✅ |
| ollama-ram | `releaseHeavyResources()` в `defer` у `runTranscribe` срабатывает **после** `runSave → runRecap` → во время работы LLM модель GigaAM всё ещё в RAM. На 8 ГБ-маке критично | 🟠 | ✅ |
| H2 | Мёртвые `mergeConsecutiveSameSpeaker` / `formatTranscript(_:speakerNames:)` | 🔵 | ✅ |

**Здоров, когда:** один проход по стему на все окна, ASR-ресурсы освобождаются до старта LLM, мёртвые методы удалены.

---

### 4. Координация пайплайна (`AppState`) — **самый больной компонент**

**Файлы:** `AppState.swift` (~1100 строк)

**Состояния «не удалось обработать» больше нет — сделано 2026-08-04.** Концепция и разбор: [`design/no-dead-ends.md`](design/no-dead-ends.md). Было двенадцать способов стать красным, причин четыре: пять из двенадцати — наши баги (413 = чанкер посчитал неверно, I11-стол, потерянный чекпоинт); вторую группу красной делал **не сбой, а счётчик попыток**; третьей нужна деградация; четвёртая — вовсе не отказ, а результат. Ось сменилась с «получилось / не получилось» на **глубину** (записана → расшифрована → спикеры разделены → саммари → темы), где каждая ступень — законный терминал и ступень не заложник следующей. Что сделано по этапам: **Э1** диаризатор, который не загрузился, больше не отнимает готовый ASR-проход — спикеры по дорожкам («Левон / Собеседник»), `SpeakerAttribution.stems`, признание в карточке; наружу бросала именно `prepareDiarizer` (модели FluidAudio качаются из сети), само падение кластеризации переживалось и раньше. **Э2** `FailureKind` = `transient / ourFault / terminal`, лестница до часа и **без конца** (`maxAttempts` удалён), `classify` никогда не объявляет терминал (это делает только тот, кто посмотрел на вход), `report()` пишет в лог и телеметрию и больше никуда, потерянный чекпоинт **починяет стадию** вместо отказа, красные встречи старых архивов распарковываются при чтении индекса — это вся миграция. **Э3** `ensureSummaryModel()` на каждом запуске и на каждой остановке из-за провайдера, решение чистое (`ModelProvisioning`), согласия как гейта нет, онбординг-шаг из вопроса стал состоянием, CTA «Скачать» ушёл. **Э4** 413 → делить кусок пополам до пола 30 с; снята устаревшая проверка места под ASR-модель (веса в бандле). **Э5** `MeetingRest` (`waiting`/`done`, третьей ветви нет), причину терминала несёт сам отказ, **I13** («ни один отказ не требует человека») проверяется свойством — включая то, что ни одно состояние покоя не содержит слов «Повторить», «Не удалось», «Ошибка». Из интерфейса ушли ⚠ (стал пустым кружком — состояние, не тревога), «Не удалось обработать» в строке и «Повтор» в карточке. Тесты 309 → **329**. **Живьём не проверено:** деление на 413 требует файла, который его вызывает, деградация спикеров — первого запуска без сети.

**Здорово:** ✅ пайплайн работает по `recordingID`, а не по текущему выбору (C1); одна очередь, выводимая из стадий, переживает краш без отдельного механизма; `stopRecordingAndWait` идемпотентен; «догон» несделанных саммари — часть той же очереди, а не отдельная подсистема; незакрытая работа всегда имеет дедлайн, когда на неё посмотрят снова (I12), а переживаемые отказы повторяются молча (Часть 0в).

**Сломано:**

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| ~~G1~~ | **Закрыто:** `reconcilePendingPipeline` продвигает следующую запись после каждой завершённой цепочки — очередь есть. Сверено 2026-07-27 | — | ✅ |
| ~~G2~~ | **Закрыто:** реконсилятор при старте и после цепочки поднимает `recorded` / `transcribed_raw`. Проверено живьём 2026-07-27 — застрявшая запись догналась сама | — | ✅ |
| ~~G4~~ | **Закрыто 2026-07-30:** метаданные ретраятся по той же лестнице, что и всё остальное, и по короткому пути (`SummaryWork.metadataOnly`) — без перегенерации саммари. Кончились попытки — тем просто нет, встреча всё равно готова | — | ✅ |
| rev-2 | Старт записи B во время ASR A **сбрасывает** глобальные pipeline-steps → UI встречи A врёт | 🟠 | 📋 |
| rev-4 | `selectRecording` mid-pipeline перетирает `.running` статусами выбранной записи | 🟠 | 📋 |
| rev-8 | Гонка Stop vs Discard vs Zoom-end — три терминальных действия без мьютекса | 🟠 | 📋 |
| rev-5/30 | Рекап A завершается во время встречи B → **кража фокуса** окна (`surfaceSummaryUI`). _Закрыто для догона 2026-07-30:_ фокус и уведомления только для встреч этой сессии (`isAwaited`); во время записи `surfaceSummaryUI` и так молчит | 🔵 | ✅ |
| G3 / R7-хвост | «Не записывать» **отменяется флапом детектора**: `ignoredZoomMeeting = false` и на started, и на ended | 🟠 | ✅ |
| rev-13 | Ручная запись + Zoom mid-session → hangup Zoom остановит ручную запись | 🔵 | ✅ |

**Фикс G1+G2 одним механизмом — реконсилятор пайплайна.** По образцу бэкфилла рекапов: при старте и после каждой завершённой цепочки сканировать записи не в терминальном статусе и продвигать на шаг вперёд, последовательно, с теми же гардами. Очередь для G1 — бесплатный побочный эффект. Туда же G4 (условие «есть рекап, нет тегов»).

**Здоров, когда:** встречи подряд обе доходят до саммари без участия человека; после любого краша/выхода приложение само доводит начатое; отказ от записи липкий; UI одной встречи не врёт из-за другой.

---

### 5. Хранилище и экспорт

**Файлы:** `RecordingStore.swift` · `Models.swift` · `MarkdownWriter.swift` · `Preferences.swift` · `KeychainHelper.swift`

**Здорово:** ✅ битый индекс карантинится, не затирается (C5); поэлементное восстановление + `.bak`; финальный микс пересобирается из стемов после жёсткого выхода (C3); `-recap.md` больше не удаляется при пересохранении (C7); `prettyPrinted` убран (P6); ключи в Keychain.

**Сломано:**

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| P6-хвост | `save()` пишет весь архив (с транскриптами) **синхронно на MainActor** | 🟠 | ✅ |
| rev-9 | `Delete meeting` удаляет аудио + запись индекса, но **не** markdown/recap на диске → сироты | 🟠 | ✅ |
| rev-29 | Переименование встречи не переименовывает файлы на диске (Finder/Obsidian видят старые имена) | 🔵 | 📋 |
| retention-dead | Time-based Retention UI снят; size-nudge 5 GB (GROW-11) | 🟢 | ✅ |
| rev-22 | Короткая запись <5 с исчезает из списка, но живёт в поиске | 🔵 | 📋 |

**Здоров, когда:** запись индекса не блокирует UI, удаление встречи не оставляет сирот, в Settings нет контролов, которые ничего не делают.

---

### 6. Саммари (рекап)

**Файлы:** `RecapService.swift` · `Preferences.recapPrompt`

**Здорово:** ✅ `keep_alive: 90` (не 10 с — дрейф дока поправлен 2026-07-27) + `OllamaSidecar.stopAfterIdle` выгружают модель между встречами; окно контекста считается от промпта, транскрипт не режется; ephemeral URLSession; бэкфилл догоняет пропущенные саммари; заметки вплетаются в конспект (+ сырой блок «Заметки» дописывает система); редактируемое саммари; модель по умолчанию `qwen2.5:7b`; **`languageLock` всегда append** к промпту; дефолт-промпт (2026-07-27) — конспект договорённостей: `## Итог` / `Решения` / `Задачи` / `Открытые вопросы` / `Ход обсуждения` (с таймкодами) / `Прочее`; пустые секции опускаются; миграция старого вшитого дефолта из UserDefaults.

**Сломано:**

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| ~~recap-silent~~ | **Закрыто:** `recapSkipHint` / `pipelineError` в панели + тост; при отсутствии модели панель предлагает «Скачать». Сверено 2026-07-27 | — | ✅ |
| ~~R2~~ | **Исправлено 2026-07-27.** Было: без `options.num_ctx` Ollama держит окно ~4k и **сдвигает** его (`--context-shift`), выбрасывая начало встречи. Замер на `20260724_182534` (42 мин): промпт 5538 токенов, модель видела **2050** — конспект писался по последней трети. Теперь `num_ctx` считается от размера промпта (`PropellerPure.OllamaContext`, бакеты 16k/32k) + `temperature: 0.2`. Проверено после фикса: `prompt_eval_count` 6455 из 6455, «Ход обсуждения» снова начинается с `[00:28]` | — | ✅ |
| meta-1.11 | ~~`generateMeetingMetadata` выходил по `guard needTitle`, поэтому встречи с календарным/ручным заголовком **никогда** не получали темы и теги~~ **Исправлено 2026-07-27:** пасс отвязан от заголовка + бэкфилл метаданных (`topics == nil` = не запускался) | — | ✅ |
| cloud-default | Провайдер Auto уходит в облако, если Ollama недоступна **и** есть ключ. Сверено 2026-07-27: без ключа облака не будет, то есть у коллег не выстрелит — но у владельца ключа транскрипт уедет без спроса | 🟠 | ✅ |
| rev-21 | Auto: Ollama думает >5 с → уход на платный API, если есть ключ | 🟠 | 📋 |
| rev-25 | Двойной regenerate / бэкфилл против ручной правки саммари — last-write-wins | 🔵 | 📋 |

**Здоров, когда:** отсутствие провайдера — явное состояние с инструкцией, а не тишина; облако — только с осознанного согласия; длинный транскрипт не режется молча.

---

### 7. Заметки — потеря данных закрыта (rev-6, 2026-07-25)

**Файлы:** `NoteOverlayController.swift` · `RecordingInProgressView.swift`

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| ~~rev-6~~ | ~~Оверлей-заметки (⌃⌥N) затираются~~ **Исправлено 2026-07-25:** нотпад слушает `selectedRecording?.notes` и принимает внешний дописанный хвост (только чистый append, чтобы не перебить набор); `flushNotes` не пишет поверх более свежего стора | — | ✅ |
| rev-10 | ⌘K → открыть другую встречу во время записи: нотпад пишет заметки не в тот entry (оверлей корректен по `recordingID`, UI — нет) | 🟠 | 📋 |
| rev-37 | Hard kill в окне дебаунса 0.6 с — потеря последних нажатий | 🔵 | ✅ |

> **Поправка:** в разборе сценариев 2026-07-25 я утверждал, что заметки безопасны. Это неверно — ре-синка `liveNotes` нет, и релиз-ревью право. Единственный сценарий потери данных, подтверждённый в коде.

**Здоров, когда:** оверлей и нотпад — один источник правды (нотпад слушает стор), флаш сливает, а не перезаписывает.

---

### 8. Zoom-детект и уведомления

**Файлы:** `ZoomMeetingDetector.swift` · `NotificationManager.swift` · `PropellerPure/PushPolicy.swift` · `PropellerPure/ToastQueue.swift`

**Здорово:** ✅ таймер только пока Zoom запущен + tolerance 30 % (E3); ранний выход без дорогих сканов; русские idle-заголовки и убран фолбэк «любой длинный тайтл» (C6); интерактивное «Не записывать».

**Правила уведомлений — `design/notifications.md`, реестр в коде (2026-08-04).** Раньше «слать или нет» решали девять точек вызова по отдельности, и получалось: «Заметка сохранена» — баннер со звуком **посреди записи** (свой бип в свой же микрофон), «Запись остановлена» — сообщение человеку о его собственном нажатии, «Сохранено: файл.md» и «Саммари готово» — пара уведомлений на одну встречу, а готовое саммари **выдёргивало окно** на себя поверх чужой работы. Теперь один канал: `PushPolicy.surface(for:in:)` — чистая функция от повода и контекста (идёт ли запись, видно ли окно, ждут ли эту встречу, есть ли разрешение), тост — всегда (`ToastQueue`), баннер — только когда окна перед человеком нет. Звук — только у аларма, и единственный аларм со звуком во время записи это сам её старт. Штатный бюджет: **два** уведомления на встречу (старт авто-записи + «готово»). Чего ещё нет: дедупа и склейки *между* пушами (две встречи, дошедшие до «готово» подряд, дадут два баннера) — Ф2 плана.

**Шесть решений 2026-08-05 — после разбора кадров галереи.** (1) **«Аудио удалено» как состояние удалено.** Удалил человек — встречи в списке нет; удалили мы ради места — транскрипт и саммари живут как обычно, без пометок. Отсутствие аудио вообще перестало быть отказом: планировщик и так не ставит такие встречи в очередь, а запись без аудио **и** без текста не показывается в списке (`RecordingEntry.hasSomethingToShow`, из индекса не удаляется — том мог просто отмонтироваться). (2) **`saving` больше не работа в расписании:** markdown пишется в том же проходе, что и диаризация — «транскрипт есть, а на диске нет» это уже нарушенное обещание. Попытка сложить сохранение внутрь фазы саммари вскрыла хуже: с выключенным саммари файл не записался бы никогда. Состояние «идёт сохранение» из каталога ушло, стадия `.transcribed` получила свой кадр. (3) **Ждущая встреча говорит «В очереди» и шиммерит** — `SidebarRowActivity.queued`, выведенное из `MeetingRest.owesWork`; прежнее решение `queuedIsQuiet` («очередь выглядит как ничего») делало ждущую встречу неотличимой от готовой. (4) **«Всё готово» без саммари невозможно** — и в приложении никогда не было (`reconcileSummarizedStage` понижает стадию, I9); врала фикстура галереи, теперь файл саммари пишется каждой позируемой встрече, которая объявляет себя готовой. (5) **Upcoming вырезан целиком:** блок «Скоро», муты «не записывать эту встречу», `lib-upcoming`; чтение календаря осталось — оно даёт название записи и `CalendarMeta`, а тип назван по делу (`CalendarEvent`). Заодно удалён весь старый экран списка, мёртвый после редизайна (~340 строк `MainView`). (6) **Письмо удалено как фича:** вкладка, панель, чтение и запись `-followup.md`, кадр `tab-letter`. Плюс два побочных: тест-скретч `TempRailShotTests` падал на `NSApp` (nil в тест-процессе) и уносил с собой семнадцать тестов после себя — теперь пропускается без `RAIL_SHOT`; экспортёр галереи не задавал appearance окна и на светлой теме Mac выдавал 41 почти пустой кадр — теперь всегда `darkAqua`.

**Тоста в приложении нет (решение 2026-08-04).** Их было две системы (карточка 280 pt в панели на четырёх `@Published`-флагах + полоса в рельсе), они свелись в одну очередь, а очередь была удалена вместе с обоими компонентами: из шести поводов пять не давали ни действия, ни рекомендации, а «N записей прервано» объявляло человеку, что *его* записи прервались, — о нашем же падении. Обоснованные два переехали на объекты, к которым относятся: **«Вернуть»** — вид строки самой встречи (`SidebarRowActivity.deletedUndoable`: удалённая встреча остаётся в списке на своём месте, гаснет и держит «Вернуть»; заодно переживает свёрнутый рельс), **«Нет доступа к микрофону»** — свойство записи (строка «Начать запись» → «Нет доступа к микрофону», клик ведёт в разрешения; `AppState.micAccessDenied`). Удалены без замены: предупреждение о диске (было гейтом на continuation, из-за которого детектированная встреча молча не начиналась), нудж хранилища (в настройках то же число и список самых больших встреч), тост ошибки обработки (дублировал ⚠ на строке и карточку), модалка «Запись восстановлена». Правило, которое из этого вышло: **состояние принадлежит объекту, а не приложению** — тот же инвариант, что у пайплайна, доведённый до интерфейса.

**Сломано:**

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| G3 | Отказ «Не записывать» снимается флапом детектора (см. компонент 4) | 🟠 | ✅ |
| zoom-notify | Notifications denied → авто-запись стартует без баннера. _Смягчено:_ отказаться можно в окне (`Discard`, rev-12 ✅) и в меню-баре, но человек может не заметить, что запись пошла | 🟠 | ✅ |
| ~~mic-disk-unseen~~ | **Закрыто 2026-08-04:** отказ микрофона и мало места были тостами в окне, которого во время авто-записи никто не видит (человек в звонке) — встреча не записывалась молча. Диск вообще держал старт на continuation без таймаута. Теперь уровень 3 эскалируется в баннер, а диск предупреждает и **не блокирует** | 🔴 | ✅ |
| rev-27 | Screen Recording отозван mid-call → правда всплывает в основном после Stop | 🟠 | 📋 |
| R6 | Нет обработки сна Mac / смены input-устройства во время записи | 🟠 | ✅ |

**Здоров, когда:** отказ переживает флап; без разрешения на уведомления авто-запись либо не стартует, либо даёт отказ внутри приложения.

---

### 9. Календарь

**Файлы:** `CalendarService.swift`

**Здорово:** ✅ Upcoming через EventKit; при `beginRecording` заголовок берётся из `suggestedRecordingTitle` (перекрывающееся / скорое событие), иначе placeholder «Recording …»; LLM потом переименовывает только авто-placeholder (не календарный и не ручной); **с 2026-07-31 от того же события снимается вся мета** (`CalendarMeta` в `PropellerPure`): id события и серии (`calendarItemExternalIdentifier`), приглашённые, организатор, календарь, ссылка на созвон, повторяемость. Пишется один раз на старте в `RecordingEntry.calendarMeta` и в документ встречи (YAML `calendar_*` в Obsidian-формате, строки шапки в простом). **Ничего этого пока не читает** — снимается ради будущей склейки встреч в проекты (серия и состав участников — ключи, а не догадки).

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| ~~rev-11~~ | **Закрыто:** `startRecordingFromZoom` спрашивает `CalendarService.isMutedForRecording()`. Сверено 2026-07-27 | — | ✅ |
| rev-36 | Dismiss не переживает перезапуск | 🔵 | 📋 |
| rev-14 | Нет Join / открыть Zoom-ссылку из Upcoming | 🔵 | 📋 |
| calendar-1-4 | Нет «Записать?» для не-Zoom встреч (plan-v2 1.4) | 🔵 | 📋 |

**Здоров, когда:** «Don't record» реально глушит ближайшую Zoom-встречу.

---

### 10. UI

**Файлы:** `MainView.swift` · `RecordingDetailView.swift` (~1600 строк) · `RecordingInProgressView.swift` · `SearchPalette.swift` · `MenuBar*.swift` · `Onboarding*` · `SettingsSheet.swift` · `PropellerUI/`

**Здорово (2026-07-24…27):** ✅ онбординг требует Mic + Screen Recording для Next (Notes / Launch optional); glass `#0A0A0A`@80%; спиннер списка только при `.recording` / `activity.concerns(id)`; live title не латчит `titleManuallySet` на авто-«Recording …»; ложные `titleManuallySet` чистятся при load; убран нерабочий Update-pill из top bar.

**Сломано (правда и мёртвые контролы):**

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| ~~onboard-lie~~ | **Исправлено:** Screen Recording — отдельный обязательный грант в онбординге; Google = EventKit через системные аккаунты | — | ✅ |
| ~~perm-gate~~ | **Частично закрыто онбордингом** (Screen Recording обязателен). Гейт при каждом старте записи, если разрешение отозвали mid-life — всё ещё слабо | 🟠 | ✅ |
| ~~rev-12~~ | **Закрыто:** `showingDiscardConfirm` + `cancelRecording()` в `RecordingInProgressView`. Сверено 2026-07-27 | — | ✅ |
| empty-home | Нет Record CTA и пустого состояния библиотеки | 🟠 | 📋 |
| rev-7 | ◐ **Половина исправлена 2026-07-25:** `player.stop()` добавлен в `beginRecording`. Осталось: ранний выход `autoLoadAudioForPlayer` при `totalDuration > 0` — грузит не тот файл | 🟠 | ✅ |
| ~~rev-3~~ | **Исправлено:** busy-спиннер / «Generating…» завязаны на `activity.concerns(entry.id)` | — | ✅ |
| rev-20 | Бейдж «Mic only» глобальный — залипает на чужих встречах | 🟠 | ✅ |
| ~~rev-17~~ | **Исправлено 2026-07-25:** `MenuBarContentView.status` учитывает `recapStep` | — | ✅ |
| P1 | SearchPalette: полнотекстовый поиск гоняется по всем транскриптам на каждый рендер (computed, дёргается многократно) | 🟠 | ✅ |
| P2 | RecordingDetailView: `NSRegularExpression` компилируется в `body` на каждый рендер | 🟠 | ✅ |
| ~~P3~~ | **Исправлено 2026-07-25:** `recorder` проброшен как `@ObservedObject` в `RecordingInProgressView`. _Свечение по краям окна в `MainView` — та же болезнь, не исправлено_ | 🟠 | ✅ |
| P4 | Целый `AppState` (~25 `@Published`) в каждой view → пересборки дерева | 🟠 | ✅ |
| P7 | `distinctSpeakerNames` / `loadPersistedSegments` декодируют JSON в `body` на каждый рендер | 🟠 | ✅ |
| waveform-dead | `WaveformScrubber` не встроен в деталь | 🔵 | 📋 |
| followup-deadend | Follow-up — тупик без саммари; «regenerate» = локальное сжатие, не LLM | 🔵 | 📋 |
| ru-en | Языковой раскол: UI ≈ EN, уведомления RU, теги/LLM RU | 🟠 | ✅ |
| H1 | Мёртвый UI: тело `MenuBarPanelView` (используется только статик `showMainWindow`), старый `OnboardingView` ⚠️ **`markdownURL` и кластер participants/reassign — ЖИВЫЕ, не удалять** | 🔵 | ✅ |

**Здоров, когда:** каждое обещание в онбординге мапится на реальное разрешение/фичу 1:1; нет контролов, которые ничего не делают; статус одной встречи не показывается на другой; поиск и деталь не пересчитывают всё на каждый кадр.

---

### 11. Сборка и раздача

**Файлы:** `build.sh` · `.github/workflows/build.yml`

| ID | Проблема | Тяжесть | Пров. |
|---|---|---|---|
| ~~dmg~~ | **Закрыто:** DMG + GitHub Releases (3 релиза) + `appcast.xml` отдаётся по фиду. Сверено 2026-07-27 | — | ✅ |
| ~~build-soft-gigastt~~ | **Неверно, сверено 2026-07-27:** сборка падает без `gigastt` / Sparkle / моделей. _Codesign-ошибки всё ещё глотаются_ | 🔵 | ✅ |
| ~~adhoc-tcc~~ | **Снято 2026-07-25:** `build.sh` подписывает сертификатом «MeetingRecorder Dev» (стабильная идентичность) → TCC между сборками больше не слетает | — | ✅ |
| — | Нет `COLLEAGUES.md` (инструкция ПКМ→Открыть, разрешения, Ollama/ключ) | 🔴 | ✅ |
| — | CI гоняет только `swift build`, тесты не запускаются | 🟠 | ✅ |
| H2 | Зашитый dev-путь `/Users/levonlobanov/...` в `GigasttSidecar` **и** `Bench/Harness.swift:160` | 🔵 | ✅ |

---

### 12. Тесты и метрики

**Файлы:** `Tests/MeetingRecorderTests/` · `PropellerPure/` · `Bench/` · `benchmarks/` · `PropellerMetrics`

**Здорово:** ✅ MVP из [plan-testing-metrics.md](archive/plan-testing-metrics.md) частично сделан — сигнпосты (`PipelineMetrics.interval`) вшиты в пайплайн, есть батч-харнесс `Bench/`, фикстура `ru-short-2spk`, `benchmarks/report.md`.

**Сломано:** тестов на запись/микс/SCK/сайдкар нет (`tests-thin`); бенч-харнесс **оставляет процесс gigastt живым** (`retainedGigastt` не убивается) ✅; baseline ещё не снят на реальных прогонах.

**Галерея состояний (2026-07-28/29):** `UIStateCatalog` — 41 состояние как данные, полнота проверяется против типов (`UIStateCatalogTests`). `./build.sh --gallery` вкомпиливает окно-галерею и `--gallery-export <dir>`, снимающий все экраны в PNG; в обычной сборке ничего этого нет. Съёмка идёт на фикстурном архиве (`GalleryFixture` + `Preferences.galleryArchiveRoot`), реальный архив не читается и не пишется, воркер заморожен. Экспортёр хеширует кадры и печатает группы дублей — две группы сейчас честные (три очереди в UI неразличимы; шаг скачивания модели без прогресса намеренно). Порядок работы и разложение по сценариям в Фигме — [design/state-gallery.md](design/state-gallery.md).

---

## Часть III — Что уже здорово (не сломать при рефакторинге)

ASR-чекпоинт с возобновлением диаризации · карантин битого индекса · пересборка микса из стемов · quit-flush · watchdog микрофона · ленивый gigastt с idle-stop · **чанкинг длинных встреч (анти-413)** · Keychain · idle-friendly Zoom-детектор · заметки-якоря в промпте · бэкфилл саммари · пайплайн по `recordingID` · **SCK-primary + честный mic-only на stop** · календарный заголовок при старте · `languageLock` на рекапе · брендовый dark-glass UI (`#0A0A0A`@80%).

---

## Часть IV — Решения владельца (не код)

1. **Язык UI** — RU целиком для менеджеров, или осознанно EN UI + RU уведомления?
2. **Саммари без Ollama** — требовать модель/ключ в онбординге, или шипнуть с честным «summary unavailable»?
3. **Definition of Done для R1** — предложение из ревью: 3 коллеги, 1 неделя, у каждого ≥1 саммари без пинга автору.
4. **Судьба `ProcessTapAudioCapture.swift`** — ~~оставить dormant~~ → **удалён 2026-07-29** (решение владельца). Причина: замер на живом звонке снял сценарий, ради которого его держали.

---

## Часть V — Порядок работ

**Сейчас (2026-07-27, вечер):** первая раздача. DoD — 5 коллег × неделя × ≥1 саммари без пинга автору; чеклист прогонов — [dogfood-checklist.md](dogfood-checklist.md).

**Прогон «с нуля» 2026-07-27** (снос приложения + разрешений + системной Ollama) закрыл главный пробел: путь первого запуска у коллеги ни разу не выполнялся, потому что `adopt-if-present` всегда подхватывал системную Ollama на машине владельца. Он же вскрыл лишние 885 МБ и зависание на 85 % — см. Часть 0б.

**Дальше — по компонентам.** Порядок по «сколько боли снимает за единицу работы»:

1. **Обратная связь от коллег** — DoD в работе, ждём первых прогонов. Всё ниже двигать по тому, что реально всплывёт.
2. **Саммари** (компонент 6) — `cloud-default`: согласие на облако. Единственный оставшийся вопрос доверия.
3. **Заметки** (компонент 7) — `rev-10`: ⌘K на другую встречу во время записи пишет заметки не в тот entry.
4. **Zoom** (компонент 8) — `zoom-notify`: без разрешения на уведомления запись стартует незаметно.
5. **UI-перф** (компонент 10, P1–P7) — дёшево и заметно.
6. **Аудио-качество** (компонент 1, M1–M5) — только с замером (WER до/после).
7. **Хотворды** — замерить прирост: `reprocess()` на записи со словарём и без, продиффать транскрипты (`vocab/slang-vocabulary.md`, раздел «Как применять»).

---

## Часть VI — Карта документации

| Документ | Роль | Живой? |
|---|---|---|
| `STATE.md` (этот) | **Источник правды** по состоянию и дефектам | да |
| `README.md` | Вход в репозиторий, карта каталогов | да |
| `CHANGELOG.md` | Что накопилось с последнего релиза + порядок сборки релиза. Строка сюда — в том же коммите, что и изменение поведения | да |
| `design/state-gallery.md` | Галерея состояний: как переснять, фикстурный архив, раскладка по сценариям в Фигме | да |
| `meeting-recorder/docs/AUDIO-CAPTURE-CASES.md` | Захват звука: обещания, полный список юзкейсов, где логика врёт, куда идти | да |
| `meeting-recorder/docs/PLAN-CAPTURE-AND-LIVE.md` | План перехода на process tap + агрегатное устройство + FluidAudio (Parakeet v3), с гейтом по качеству русского и этапами | да |
| `meeting-recorder/docs/ECHO_AND_MIX_EXPERIMENTS.md` | Замер задвоения: эхо, сдвиг стемов, некогерентность, почему AEC пока невозможен | да |
| `COLLEAGUES.md` | Инструкция для тестировщиков (установка, разрешения, проблемы) | да |
| `dogfood-checklist.md` | 15 прогонов перед раздачей + DoD | да |
| `meeting-recorder/CLAUDE.md` | Гайд для агентов | да |
| `meeting-recorder/docs/SPEC.md` | Продуктовое поведение | да |
| `meeting-recorder/docs/ARCHITECTURE.md` | Архитектурные решения | да |
| `vocab/` | Жаргон встреч → хотворды. `hotwords-core.txt` — источник для `BuiltinHotwords`, расхождение ловит тест | да |
| `design/` | UI-спека и состояния экранов | да |
| `product-ideas.md` | Реестр идей: убито / отложено, с причинами | да |
| `plan-v2.md` · `plan-optimization.md` · `plan-testing-metrics.md` | **История решений.** Не редактировать под текущее состояние — при расхождении верить `STATE.md` | нет |
| `release-review.md` · `tasks-for-approval.md` | Разбор 2026-07-25. Дыры разошлись по компонентам выше | нет |
| `archive/`, `phase*/` | История, не трогать | нет |

**Правило против дрейфа:** доведён компонент — в том же заходе правится `STATE.md` и, если менялось поведение, `SPEC` / `ARCHITECTURE`. Планы (`plan-*`) заморожены как история: дописывать в них текущее состояние — это и есть механизм, которым дрейф возвращается.

**Известные ловушки, проверенные на практике 2026-07-27:** доки трижды за день расходились с кодом (`keep_alive: 10s` против 90, «GigaAM ~225 МБ, автозагрузка» против 247 МБ из бандла, половина 🔴 давно закрыта). Прежде чем принимать решение «мы не готовы» по доке — сверяться с кодом.
