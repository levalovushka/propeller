# Качество саммари и расшифровки — где мы остановились

_Сессия 2026-08-08 … 2026-08-12. Всё зелёное закоммичено, тестов 692, `./build.sh`
проходит. Этот файл — чтобы продолжить без контекста той сессии._

Подробности живут в двух местах и не дублируются здесь:
[`tools/recap-lab/README.md`](tools/recap-lab/README.md) — про качество конспекта,
[`meeting-recorder/docs/ASR-QUALITY.md`](meeting-recorder/docs/ASR-QUALITY.md) — про
точность расшифровки. Ниже — состояние и следующий шаг.

## Сделано и уехало в продукт

| что | где |
|---|---|
| Короткий промпт извлечения + второй проход-редактор | `Sources/RecapService.swift` |
| Детерминированные проверки конспекта, они же адреса для редактора | `PropellerPure/RecapLint.swift` |
| Нарезка длинной встречи по границам реплик | `PropellerPure/TranscriptChunking.swift` |
| Канонизация терминов в конспекте | `PropellerPure/TermCanon.swift` |
| Словарь: +32 термина, −2 вредных | `vocab/hotwords-core.txt`, `PropellerPure/BuiltinHotwords.swift` |
| Расшифровка по двум дорожкам вместо микса, эхо снимается по словам | `PropellerPure/StemAssembly.swift`, `TranscriptionService` |

Замеренный эффект по конспектам: решений найдено 28 → 36, задач 16 → 22, пассив
17 → 1, при сохранности состава (ни одна встреча не потеряла пункт при редактуре).
На длинной встрече нарезка дала 13 решений против 4.

## Инструменты замера (в приложение не входят)

```bash
# конспекты
cd tools/recap-lab
python3 run.py --variant current      # прогнать корпус текущим промптом
python3 polish.py --dir out/current   # второй проход с адресами линтера
python3 lint.py --dir out/current     # таблица находок
python3 chunked.py --variant v4       # для встреч, не влезающих в окно

# словарь
cd vocab
python3 mine.py                       # чего не хватает (по расшифровкам)
python3 mine.py --source recaps       # что ломается уже в конспекте → TermCanon
python3 mine.py --audit               # что в словаре вредит

# принадлежность реплик и полнота слов
python3 tools/echo-probe/ownership.py <каталог> <имя>      # смешанные строки по словам
python3 tools/echo-probe/owner-echo.py <каталог> <имя>     # чужие слова у владельца
python3 tools/echo-probe/coverage.py <каталог> <кандидат.srt> <эталон.srt>
STEMS_OUT=/tmp/attrib python3 tools/echo-probe/stems.py <id>   # расшифровать оба стема
```

## Подключено 2026-08-12 вечером: лента собирается из дорожек

Офлайн-путь больше не читает микс, когда рядом лежат стемы. Что стоит в коде:

| что | где |
|---|---|
| Снятие эха по таймингам слов + сборка ленты | `PropellerPure/StemAssembly.swift` |
| Два прохода ASR по стемам, метка дорожки на реплике | `TranscriptionService.transcribeStems` |
| Диаризация только системного стема, лента без энергии | `TranscriptionService.assembleFromStems` |
| Слова с таймингами от сайдкара (и через нарезку) | `BoundaryResponses`, `GigasttClient.ASRResult` |
| Прогон всего пути на готовой записи | `--stem-probe <файл.wav>` |

Замеры и отвергнутые заходы — [`meeting-recorder/docs/ASR-QUALITY.md`](meeting-recorder/docs/ASR-QUALITY.md),
раздел «Подключено». Коротко:

| | микс | лента |
|---|---|---|
| чужих слов в репликах владельца (встреча тестировщика) | 9.9 % | **4.7 %** |
| полнота чужой речи, та же встреча | 93.1 % | **100 %** |
| полнота чужой речи, «Обед» (58 мин, колонки) | **57.1 %** | **100 %** |

A/B на семи записях архива со стемами (одно аудио, тот же ASR и диаризатор,
прежняя сборка против этой): микс не хуже дорожек на пяти встречах и катастрофичен
на двух. «Обед» — чужая речь 65.4 % → 93.6 %. Демо клиенту `20260806_160258` —
**в миксе 9.2 % слов владельца**, прежняя сборка не отдала ему ни одной строки из
386; с дорожек у него 1050 слов. Причина известна и замерена раньше: свой голос в
своём микрофоне на 4–6 dB тише дальней стороны, и в сумме дорожек тихая сторона
пропадает.

**«Ноль смешанных строк по построению» — опровергнуто.** ASR режет микрофон не по
границам чужой речи, поэтому эхо и своя речь приезжают в одном сегменте, и
построчного правила не хватает. Помогли тайминги слов; окно 0.5 с выбрано по
разбросу (80 % совпадений в 0.04 с, 95 % в 0.12 с).

Цена: **два прохода ASR вместо одного** — 90 с на 17 минут, замерено пробой.

### Как перемерить

```bash
# 1. сайдкар (модели уже на диске)
/Applications/Propeller.app/Contents/MacOS/gigastt serve --offline -p 9891 \
  --model-dir "$HOME/Library/Application Support/Meeting Recorder/gigastt-models"

# 2. расшифровать оба стема записи архива (сегменты + слова, куски по 20 минут)
STEMS_OUT=/tmp/attrib python3 tools/echo-probe/stems.py 20260729_155956

# 3. собрать ленту тем же кодом, что в приложении
cd meeting-recorder/swift
swiftc -O PropellerPure/*.swift ../../tools/echo-probe/assemble/main.swift -o /tmp/assemble
/tmp/assemble /tmp/attrib 20260729_155956

# 4. числа
python3 tools/echo-probe/owner-echo.py /tmp/attrib 20260729_155956   # чужие слова у владельца
python3 tools/echo-probe/coverage.py /tmp/attrib \
  20260729_155956.assembled.srt 20260729_155956.sys.srt              # полнота чужой речи
```

Весь путь приложения целиком — пробой, она пишет отчёт в
`~/Library/Application Support/Meeting Recorder/stem-probe.txt`:

```bash
/Applications/Propeller.app/Contents/MacOS/MeetingRecorder \
  --stem-probe ~/.meeting-recorder/recordings/20260729_155956.wav
```

Сравнение «до/после» на одном аудио: собрать прежнюю сборку в отдельном worktree
(`git worktree add … HEAD~2`), положить туда `Sources/StemProbe.swift`, собрать
`swift build` и запускать отладочный бинарник внутри копии бандла с
`DYLD_FRAMEWORK_PATH=<копия>.app/Contents/Frameworks` — иначе он падает на
`bundleProxyForCurrentProcess is nil` и на `@rpath` до Sparkle.

### Замер конспектов остановлен по решению владельца

Сравнение конспектов «микс против дорожек» на корпусе (полный пайплайн: извлечение,
нарезка, проход-редактор, две прогонки на сторону из-за шума модели ±20 %) начато и
**прервано**: владелец сказал, что сравнение LLM + транскрипт уже проведено в
другом чате. Готово было 5 конспектов из 28; скрипт остался в
`tools/echo-probe/`-стиле у меня в песочнице и в репозиторий не уехал.

Что успело показаться на одной встрече (дейлик, два прогона) — счётчики решений и
задач плавают в пределах шума, но по миксу четыре решения из семи приписаны
«Системе», потому что реплики владельца рассыпаны на обрывки по 5 слов; по дорожкам
ответственные названы по именам. Одна встреча — не доказательство, поэтому это
наблюдение, а не результат.

### Что осталось незакрытым в этом месте

- **`relabelSegmentsFromStems`** («Уточнить спикеров», интерфейса нет) по-прежнему
  расставляет спикеров по энергии дорожек. На ленте из стемов это правило врёт: оно
  перепишет верные имена. Дверь к нему не приделана, поэтому не горит, но чинить
  надо вместе с решением про шесть функций без интерфейса (STATE.md, Часть V, п. 2).
- **Один прогон полного набора тестов упал по SIGSEGV** (в `xctest`, не в моём
  коде); повторить не удалось — четыре следующих прогона зелёные, 692 теста.

## Опровергнуто замером — не переоткрывать

- **Длинный промпт конспекта.** 2525 → 5183 символа правил: решений 28 → 18, три
  встречи из восьми вообще без решений. На 4B-модели длина конкурирует с полнотой.
- **Микс теряет слова.** На четырёх встречах целиком микс доносит 89–95 % обеих
  сторон. Исключение одно — колонки («Обед» 58.5 %).
- **Метрика смешанных строк по времени.** Давала 68–87 %, а на заведомо
  правильной ленте даёт 96–99 %: сегменты ASR покрывают почти всё время встречи.
  Правильная мера — по словам (`ownership.py`), и она даёт 10.6 %.
- **«Склейка дорожек даёт ноль смешанных строк по построению».** Нет: ASR режет
  микрофон не по границам чужой речи, и построчное снятие эха оставляет 10.4 %
  чужих слов — как у микса. Лечится только послововым снятием по таймингам.
- **Построчное снятие эха поверх пословового.** Выбрасывает 142 слова владельца из
  773, чтобы снять два чужих из семнадцати. Оставлено запасным путём — на случай,
  когда сайдкар не отдал таймингов.
- **Время слова по его номеру внутри реплики** (вместо таймингов). Хуже грубого
  окна на весь сегмент: 7.9 % чужих слов против 4.3 %. Один системный сегмент
  бывает длиной 80 секунд, и линейная доля внутри него врёт на секунды.
- **VAD против мусора в начале записей.** С `--vad` та же каша: там плохой звук,
  а не тишина.
- **Фонетическая коррекция по индексу словаря.** 8 срабатываний, 6 из них — порча
  правильного текста. Метод рассчитан на каталоги в миллионы имён.
- **Детерминированное вырезание выдуманных сроков.** Отклонено владельцем: зацепит
  лишнее. Сроки остаются задачей редактора.

## Не проверено, но дёшево и данные уже лежат

**Словарь одной записи.** Участники и организатор из `CalendarMeta` (включая разбор
почты: `radik.sitdikov@vkteam.ru` → «Радик Ситдиков»), заметки со встречи (набраны
руками и готовы раньше, чем стартует расшифровка), термины из прошлых встреч той же
серии (`seriesID`). Контекстное смещение по точному списку участников даёт до 76.8 %
на именах собственных. Это не общий словарь, поэтому не платит ценой, из-за которой
удалены «нид» и «эдит». `gigastt` принимает пофразовые веса (`\t<weight>`).

Пайплайн не трогает вовсе — бьёт ровно в «Фенькью Пейдж» и «Ситдикова».

## Долг

- **Эталон расшифровки.** 10–15 минут аудио, размеченных руками, по одному
  фрагменту на жанр (наушники, колонки, вживую). Без него WER не посчитать; все
  замеры выше — относительные, где эталон это чистая дорожка.
- **`golden/` в recap-lab** — одна встреча размечена и согласована, нужно ещё 4–5.
- Файл `20260727_193927-20260727_193927.md` в архиве переименован приложением во
  время неудачного теста 2026-08-12; старый текст лежит в `recordings.json.bak`.
  Владелец сказал не переживать, решение не принято.
