# Рабочий лог

Что сделано, зачем, как проверено, что осталось. Новейшее сверху.
Формат — DOCS.md, слой «Рабочий лог».

## 2026-08-20 · Разбор созвона с собой: три хвоста с числами

- **Что:** три записи в `TAILS.md` — имён нет на 1×1, микрофон в мьюте попадает
  в расшифровку, отсутствие права на AX ничем не заметно. Кода не касался.
- **Зачем:** созвон владельца с собой (`20260820_213214`) дал первую трассу на
  двух участниках после выпуска метода, и она объясняет сразу три вещи, которые
  иначе выглядели бы как поломка сборки.
- **Числа, взяты из трассы `<id>.calltrace.jsonl` и ленты:** 316 опросов;
  метка `active speaker` — 0 раз; обе плитки 608×342 (геометрия мертва); право
  живо (`"trusted": true`); из 23 реплик владельца **21** сказана при
  замьюченной плитке Мака (мьют с t=7,26, 246 опросов из 287); третий маркер
  хвоста `звуковое устройство не подключено` — 3 опроса; своей плитки Zoom не
  помечает (ни `(Me)`, ни `(Я)`).
- **Что из этого следует, а не только записано:** на двух плитках метка
  говорящего не нужна — знай, какая плитка владельца, и вторая по определению
  дальняя сторона. Тот же якорь (запомненное zoom-имя владельца) закрывает и
  вето по мьюту. Удвоение реплик в ленте этой встречи — артефакт созвона с
  собой (голос вернулся с телефона через Zoom), не дефект снятия эха.
- **Проверено:** это разбор и документы; счёт — скриптом по трассе и по
  расшифровке, полученной через отгруженный `PropellerMCP`. Архив не менялся.
- **Осталось:** числа метода принадлежат `DIARIZATION.md` H12 — её ведёт сессия
  про механику имён, ей отправлено. Трасса живёт рядом с записью до retention.

## 2026-08-20 · Доки догнали правки про имена

- **Что:** `STATE.md` §6 — абзац про состав в промпте (что было сломано, чем
  проверено) и строка `roster-shape` в таблицу «Сломано»; `CHANGELOG.md` —
  пункт «Конспект знает, кто был на встрече» рядом с пунктом про написание имени.
- **Зачем:** правило репозитория — поменял поведение, строка в CHANGELOG; и
  источник правды не должен говорить, что фильтр исполнителя по составу работает,
  когда до вечера он не работал ни на одной встрече.
- **Проверено:** это правка документов, кода не касается; `git diff --stat` —
  `CHANGELOG.md +5`, `STATE.md +3`. Числа в абзаце — из прогонов, записанных
  выше в этом же логе.
- **Осталось:** ничего. Живая проверка в приложении — за владельцем, на созвоне
  с собой со второго аккаунта.

## 2026-08-20 · Состав встречи не доезжал до промпта ни на одной живой встрече

- **Что:** `RecapDocument.participants(fromTranscript:)` научена второму виду
  подписи — `**Кто** · 12:34`, тому, который лежит на диске
  (`MeetingMarkdown.transcriptBody`, `MeetingMarkdown.swift:129`). Знала она
  только внутренний вид пайплайна `[Кто] [12:34]`. Плюс два теста, где вход
  строится **выводом самого writer'а**, а не переписан руками, и длинная метка
  `83:12`.
- **Зачем:** саммари читает файл (`AppState.runRecap` → `String(contentsOfFile:)`,
  `AppState.swift:2314`), то есть ровно тот вид, которого функция не знала.
  Следствия молчаливые, все три: строки «Участники» в промпте не было никогда;
  `RecapLint.assigneesOutsideRoster` выходит на пустом составе, то есть защиты от
  выдуманного исполнителя не существовало; `PersonCanon` получал пустой состав и
  не правил ничего. Ни один тест этого не видел — все фикстуры написаны во
  внутреннем виде, включая мои утренние.
- **Как нашлось:** попытка прогнать канон на живой встрече вместо синтетики.
  Прогон через сам writer: состав по внутреннему виду
  `["Левон", "Aleksandra Nikandrova", "костя"]`, по файлу с диска — `[]`.
- **Проверено:** `swift test` — **1027 тестов, 5 пропущено, 0 падений**. Дальше
  на живых данных, без записи в архив (расшифровки и конспекты читал отгруженным
  `PropellerMCP`, он только читает):
  - `20260820_180102` (VK Музыка, 324 реплики, 25k символов на диске): состав из
    файла `["Левон", "Александр Яшин", "костя", "Aleksandra Nikandrova"]`, строка
    «Участники» в промпте есть, нарезка не нужна, окно 16384. Полный путь
    конспекта тем же промптом, что в приложении, `qwen3.5:4b`, `think:false`,
    t=0 → черновик 2203 символа за 20 с; канон не поменял ничего, и это верно:
    модель написала «Левон» ровно как в ленте.
  - `20260820_160014` (та, где кросс-скрипт): состав
    `["Левон", "Arina Soldatenkova", "Вячеслав Киржаев"]`, канон правит **три
    строки** живого конспекта, «Арина Soldatenkova» → «Arina Soldatenkova»,
    больше ничего.
- **Попутно подтвердилось поведение, описанное в коде:** первый прогон я сделал
  без `think:false` — мышление съело всё окно 16k и `content` вернулся пустым,
  ровно как написано в комментарии к `callOllamaTracked`.
- **Осталось:** в приложении это ещё не наблюдалось живьём — нужна новая встреча
  (перегенерации конспекта у существующей встречи в интерфейсе нет: `regenerateRecap`
  — функция без двери, `STATE.md` Часть V, п. 2). Находка `inventedActor:
  Участники` на конспекте 16:00 висит и не связана с этой правкой.

## 2026-08-20 · P0 №4 и №5: заметки не исчезают, размер окна больше не угадывается по ширине

- **Что:** `MeetingPane.leftColumn` — ветка «человек выбрал Транскрипт» теперь
  тоже получает `notes:`, как и ветка-заменитель. `SceneWindowChrome`: чёрный
  список ширин (1100, 1020) заменён провенансом — `placeCentered` запоминает
  написанный кадр в `PropellerMainFramePlacedByUs`, и кадр считается нашим,
  пока совпадает с этой строкой символ в символ. Правило вынесено в
  `PropellerPure/WindowFrameProvenance` (8 тестов). Две строки в `STATE.md` §10.
- **Зачем:** P0 №4 и №5 из списка ревизии. Написанное человеком на встрече
  исчезало от переключения колонки. Ширина не может сказать, кто её написал:
  растянувший окно до 1100 терял размер при каждом открытии, навсегда и без
  объяснения на экране. Требование владельца — пользовательский размер обязан
  возвращаться после перезапуска — выполняется по построению: без маркера кадр
  человеческий, и установки, где маркера нет, трактуются в его пользу.
- **Проверено:** `swift test` — **1025, 5 пропущено, 0 падений** на `8b5f3d0`;
  `WindowFrameProvenanceTests` 8/8, включая «один драг делает кадр человеческим
  навсегда» и «подпиксельный дрейф — не другой размер». `./build.sh` собрал и
  поставил.
- **Не проверено, и это важно:** сквозной прогон на живом окне **не удался**.
  Пробовал через `defaults`: ставил кадр 1100×800 без маркера, с маркером, и
  удалял ключи целиком — во всех трёх случаях значения не менялись. Причина
  найдена третьим опытом: на чистом запуске **не записывается ни кадр, ни
  маркер**, то есть `showMain` не вызывается вовсе — окно не показывается, пока
  человек его не откроет, а меню-бар мне недоступен (Accessibility для
  `osascript` запрещён, ошибка −25211). Значит первые два «замера» были пустыми:
  кадр возвращался неизменным потому, что его никто не читал. Правило покрыто
  тестами, проводка проверена только чтением кода и сборкой.
- **Осталось:** сквозную проверку делает владелец в один жест — открыть окно,
  растянуть, выйти, открыть снова: размер обязан вернуться. Исходный кадр в
  `defaults` я вернул на место (`1833 -10 797 760 …`), маркер удалён, как и было.

## 2026-08-20 · Проход UX-райтера по прототипу настроек

- **Что:** в `design/prototypes/settings-v2.html` переписаны все вторые строки и
  пять заголовков: «Показывать Propeller в меню баре» → «Иконка в меню баре»,
  «Показывать запись в чёлке» → «Запись в чёлке» (осталось только «Заметка по
  ⌃⌥N»), «Ваше имя в расшифровках» → «Ваше имя», «Промпт саммари» → «Промпт»
  без подписи, «Запускать при входе» → «Запуск при входе» — вся группа «Запись»
  теперь одними существительными. Подпись «Имя как в каталоге провайдера»
  показывается только у OpenRouter, где префикс вендора и правда нужен; у
  OpenAI и Claude строка модели идёт без подписи. Тем же проходом по решению
  владельца убраны метка «изменено» (со счётчиком правок и «Вернуть как было») и
  поиск по настройкам в ⌘K — вместе с их разметкой, CSS и JS; доводы за оба
  перенесены в раздел «Отвергнуто владельцем · 2026-08-20» вкладки
  «Спецификация», чтобы не вернулись «на всякий случай». Следующим кругом по
  решениям владельца: подэкраны промпта и словаря сняты — оба снова строками в
  списке (`.stack`), аналитика уехала из «Что уходит наружу» в «О программе»,
  подпись словаря сведена к «Имена, компании и сленг», подпись локального
  провайдера — к «Отвечает локально» («локально» уже означает, что транскрипт
  не уходит).
- **Зачем:** заказан проход по многословию. Правило §5.6 дока — вторая строка
  только там, где без неё непонятно, и в одну строку; половина подписей
  объясняла то, что заголовок уже сказал.
- **Проверено:** вторые строки 786 → 433 знака (−45 %), 110 → 67 слов;
  перерисован в браузере на 1260×900, снято два экрана; JS после правки цел —
  смена провайдера на OpenRouter поднимает подпись модели, на OpenAI прячет
  (проверено чтением DOM); после сноса — ноль `.chg` и `.pal` в DOM, ноль ошибок
  в консоли, сегментед и контекстные строки живы. После сноса подэкранов —
  21 строка в 6 группах при потолке 24; колонка скроллится на 1909 pt при 710 pt
  видимых, то есть «Файлы», «Наружу» и «О программе» ушли за второй экран (это
  и есть цена инлайнового редактора, замерено в DOM). После сжатия поля промпта
  с 260 до 80 pt (решение владельца) — 1729 pt, «Файлы и место» встаёт сразу за
  группой; поле показывает три строки из содержимого 229 pt. Последний круг
  правок владельца: имена групп «MCP» и «Хранилище» возвращены (переименования
  уехали в «Отвергнуто»), «Анонимная аналитика» → «Отправлять аналитику» без
  длинного тире в подписи, поле имени сужено 220 → 150 pt, «Запись в чёлке» →
  «Индикация записи в чёлке», «Запуск при входе» → «Запуск Propeller при входе»
  (в группе «Запись» читалось как «запускать запись при входе»).
- **Осталось:** заголовок группы «Расшифровка и саммари» длинный, короче не
  придумал; текст вкладки «Спецификация» не сокращал — это документ, а не экран.
  Без поиска потолок 24 строки становится жёстче: находимость держат только
  заголовки групп и порядок. **Главное открытое:** чем заменить подэкраны
  промпта и словаря — четыре кандидата с ценой каждого выписаны в
  «Спецификации», раздел «Промпт и словарь — открытая вилка».

## 2026-08-20 · Канон имён вписан в STATE, дыра «заглушка как участник» сошлась в один предикат

- **Что:** `STATE.md` §6 получил файлы `PersonCanon.swift` и `RecapDocument.swift`
  в шапку компонента и датированный абзац с порядком сборки конспекта: черновик
  → канон имён → редактура → `RecapLint.grounded` → `TermCanon`, с доводом,
  зачем канон появился, и с тремя вещами, которых он сознательно не делает.
- **Зачем:** `PersonCanon` лёг в main (`9e77fc0`), а в источнике правды его не
  было — ровно класс Д1 из ревизии («STATE не знает о том, что выпущено»).
  Владелец кода предложил выбор, кто пишет строку; взял себе, документация в
  этом заходе моя.
- **Проверено:** оба чужих коммита открыл, а не поверил на слово.
  `RecapDocument.swift:69` действительно зовёт `SourceAwareSpeaker.isPlaceholder`,
  свой регекс (вместе с `|^Я$`) удалён целиком — `228c06d`. `PersonCanon` в main,
  вызов в `RecapService.swift:367` до редактуры. `swift test` — **1017,
  5 пропущено, 0 падений** на дереве с `358297d`.
- **Осталось:** дыра «заглушка выдаётся за человека» была в **трёх** местах, а
  найдена в одном: файл встречи (мой, `8094c0e`), ростер промпта саммари (его,
  `228c06d`) и `RecapLint`, который сверяется по составу. Теперь вопрос задаётся
  одним предикатом из двух потребителей, и второй потребитель охраняет его
  снаружи своей фикстурой. Д1 закрыт не мной: компонент 3 получил абзац про
  журнал окна от его владельца (`358297d`), в `STATE.md` теперь 5 упоминаний
  вместо нуля. P0 №4 (заметки в колонке транскрипта) и №5 (ширина окна) не
  начаты — жду решения владельца.

## 2026-08-20 · Один предикат «это человек?» вместо второго регекса

- **Что:** `RecapDocument.participants(fromTranscript:)` больше не носит свой
  регекс плейсхолдеров — зовёт `SourceAwareSpeaker.isPlaceholder`
  (`PropellerPure/PropellerPure.swift:302`, коммит 8094c0e сессии код-ревью).
  Поведение то же, что было после моей утренней правки, включая «Я».
- **Зачем:** я дописал `|^Я$` в свой регекс (9e77fc0) за минуты до того, как
  ревизия свела тот же вопрос в один предикат рядом с типом, который эти подписи
  и порождает. Два ответа на один вопрос в двух файлах — ровно тот дефект,
  который она чинила; мой фикс был бы третьей копией.
- **Проверено:** `swift test` — **1017 тестов, 5 пропущено, 0 падений**
  (на дереве с вечерними коммитами 2351690, 8094c0e).
- **Осталось:** ничего. Фикстура `RecapDocumentTests` со строкой «Я» осталась —
  она проверяет поверхность, а не реализацию, и теперь охраняет предикат снаружи.

## 2026-08-20 · P0 ревизии: заглушка перестала быть участником, слаг — понимать кириллицу

- **Что:** `SourceAwareSpeaker.isPlaceholder` — один предикат «это подпись, а не
  человек», рядом с типом, который эти подписи и порождает. На него переведена
  `MeetingMarkdown.extractSpeakers` (знала только про `Speaker …`). У
  `speakerSlug` алфавит `[^a-z0-9\s-]` заменён на `[^\w\s-]`. Пять новых
  тестов, две строки в `STATE.md` §5.
- **Зачем:** два подтверждённых дефекта из общего списка ревизии, оба мои —
  перенёс их в `PropellerPure` в шаге 2.2 вместе с кодом и накрыл тестами ровно
  те случаи, которые и так работали (`Speaker N` и латиница).
- **Проверено:** до правки `extractSpeakers` на ленте пути дорожек возвращала
  `["Собеседник", "Я"]`, `speakerSlug("Левон")` → `''`. **На живом архиве дефект
  сработал: 2 файла из 122** со строкой участников несут «Собеседник» —
  прочитано скриптом по `~/.meeting-recorder/meetings`, только чтение. После —
  `swift test` **1017, 5 пропущено, 0 падений**.
- **Осталось:** старые файлы не перезаписываются, правило действует на следующем
  сохранении встречи — переписывать чужой архив ради строки участников я не
  буду без отдельного решения. Третий дефект P0 (`assigneesOutsideRoster` не
  видит `## **Задачи**`) забрал себе propeller-1a: это его сегодняшний код, мой
  замер он берёт как тест. P0 №4 (заметки исчезают из колонки транскрипта) и
  №5 (ширина окна 1100/1020 выбрасывается) не начаты.

## 2026-08-20 · Прототип настроек v2 — жанры строк, подэкраны, память о правках

- **Что:** `design/prototypes/settings-v2.html` — интерактивный HTML-прототип
  раздела настроек на токенах `PropellerUI/Tokens.swift` (тёмная сторона пары):
  6 групп / 22 строки вместо нынешних 6 / 24, блок «Доступ» отделён от решений
  человека, «Нейросети → Расшифровка и саммари», «MCP + аналитика → Что уходит
  наружу», промпт и словарь — подэкраны со значением на родительской строке,
  метки «изменено» + общий «Вернуть как было», ⌘K-палитра с симптомом вместо
  названия, три вида второй строки (пояснение / процесс / отказ). Вкладка
  «Спецификация» в том же файле держит доводы и таблицу «было → стало».
- **Зачем:** заказан разбор редизайна и прототип. Доводы — из `design/settings.md`
  (свои же правила «вопрос, а не механизм» и §3.5) плюс видео Interface Studies
  «Do We Have Too Many Settings?»: проблема словаря (Furnas 1987), забытые
  собственные решения (49 % / 34 %), асимметрия добавления и удаления настроек.
  Код не менялся — это макет для решения, а не реализация.
- **Проверено:** отрендерен в браузере на 1260×900, снят на четырёх состояниях
  (главный экран, файлы/наружу, палитра ⌘K, подэкран промпта); JS проверен —
  переключение провайдера поднимает две контекстные строки, счётчик правок
  считает (4 после смены провайдера), подэкран и возврат работают. Сборка Swift
  не запускалась: ни одного файла проекта не тронуто.
- **Осталось:** не решено, куда уходят права macOS — в «Доступ» или в «Запись»;
  вилка быстрых настроек (пауза автозаписи в поповере) открыта; диагностика
  «почему нет саммари» прототипом не покрыта — ей место на строке встречи.

## 2026-08-20 · Документация по итогам ревизии: пять расхождений закрыто

- **Что:** `plan-recap-v3.md` уехал в `archive/` со строкой в `archive/README.md`
  и переписанной входящей ссылкой в `OPTIMIZATION.md`. В `product-ideas.md` три
  строки «имплементируем (1.17)» приведены к факту: две **сделано** (1.16.5),
  ансамбль — **убито** решением владельца 2026-08-14 с условием возврата; плюс
  новая строка про отвергнутую парадигму v3, которой в реестре не было вовсе.
  В `design/notifications.md` пять корневых ссылок исправлены на относительные и
  переведены на имена символов вместо номеров строк, раздел Ф0 помечен сделанным.
  В `docs/SPEC.md` сняты три поверхности, которых нет в коде. В
  `design/settings.md` строка retention в таблице «чтобы удалённое не вернулось»
  отмечена вернувшейся (два режима вместо пяти) и дописаны четыре строки §9:
  «Хранить аудио», MCP, OpenRouter, Accessibility.
- **Зачем:** доки блока 8. Реестр решений держал отменённое как планируемое —
  по правилу слоя это гарантирует, что решение переспорят заново. `SPEC.md`
  посылал искать `WindowReveal`, `MeetingNotesColumn` и переименование спикера
  панелью участников — ни одного из трёх в дереве нет. Таблица, существующая
  ровно для того, чтобы удалённое не возвращалось, сама пропустила возврат.
- **Проверено по коду, не по докам:** `WindowReveal` — 0 вхождений в
  `Sources`/`PropellerUI`; `reassignSegments` — **0 вызовов** при живом
  объявлении; три цитаты из `notifications.md` открыл по номерам строк — ни одна
  не указывает на то, что описывает (`AppState.swift:807` — комментарий про
  отмену записи, `:564` — `noteDiskSpace()`, `MainView.swift:101` — `} else {`,
  а строки `.alert("Запись восстановлена")` нет в дереве вовсе). `swift test` —
  **1011, 5 пропущено, 0 падений**; кода я не касался.
- **Осталось:** `STATE.md` не знает про `CallWindowJournal` (0 упоминаний) —
  строку пишет владелец компонента: propeller-1a сейчас упрощает это ядро и
  закоммитит в течение часа, писать поверх незакоммиченного смысла нет.
  Число тестов в `STATE.md` §0ж (718) отстало от 1011, но раздел датирован
  2026-08-06 и по правилу 4 это след, а не ложь — трогать не стал.
  Не тронуты: `plan-speaker-tags.md` и `DIARIZATION.md` (за propeller-1a),
  длина строк `STATE.md` (107 строк свыше 500 символов, самая длинная 3745) и
  гниение цитат как класс — это решение, а не работа.

## 2026-08-20 · Встреча 1×1: метки нет вовсе — подписывает состав, не сигнал

- **Что:** по разбору соседней сессии (трасса `20260820_213214`: 316 опросов,
  меток 0, плитки равные — журнал молчал честно): правило
  `CallWindowJournal.soleCompanion` — ровно две именованные плитки всю встречу,
  одна владельца → все чужие реплики подписываются второй (5 тестов, включая
  «третий выключает», «переименование выключает»); zoom-имя владельца
  выучивается на встречах с меткой и живёт в `Preferences.ownerZoomName`
  (никогда не в ленте); отказ права AX теперь виден
  (`CallJournal.noPermission` + строка лога).
- **Зачем:** 1×1 — самый частый класс встреч, и в нём нет ни метки, ни
  геометрии; состав отвечает там, где сигнал говорения не существует.
- **Проверено:** `swift test` — 1032, 5 пропущено, 0 падений; правило на живой
  трассе 213214 (python-реплика): пара постоянна 218 опросов → «Лева Ловушка».
  Не проверено сквозняком: пересчёт той встречи приложением.
- **Осталось:** якорь заполнится на первой же встрече с меткой; живая колонка
  1×1 пока без имён (только офлайн) — вписано в план; Н2 — два исхода.
  Доски: DIARIZATION.md — третий живой замер; план §2 — Н2 переписан.

## 2026-08-20 · «Сначала запись, потом Zoom» больше не теряет журнал

- **Что:** `handleCallStarted` будит наблюдателя, если Zoom задетектился во
  время уже идущей ручной записи (`AppState.swift`, ветка «already recording»);
  `CallWindowObserver.start` охраняет трассу от перезаписи повторным звонком
  в той же записи (`activeRecordingID`).
- **Зачем:** владелец проверил порядок «запись → Zoom» — встреча
  `20260820_204253` осталась без трассы: наблюдатель включался один раз в
  `beginRecording` и вопрос больше не задавал.
- **Проверено:** `swift build` чисто, `swift test` — 1017/5/0. Живьём порядок
  «запись → Zoom» не перепроверялся; соло-встреча имён не покажет и после
  фикса — там некого подписывать, кроме владельца.
- **Осталось:** живой прогон обоих порядков на настоящей встрече. Пост-скриптум
  вечера: сессия PersonCanon нашла, что «Участники» в промпте были инертны на
  живых встречах — разбор состава знал внутренний вид ленты, а конспект читает
  файл (`**Кто** · 12:34`); чинено её коммитом 4163e48 (обе ветки формата, тест
  от вывода writer'а), унификация копий формата — хвост в TAILS (уточнён вторым обходом: читателей
  внутреннего вида четыре, дискового два; `parseRemarks` — латентная дыра).

## 2026-08-20 · Живой транскрипт называет говорящих

- **Что:** имена из журнала окна — в живой колонке: `LiveTranscript.Turn.name`
  (даётся первому куску реплики, продолжение не пересматривает), доставка из
  машинки наблюдателя (`liveName(at:)`, улики владельца — только признанные
  дедупом реплики, эхо не считается), подпись в `LiveTranscriptColumn`,
  черновик `persistedSegments` несёт имя. Диаризация так не может в принципе —
  ей нужна вся запись.
- **Зачем:** сигнал журнала по природе живой; «Собеседник» на экране был
  потолком дорожек, не метода.
- **Проверено:** `swift build` чисто; `swift test` — 1014, 5 пропущено,
  0 падений (3 новых). Живьём не гонялось — нужна следующая Zoom-встреча;
  первые минуты до опознания плитки владельца строки честно безымянны.
- **Осталось:** ~~STATE.md компонент 3~~ (журнал вписан абзацем, строка «следующая
  работа — имена» закрыта им же; §10 плана — растяжка про PersonCanon против
  будущей нормализации подписей ленты); догфуд живых имён; минута «Динамика» (TAILS); релиз стоит по
  решению владельца до обкатки. Доки: статус упрощения — в plan-speaker-tags
  §11.1, два новых хвоста (решающая трасса «Динамика», линейка лаборатории
  против отгруженного правила) — в TAILS.

## 2026-08-20 · Ядро спикеров упрощено по Opus-ревизии, наблюдатель — на часах записи

- **Что:** `CallWindowJournal` переписан вокруг одной ленты подтверждённых
  образцов (машинка `LiveSpeaker` — и живой, и офлайн путь; `spans` — свёртка);
  единое правило владельца share≥0,5 + этаж 25 опросов (батчевое без этажа
  замеренно короновало не того на 70-с префиксе); снесены `Tile.order`, сетка
  40pt, дотяжка концов; 7 причин молчания → 5. Наблюдатель: свой поток вместо
  пула, AX-таймаут 0,5 с, якорь от часов рекордера (дрейф до 0,64 с) и
  пауза/резюм вместе с записью. Плюс фикс ревизии: «## **Задачи**» жирным
  выключал стража исполнителей.
- **Зачем:** два сглаживания уже разошлись на 3,8 %; владелец просил «проще и
  надёжнее», Opus-ревизор оплатил каждый снос числом по 16 581 живому опросу.
  Геометрия оставлена: вид «Динамик» без меток замерен живьём 2026-08-19.
- **Проверено:** `swift test` — 1011, 5 пропущено, 0 падений; утренняя трасса
  воспроизводит 32 пролёта / 265 с, эталон — 0 с ошибок. Не проверено живьём:
  пауза посреди Zoom-встречи (сегодня пауз не было).
- **Осталось:** живые имена в лайв-колонке (машинка готова, подключение —
  следующий коммит); минута «Динамика» на текущем Zoom — хвост в TAILS.

## 2026-08-20 · «Я» больше не участник встречи

- **Что:** в фильтр плейсхолдеров `RecapDocument.participants(fromTranscript:)`
  добавлено `^Я$` — это `SourceAwareSpeaker.defaultOwnerName`
  (`PropellerPure/PropellerPure.swift:288`), владелец, не вписавший имя в
  настройках. Плюс строка «Я» в фикстуру `RecapDocumentTests`.
- **Зачем:** дыру нашла сессия код-ревью в `MeetingMarkdown.extractSpeakers`
  (там «Собеседник» и «Я» уезжали в участников файла встречи) — то же место
  оказалось и здесь: на встрече без диаризации в промпт саммари уходила строка
  «Участники: Я», и модель сверяла имена исполнителей против неё. Поделили по
  файлам, чтобы не править один регекс дважды: `MeetingMarkdown` — их,
  `RecapDocument` — мой.
- **Проверено:** `swift test` — **1011 тестов, 5 пропущено, 0 падений**.
- **Осталось:** ничего по этой правке. `PersonCanon` от «Я» не страдал и до неё:
  ключи короче трёх букв в таблицу канона не берутся.

## 2026-08-20 · Имя участника в конспекте — одним написанием

- **Что:** новый `PropellerPure/PersonCanon.swift` — свёртка имени (нижний
  регистр, кириллица в латиницу, только буквы), таблица «свёртка → написание из
  состава» и замена по тексту конспекта. Вызов один, в
  `RecapService.generateRecap` сразу после извлечения черновика и **до**
  редактуры (`RecapService.swift:361`). Состав — подписи ленты
  (`RecapDocument.participants`), то есть то же, что уже едет в промпт. Плюс
  `PersonCanonTests` (11 тестов). Лента и транскрипт не тронуты.
- **Зачем:** журнал окна Zoom принёс в ленту имена как они стоят в звонилке, и
  модель половину такого имени переводит: в живом конспекте `20260820_160014`
  стоит «Арина Soldatenkova» при ленточном «Arina Soldatenkova». Сверка
  исполнителя с составом (`RecapLint.assigneesOutsideRoster`) ищет хотя бы один
  совпавший токен, поэтому это написание её проходит за счёт фамилии — а «Арина»
  против «Arina Soldatenkova» не проходит вовсе, и защита от выдуманного
  ответственного тратится на верное имя. Решение владельца 2026-08-20: чинить на
  уровне редактуры саммари, а не в ленте. Состав из календаря в промпт **не
  добавляли** — тем же решением: кого не было в ленте, тот на конспект и не влиял.
- **Ловушка:** `\u{00A0}` внутри raw-строки паттерна — это литеральные символы
  для ICU, а не escape: `NSRegularExpression` бросает «value is invalid» уже в
  `try!`. Поймано изолированным прогоном, до тестов пакета (в паттерне теперь
  сам символ NBSP).
- **Границы замены, все проверены тестом:** только точное совпадение свёртки
  (падежи остаются редактору), разница в одном регистре не правится («костя»),
  перевод имени не сшивается («Alexander» ↔ «Александр»), коллизия свёртки у
  двух участников убивает ключ, метки дорожек (`Speaker S1`) в состав не попадают.
- **Проверено:** `swift test` — **1011 тестов, 5 пропущено, 0 падений** (после
  того как параллельные сессии закоммитили своё; в середине работы пакет вообще
  не собирался, и логика проверялась изолированно — копия `PropellerPure` в
  скретчпаде с `CallWindowJournal.swift` из HEAD плюс те же 15 утверждений на
  `swiftc`).
- **Осталось:** живьём не проверено — настоящая замена случится на следующей
  Zoom-встрече, где журнал даст латинское имя. `./build.sh` не гонял.

## 2026-08-20 · Часть 0 в STATE больше не спорит с Частью II про M1 и M4

- **Что:** строка «по-прежнему сломано» в Части 0е (`STATE.md`, дата 2026-08-06)
  перечисляла M1 и M4 как открытые. Оба я закрыл сегодня в Части II §1 и шапку
  не тронул. Теперь там вычеркнуто со ссылкой на закрытие и оговоркой, что
  строка — след состояния на свою дату, а не состояние.
- **Зачем:** Часть 0 стоит **выше** Части II, читающий сверху вниз встречает
  отозванный вердикт первым. Оба указания на строки в нём вдобавок мертвы:
  `AudioRecorder.swift:521` теперь ветка mic-only-копии, а «строка 558» —
  объявление `mix`, чей комментарий отсылает к `PropellerPure/MixPlan.swift`.
- **Проверено:** `grep` по Части II — M1 и M4 закрыты строками с датой
  2026-08-20; прочитал обе цитаты по коду, ни одна не указывает на то, что
  описывает.
- **Осталось:** ничего по этой правке. Прочие расхождения ревизии документации
  (см. общий список) не тронуты.

## 2026-08-20 · Хвосты ревизии переехали из лога в файл хвостов

- **Что:** восемь строк в `TAILS.md` — из блоков «Осталось» моих записей за
  сегодня: непроверенный на живой встрече потоковый микс, допуск
  `live.app_cpu_cores` внутри своего разброса, непереснятая фикстура
  `ru-pauses-2spk`, недоказанная причина +74 МБ, отсутствующий режим `Bench`
  для микса, `resolveBackend`/`throwIfBadHTTP` в `Sources`, три обёртки над
  `Timecode`, префикс `Temp` на пяти постоянных инструментах.
- **Зачем:** правило DOCS.md — то из «Осталось», что является чьей-то будущей
  работой, переезжает строкой в `TAILS.md`, иначе хвост остаётся в истории, где
  у него нет состояния. За десять записей я этого не сделал ни разу.
- **Проверено:** `gallery-build-broken` сюда не переехал намеренно — это дефект
  компонента, его слой `STATE.md` §10, и там он уже есть.
- **Осталось:** ничего по этой правке.

## 2026-08-20 · OpenRouter четвёртым провайдером саммари

- **Что:** `RecapProviderKind.openrouter` + ключ в Keychain
  (`openrouter_api_key`) и имя модели (`recapOpenRouterModel`, дефолт
  `anthropic/claude-sonnet-4.5`). Своей функции вызова нет: OpenRouter говорит
  на протоколе OpenAI, поэтому `callOpenAI` получил параметр `baseURL` и два
  заголовка атрибуции (`X-Title`, `HTTP-Referer`) на openrouter-адресе.
  Тронуто: `RecapService.swift` (enum, `resolveBackend` + 4 диспатча,
  `RecapPreferences`), `Preferences.swift`, `SettingsPane.swift` (пара строк
  «ключ + модель» у выбранного провайдера, `modelRow` получил `subtitle`),
  `AppState.needsLocalRecapModel`, `ModelProvisioning.usesLocalModel`,
  `NoDeadEndsTests`, `docs/SPEC.md`, `docs/ARCHITECTURE.md`, `CLAUDE.md`.
- **Зачем:** запрос от коллеги — саммари не только Клодом и OpenAI. Отдельным
  вариантом enum, а не скрытым полем «свой base URL» у OpenAI: провайдер это
  ось телеметрии (`recap_provider`), заголовок настроек и ответ на вопрос «куда
  уехала эта встреча» — спрятанный адрес сделал бы все три ответа неправдой.
- **Ловушка:** `ModelProvisioning.usesLocalModel` для незнакомой строки
  возвращает `true` (`ModelProvisioning.swift:74`) — без правки этой строки
  каждый пользователь OpenRouter качал бы 3,4 ГБ qwen впустую.
- **Решение владельца:** нарезки длинной встречи у облачного пути нет и не
  будет (`TranscriptChunking.needed(backend:)` — только `ollama`). У OpenRouter
  имя модели свободный текст, значит модель с окном 8k упадёт на длинной
  встрече в HTTP 400. «Опенроутер для пауэр-юзеров, они несут ответственность
  сами» — записано в `docs/SPEC.md` и в комментарии у `openRouterChatURL`.
- **Проверено:** `swift test` — **997 тестов, 0 падений, 5 пропущено**.
  `./build.sh` → `/Applications/Propeller.app`, подпись валидна. Формат провода
  проверен без ключа: `POST openrouter.ai/api/v1/chat/completions` с фейковым
  ключом правильной формы → `401 {"error":{"message":"User not found."}}`, то
  есть адрес, путь, заголовок и тело приняты.
- **Осталось:** живой запрос с настоящим ключом не делал — ключа нет.
  Настройки глазами не видел: `osascript` не имеет прав Accessibility в этой
  сессии, открыть «Нейросети» и снять окно нечем. Пара строк собрана из тех же
  `keyRow`/`modelRow`, что у OpenAI и Claude, но это довод, а не скриншот.

## 2026-08-20 · Базлайн бенчей переснят: гейт снова говорит правду

- **Что:** `benchmarks/baseline.json` ← `latest.json` на `d52ebb7`. Перед этим
  прогнаны **оба** харнесса: `K=3 measure-batch.sh` и `K=3 measure-live.sh`.
  Довод записан в `benchmarks/report-gate.md`, раздел «Базлайн переснят
  2026-08-20». `report.md` и `report-ru-short-2spk.md` перегенерированы,
  `VERDICT: PASS`.
- **Зачем:** решение владельца принять обмен «+74 МБ транзиентного пика за
  −67 % времени диаризации» (кандидат — пин FluidAudio 0.13.6 → 0.15.5).
  Красный гейт, который никто не чинит и не объясняет, учит игнорировать все
  красные.
- **Ловушка, из-за которой пришлось гонять и живой слой:** `writeMetrics`
  (`Bench/Harness.swift:136`) правит `latest.json` **по ключам** — батч-прогон
  переписывает `batch.*`/`asr.*`/`diarize.*`, оставляет `live.*` от прошлого
  раза, но штампует весь файл нынешним коммитом. После одного
  `measure-batch.sh` файл выглядит однородным, а половина ключей в нём
  девятидневной давности; копирование такого файла в базлайн завело бы гейт на
  числах, которых никто не мерил.
- **Проверено:** `tools/bench-diff` после копирования — `VERDICT: PASS`, все 15
  метрик 0,0 %. Побочно: `asr.cpu_cores` впервые получил сторону базлайна (до
  этого `skip — missing side`, метрика была вне охраны). И замер живого слоя дал
  прямое подтверждение старому доводу: `live.app_cpu_cores` 0,00740 → 0,01025 на
  одном и том же коде, разброс 39 % при допуске +20 %.
- **Осталось:** допуск `live.app_cpu_cores` так и лежит внутри своего шума —
  сделать его абсолютным (+0,004) это отдельное решение, не тронул. Фикстура
  `ru-pauses-2spk` не переснята, её `FAIL` на месте. Причина +74 МБ —
  предположение по трём согласующимся показаниям, а не замер: подтверждается
  только откатом пина FluidAudio.

## 2026-08-20 · Красный `batch.peak_rss_mb` пересняли: он настоящий, и виноват не микс

- **Что:** прогон `K=3 tools/measure-batch.sh` на `caf5f9f`, перезаписаны
  `benchmarks/report.md` и `latest.json`; разбор в
  `benchmarks/report-gate.md` — новый раздел «Пересняли 2026-08-20».
- **Зачем:** гейт стоял красным с 11 августа, и в `report-gate.md` было
  написано, что его просто не пересняли. Я на это купился и вчера назвал
  красное устаревшим — **неверно**. Пересняли: **185,97 против 112,03, +66 %**
  при допуске +10 %, прогоны 186,0 и 185,0, то есть не шум.
- **Проверено:** `batch.rss_after_release_mb` 83,4 → 84,8 (+1,7 %) — удержанная
  память не выросла, значит +74 МБ транзиентные. `diarize.rtf` за то же окно
  −66,7 %. В том же окне пин FluidAudio 0.13.6 → 0.15.5 (`Package.resolved`,
  базлайн на `40ae070`). Три согласующихся показания на «новый набор моделей
  диаризатора: быстрее и толще». Микс исключён: `Bench` `produceFinalMix` не
  зовёт вовсе.
- **Осталось:** решение владельца — принять обмен и переснять базлайн (с
  причиной в `benchmarks/`, как требует правило) либо подтвердить причину
  откатом пина на 0.13.6. Откат недешёвый: при обратном подъёме заново
  проверять `prepareModels` (макось-14 краш). Сам базлайн не пересниму —
  это и есть «тихо расширить допуск».

## 2026-08-20 · Где пропадает владелец: замер вместо гипотезы

- **Что:** новая фикстура `swift/Tests/Fixtures/ru-echo-loud-2spk` (+ README) и
  генератор `tools/generate-fixture-ru-echo-loud.sh` — та самая, которую
  `ru-short-2spk/README.md` записал в долг: дальняя сторона в микрофонном стеме
  **выше** владельца, два расклада (речь в свои слоты / речь поверх чужой) × два
  пути эха (масштабированная копия / комната с задержкой, фильтром и поджатием).
  Драйвер замера `tools/echo-probe/owner-loss.py`. Отчёт с числами и оговорками —
  `swift/benchmarks/report-owner-loss.md`. Код приложения не тронут.
- **Зачем:** внешний тестировщик сообщил, что в расшифровке нет ни одного его
  слова, и стал недоступен; проверять пришлось самим. Аудит архива (70 записей)
  показал, что это не его машина: 2 встречи без единого слова владельца и ещё 6,
  где его меньше 6 % слов. По архиву «молчал» от «потерян» не отличить — из
  захвата в записи не сохраняется ничего, кроме `micOnlyCaptured`.
- **Проверено:** `python3 tools/echo-probe/owner-loss.py` на движке приложения
  (`gigastt e2e_rnnt` из бандла). Речь в свои слоты — 31/31 слова владельца при
  эхе от 0 до +20 dB. Речь поверх звучащей дальней стороны — **2/31** от +5 dB и
  14/31 даже при равных уровнях, то есть теряется в ASR, до всякого снятия эха.
  NLMS с системным стемом как эталоном (`aec2.py`) даёт ERLE +9.6 dB и
  возвращает **31/31**; на чистом случае звук владельца не тронут (+0.00 dB),
  recall 31/31, а эхо в микрофоне падает с 39/40 до 15/40 распознанных слов.
- **Поправка того же дня:** владелец ответил, что на всех восьми встречах он
  молчал. Значит архив потерю **не подтверждает**, частота дефекта не измерена, а
  улика у нас одна — замер на фикстуре. Зато случай «владелец молчит» из
  подозрительного стал нормальным, и его надо держать в уме: `ownerZoomName` без
  микрофонных реплик отдаёт `nil`, и тогда чужой реплике нечем помешать взять
  zoom-имя владельца.
- **Осталось:** ничего не починено — это замер. Подтвердить на живой записи
  (нужна догфуд-встреча с хранением «Всегда»: стемы удаляются сразу после
  расшифровки). Не проверены две опасные конфигурации устройств
  (выход с входными каналами; вход == выход) — ступени 5–6 старой `tap-probe`
  не состоялись, устройство переключить программно не удалось. Не сделаны
  видимые сигналы: `micCaptureWarning` по-прежнему без потребителя,
  `capturedUsableStem` смотрит только системный стем, здоровье захвата в
  `recordings.json` не пишется.

## 2026-08-20 · Блок 3, M1: микс стал потоковым — 560 МиБ пика превратились в 14

- **Что:** новый `PropellerPure/MixPlan.swift` — план блоков (`blocks`), сложение
  с клампом (`sum`) и потоковый накопитель уровня (`Level`), плюс
  `MixPlanTests` (7 тестов). `AudioRecorder.mix` переписан на два прохода
  блоками по 64 Ki кадров: первый собирает RMS и пик для `MixGain`, второй
  микширует и пишет. Прежний цельнобуферный путь оставлен как
  `mixWholeBuffer` — для стемов не 16 кГц моно, то есть архива старше того
  решения. `STATE.md` §1: `~~M1 / mix-ram~~` закрыт, строка «Здоров, когда»
  подрезана.
- **Зачем:** дефект M1. `readAndResample` читал каждый стем одним буфером на
  `file.length`, плюс третий буфер под сумму — и платилось это на **каждой**
  встрече, а на запуске ещё раз в цикле по всем записям без финала.
- **Проверено, тремя разными способами:**
  1. `MixPlanTests` — потоковая сумма против прежнего цельнобуферного цикла,
     транскрибированного в тест дословно: 11 синтетических форм × 8 размеров
     блока, и обе коммитнутые фикстуры целиком при сдвигах 0 и 0,484 с —
     сэмпл в сэмпл.
  2. Черновой харнесс (`scratchpad/MixCheck.swift`, в репозиторий не идёт):
     оба алгоритма поверх одних стемов, сравнение файлов побайтово. **IDENTICAL**
     на обеих фикстурах при обоих сдвигах и на синтетической 50-минутной паре
     (48 000 000 кадров).
  3. Тот же харнесс под `/usr/bin/time -l` на 50 минутах: пик RSS
     **587 661 312 → 14 680 064 байт (560,4 → 14,0 МиБ, в 40 раз)**, время
     **1,22 → 0,23 с**. Замер, а не расчёт по коду.
  `swift test` — **991, 5 пропущено, 0 падений**. `./build.sh` →
  `/Applications/Propeller.app`, приложение перезапущено (PID 78635), свежих
  `.ips` нет.
- **Осталось:** живой встречи на новом коде ещё не было — путь исполняемой цели
  тестам недоступен, поэтому побайтовое совпадение и есть главный гейт, но
  первую запись после этой правки стоит проверить глазами (длительность в
  списке, звук в файле). Бенчмарка, который показал бы выигрыш по памяти, в
  проекте нет: `Bench` микс не зовёт вовсе — отдельный режим харнесса остаётся
  должен.

## 2026-08-20 · Блок 3, часть первая: M4 закрыт замером, M3 — отчётом владельца

- **Что:** комментарий у цикла сложения в `AudioRecorder.mix` переписан — он
  обещал soft clamp, а код рубит насмерть; теперь там сказано, что делает код,
  замер и предел этого замера. В `STATE.md` §1 закрыты `~~M4~~` (замером) и
  `~~M3~~` (отчётом владельца: смена устройства ввода посреди встречи делалась
  не раз, звук подхватывается и пишется корректно — наблюдение в поле, не
  прибор). Кривую суммирования **не менял**.
- **Зачем:** менять кривую без числа — ровно та ошибка, от которой предупреждает
  CLAUDE.md («зелёный стенд не значит работающая фича»). Число получено: на
  обеих коммитнутых фикстурах, с воспроизведением `MixGain` и цикла сэмпл в
  сэмпл, `ru-short-2spk` даёт пик 0,787 и **0** обрезанных, `ru-pauses-2spk` —
  пик 1,117 и **7 из 869 000** (0,0008 %). Четыре десятых миллисекунды на 54
  секунды: жёсткий кламп не стоит ничего, мягкий ничего не купит.
- **Проверено:** `swift build` чисто, `swift test` — **984, 5 пропущено, 0
  падений**. Правка только в комментарии, поэтому установку не гонял.
  Замер по живому архиву был разрешён владельцем и **оказался невозможен**:
  retention по умолчанию `afterTranscript`, и на диске не осталось ни одной пары
  mic/sys — три mic-only стема от 27 июля и всё. Встреча `20260820_140110`
  (3041 с), стемы которой я видел двумя часами раньше, лежит в индексе как
  `summarized` с транскриптом — то есть звук выметен ровно по правилу, данные
  целы. Настоящее число стоит одной встречи через динамики с retention «Всегда».
- **Осталось:** M1 (микс держит оба стема в RAM: 194,7 МБ на стем, пик ~584 МБ
  на 50 минутах, и то же в цикле на запуске в `recoverMissingFinalMixes`).
  Перед правкой запущен агент на карту blast radius — переписывать путь, который
  пишет аудио, без списка потребителей финального wav я не буду.

## 2026-08-20 · Календарь молчал из-за entitlement, которого нет в подписи

- **Что:** `build.sh` генерит два файла entitlements — приложению добавлен
  `com.apple.security.personal-information.calendars`, ASR-бинарь остаётся с одним
  `audio-input`; строка `.ask` в `PropellerPure/CalendarAccess.swift` больше не
  называет причиной обновление («Доступ не выдан»), а её доккоммент — измеренную
  причину вместо прежней догадки про cdhash.
- **Зачем:** кнопка «Разрешить» не показывала окна ни разу. Зонд в живой сборке
  показал `requestFullAccessToEvents()` → `false`, статус `notDetermined`, без
  исключения; `log stream` по tccd назвал причину дословно: *«Prompting policy for
  hardened runtime; service: kTCCServiceCalendar requires entitlement
  com.apple.security.personal-information.calendars but it is missing»*. Hardened
  runtime включён 15 августа ради нотаризации (4a6fa87) — с того дня календарь
  был отключён у всех, и молча. Мой прежний диагноз (расхождение csreq по cdhash)
  неверен: строка TCC ни при чём.
- **Проверено:** `swift test` — 984 теста, 0 падений, 5 пропущено. `./build.sh` —
  entitlement в подписи (`codesign -d --entitlements`), приложение установлено.
  Живой прогон: запрос вернул `true`, статус 3 (`fullAccess`), `events = 25`,
  первые названия — «Обед», «Вячеслав х Pragmatica…», «PG x VK Музыка // пречек».
  Строка в TCC легла под Developer ID requirement и **пережила** ещё одну
  пересборку с переустановкой (`last_modified` не изменился).
- **Осталось:** зонд из кода убран, но маркер `~/.meeting-recorder/.calendar-probe`
  удалить может только владелец — хук не пускает агента в каталог данных. Файл
  инертен: кода, который его читает, больше нет. Юзерам нужен релиз — хвост в
  `TAILS.md`.

## 2026-08-20 · Кнопка «Разрешить» получает окно, которое у неё отнимал запуск

- **Что:** `CalendarService.store` стал `var` и пересоздаётся в начале
  `enableAndLoad`; запрос доступа на запуске убран — `AppState` зовёт `load()`
  (`Sources/CalendarService.swift`, `Sources/AppState.swift`).
- **Зачем:** нажатие «Разрешить» в новой строке настроек не делало ничего —
  ни окна, ни строки в TCC. Окно разрешения показывается один раз на
  `EKEventStore`: запрос на запуске тратил единственную попытку процесса, и тот
  же store дальше отвечал отказом из кэша, молча. Заодно уходит окно без повода
  на экране — спрашивают там, где видно зачем: подошва рельса и строка настроек.
- **Проверено:** `swift test` — 979 тестов, 0 падений, 5 пропущено. `./build.sh`
  — valid on disk, установлено. Запуск новой сборки: строки `kTCCServiceCalendar`
  в TCC.db не появилось (запроса на запуске больше нет), микрофон и аудио на
  месте. **Не проверено:** само окно — его вызывает нажатие человеком.
- **Осталось:** одно нажатие «Разрешить» и клик в окне macOS; после — проверить
  строку в TCC (должна лечь под Developer ID requirement, а не cdhash) и что
  названия встреч снова приезжают из календаря.

## 2026-08-20 · У аудио два режима: до расшифровки или тридцать дней

- **Что:** `keep` и выбор числа дней удалены из `AudioRetentionMode`; срок стал
  константой `AudioRetention.days`, параметр `days:` у `expired` и ключ
  `audioRetentionDays` — тоже. Строка «Хранить аудио» — сегментед из двух
  пунктов («До расшифровки» / «30 дней») без подписи под ней; степпер и
  `retentionHelp` ушли (`SettingsPane.swift`). `AudioRetention.storedMode`
  делает **одноразовый сброс** записанного (флаг `audioRetentionReset`), геттер
  `Preferences.audioRetentionMode` его исполняет. Девять тестов в
  `AudioRetentionTests`, `STATE.md` и CHANGELOG переписаны под два режима.
- **Зачем:** в настройках стояло «Столько дней · 30», хотя код с 18 августа
  живёт по `afterTranscript`: дефолт применяется только там, где ключа нет, а на
  этой машине лежал ответ, данный 17 августа — в день, когда дефолтом был
  `afterDays`. Решение владельца 2026-08-20: чинить не миграцией трёх режимов, а
  сокращением до двух — «хранить всегда» и выбор числа дней существовали для
  случая, который никто не назвал. Сброс вместо разбора: retention нигде не
  выпущен (1.16.5 — 16 августа), записанное в те два дня — шум dogfood-окна.
- **Проверено:** `swift test` — 996 тестов, 0 падений, 5 пропущено. `./build.sh`
  — подписано, valid on disk, установлено. Живой запуск: `audioRetentionMode` из
  defaults исчез, `audioRetentionReset = 1`, то есть сброс сработал в приложении,
  а не только в тесте. Сегментед снят в PNG временным шотом на настоящем ките и
  настоящих `displayName` (файл удалён): два пункта в одну строку, не колбаса.
  **Не проверено:** сама строка настроек в живом окне — Accessibility у шелла
  нет, синтетический клик по «Настройки» недоступен.
- **Осталось:** ничего. Аудио на диске сейчас 39 МБ — три стема от 27 июля, не
  видимых индексу; заявленные утром 3,32 ГБ ушли раньше, чем я замерил, момент
  назвать не могу. Кому нужен звук локально — жмёт «30 дней», один клик.

## 2026-08-20 · Календарь признаётся в настройках, имя правится там же

- **Что:** новый `PropellerPure/CalendarAccess.swift` — `CalendarAccess`
  (три ответа системы: не спрашивали / выдан / отказано) и `CalendarSettingsRow`
  (что говорит и что предлагает строка). `CalendarService` публикует `access` и
  умеет `refreshAccess()` без запроса. В «Основное» две новых строки —
  `CalendarAccessRow` и поле «Ваше имя в расшифровках» (`SettingsPane`). Ключ
  `setupNameAsked` (`Preferences`), `SetupPromptMachine.step` получил `nameAsked`,
  `AppState` — `enableCalendarFromSettings()`, `setOwnerNameFromSettings()` и
  миграцию флага для тех, у кого имя уже записано.
- **Зачем:** доступ к календарю слетел от смены подписи сборки 2026-08-15
  (Developer ID вместо самоподписанного «MeetingRecorder Dev»): TCC-строка
  осталась про прежнюю личность приложения, и с 08-17 названия встреч молча
  придумывала модель — в System Settings при этом стояла галка. Фича отвалилась
  без единого свидетеля; строка и есть свидетель, и она же чинит: `notDetermined`
  спрашивает сама, `denied` ведёт в System Settings, потому что после отказа
  окна не будет. Поле имени — вторая половина: до сих пор имя спрашивали только
  в рельсе, и передумать было негде. `setupNameAsked` разводит «ответил» и
  «сейчас пусто», иначе стёртое в настройках имя возвращало бы вопрос в подошву.
  Прошлых встреч правка не касается: имя запекается в текст на остановке
  (`persistLiveTranscript`) и на расшифровке (`TranscriptionService`), а всё, что
  уже на диске, переименование не читает.
- **Проверено:** `swift test` — **974, 5 пропущено, 0 падений** (было 966 с
  чужими правками в дереве; +9 моих: три про стёртое имя в `SetupPromptTests`,
  шесть в новом `CalendarAccessTests`). `./build.sh` зелёный, приложение
  переустановлено и перезапущено (pid 64588). Четыре состояния строки и поле
  имени отрисованы через `NSHostingView` в PNG и просмотрены глазами — текст,
  кнопки, галочка на месте. Живую панель настроек в приложении **не смотрел**:
  инъекция клавиш и клика запрещена TCC для osascript, а разрешение на это я не
  беру.
- **Не сделано:** строка календаря не заведена в галерею состояний
  (`UIStateCatalog` её не требует — тесты зелёные). Имя для встречи, записанной
  но ещё не расшифрованной, берётся текущее — снапшот в `RecordingEntry`
  сознательно не делали (поле в модели плюс миграция ради редкого случая).

## 2026-08-20 · Настройки: приватность вместо аналитики, «Нейросети» в порядке работы

- **Что:** группа «Аналитика» → **«Приватность»**, строка «Отправлять анонимную
  телеметрию / Только события приложения — без личных данных» →
  **«Делиться аналитикой» / «Только обезличенные данные»** (структура
  `AnalyticsSettingsGroup` → `PrivacySettingsGroup`). В «Нейросетях» порядок
  стал модель → промпт → словарь, словарь переписан в **«Личный словарь» /
  «Сленг и англицизмы для распознавания»**. `Tokens.Settings.editorHeight`
  400 → **260**. Догнаны `design/settings.md` (строки переименованы и
  переставлены в порядок экрана), `docs/SPEC.md` §Настройки и фикстура
  скриншотера `TempSettingsShotTests` — она рисует ту же группу вручную и
  без правки врала бы на каждом снимке.
- **Зачем:** правки владельца. «Приватность» отвечает на вопрос, с которым в
  раздел приходят, а не называет механизм; в «Нейросетях» первым идёт то, что
  меняют чаще (модель), последним — то, что правят раз под проект (словарь), и
  он единственный в группе про распознавание, а не про саммари.
- **Проверено:** снимок кита — `SETTINGS_SHOT` рендер, посмотрел глазами:
  порядок, обе новые пары строк на месте, высота поля промпта на картинке
  259 pt при заданных 260. `swift test` — **979, 5 пропущено, 0 падений**.
  `./build.sh` → `/Applications/Propeller.app`, приложение перезапущено
  (PID 66378), свежих `.ips` нет. **Живую панель снять не удалось:**
  синтетическое ⌘, через System Events запрещено (нет прав Accessibility,
  ошибка 1002) — проверен рендер кита, а строки в самой панели те же и
  правились одной правкой.
- **Осталось:** ничего по этим четырём правкам. В репозитории по-прежнему
  работает вторая сессия (retention-дефолт): `STATE.md`, `WORKLOG.md`,
  `AudioRetention`, `Preferences` — их; `SettingsPane.swift` снова общий,
  застейджены только свои ханки.

## 2026-08-20 · Блок 2, шаги 2.3 и 2.4: правила из исполняемой цели, и мёртвая ниточка

- **Что:** три новых файла в `PropellerPure` — `MeetingTitle` (правило «это
  название придумало приложение», плюс сама приставка), `TelemetryBuckets`
  (границы корзин воронки), `RecapDocument` (`userMessage` — что видит модель,
  `wrapped` — что получает человек). `AudioRetentionMode` получил `displayName`
  рядом с кейсами. Из `Sources` ушли `isAutoGeneratedTitle`, инлайновая копия
  того же правила в `RecordingStore`, тела корзин в `Analytics`,
  `retentionTitle` в `SettingsPane`, `buildUserMessage` и `wrapRecapDocument`
  в `RecapService`. Плюс вырезана мёртвая ниточка аргументов: `speakers` и
  `duration` шли через `runRecap` → протокол `RecapBackend` → `generateRecap` и
  выбрасывались в обеих функциях первой строкой. 14 новых тестов
  (`MeetingTitleTests` 4, `TelemetryBucketsTests` 2, `RecapDocumentTests` 8).
  Две строки в `STATE.md`: `~~placeholder-title-copies~~` §5,
  `~~recap-dead-args~~` §6.
- **Зачем:** одно и то же спрашивалось в трёх местах, и совпадение слов держало
  фичу: смени формулировку заглушки — и встречи молча перестают получать
  названия от модели. Границы корзин — смысл каждого графика телеметрии, а
  ошибка в них не видна в графике. Мёртвые аргументы стоили не только чтения:
  один из вызовов ради них гонял `extractSpeakers` регуляркой по всему
  транскрипту.
- **Проверено:** `swift test` — **974, 5 пропущено, 0 падений**; из них моих
  новых 36 (`Timecode` 7, `MeetingMarkdown` 15, `MeetingTitle` 4,
  `TelemetryBuckets` 2, `RecapDocument` 8), базлайн блока был 927.
  `./build.sh` → `/Applications/Propeller.app`, приложение перезапущено
  (PID 63755), свежих `.ips` нет.
- **Важная оговорка:** в этом заходе в репозитории работала **параллельная
  сессия** (онбординг: `CalendarAccess`, `SetupPrompt`, поле имени в
  настройках). `Sources/AppState.swift` и `Sources/SettingsPane.swift` правили
  оба, поэтому я застейджил **только свои ханки** (`git apply --cached` по
  отфильтрованному патчу, 7 и 2 ханка), чужое осталось в рабочем дереве
  незастейдженным. Из-за этого сборка и тесты прогонялись на **смешанном**
  дереве, и в 974 входит около десятка чужих тестов — свои посчитаны отдельно.
- **Осталось:** `resolveBackend` и `throwIfBadHTTP` не двигал: они держатся за
  `RecapProviderKind`, `RecapSkipReason` и `RecapError`, которые живут в
  `Sources`; переезд этих типов — свой шаг, не хвост этого.
  `encodeASRSegments` (3 строки JSON) и `meansNobodySpoke` (разбор ошибок
  сайдкара) оставлены сознательно: первое ничего не решает, второе завязано на
  типы ошибок из `Sources`.

## 2026-08-20 · Блок 2, шаг 2.2: рендер файла встречи стал проверяемым

- **Что:** 253 строки из `Sources/MarkdownWriter.swift` (оба формата, тело
  транскрипта, `extractSpeakers`, `speakerSlug`, `slugify`, `MarkdownOutputFormat`)
  переехали в `PropellerPure/MeetingMarkdown.swift`. В `Sources` осталась
  оболочка мира: путь файла, снос старого слага, `Date()`, обход папки людей —
  345 → 151 строка. `today` и `linkedSpeakers` стали аргументами. Восемь вызовов
  `MarkdownWriter.slugify/extractSpeakers` переписаны на `MeetingMarkdown.`.
  Новый `MeetingMarkdownTests` (15 тестов). `STATE.md` §5: файл в список,
  дата­рованный абзац про переезд.
- **Зачем:** приложение существует ради этого файла, а рендерил его код в
  исполняемой цели, куда тест не дотягивается, — покрытия было ноль.
  Заодно `extractSpeakers` перестал терять реплики с меткой за первым часом:
  у него была **седьмая** копия шаблона, ещё более узкая (`\[\d+:\d+\]` без
  третьей части), теперь общий `Timecode.transcriptHeadPattern`.
- **Проверено:** переезд сверен **байт-в-байт на трёх настоящих встречах** из
  живого архива — разовый тест читал `recordings.json` и сохранённые `.md`,
  `rendered == expected` на всех трёх; тест удалён и в репозиторий не попал
  (чужие встречи — не фикстура). `swift test` — **951, 5 пропущено, 0 падений**
  (было 936). `swift build` чисто; `-DGALLERY` падает той же чужой ошибкой,
  что и до правок. `./build.sh` → `/Applications/Propeller.app`, приложение
  перезапущено (PID 60675), свежих `.ips` нет.
- **Осталось:** шаги 2.3 (мелкие чистые функции из `AppState`/`Analytics`/
  `SettingsPane`) и 2.4 (решения саммари из `RecapService`). Два ожидания в
  тестах я сначала написал неверно и поправил по факту — в них теперь замер, а
  не догадка: `slugify` роняет краткую над `й` (`наиму`), и тело реплики
  остаётся под шапкой.

## 2026-08-20 · Блок 2 ревизии, шаг 2.1: одна метка времени вместо шести

- **Что:** новый `PropellerPure/Timecode.swift` — `text` (для человека),
  `minutesSeconds` (форма транскрипта на диске), `seconds` (обратно из любой
  формы) и `transcriptHeadPattern` одной константой. Шесть форматтеров стали
  однострочными обёртками (`TranscriptPresentation.formatTimestamp`,
  `MeetingNotes.timecode`, `TranscriptSlice.stamp`, `AppState.formatElapsed`,
  `GalleryFixture.stamp`, `TranscriptionService.formatTranscriptText`), две
  копии шаблона `[кто] [когда]` — одной. Новый `TimecodeTests` (7 тестов).
  Строка `~~timecode-copies~~` в `STATE.md` §5 переписана как закрытая; заодно
  §10 получила `gallery-build-broken`.
- **Зачем:** копии согласовывались случайно, и мина оказалась настоящая:
  на старом шаблоне реплика с трёхчастной меткой теряет и имя, и время —
  проверил, временно вернув старую регулярку, тест дал `["Иван", ""]` и `00:00`
  вместо `1:30:20`. Обе формы оставлены намеренно: живой архив
  (250 файлов, 6709 меток в индексе, минуты до **99**) целиком двухчастный,
  так что перевод писателя на `H:MM:SS` создал бы смешанный архив без миграции.
  Решение владельца по этому вопросу не пришло — иду по варианту (б),
  который и рекомендовал; переиграть стоит один коммит.
- **Проверено:** `swift test` — **936, 5 пропущено, 0 падений** (было 929).
  Тест-на-мину падает на старом коде и проходит на новом. `swift build` чисто;
  `swift build -Xswiftc -DGALLERY` падает **и до, и после** правок с одной и той
  же ошибкой (`git stash`-сверка), то есть галерея была сломана раньше.
  `./build.sh` → `/Applications/Propeller.app`; прежний экземпляр закрыт,
  новый поднялся (PID 59208), свежих `.ips` нет. Формы меток в реальном архиве
  посчитаны скриптом по `recordings.json`, не по памяти.
- **Осталось:** шаги 2.2–2.4 блока 2 (чистая половина `MarkdownWriter` в
  `PropellerPure` с байтовой сверкой рендера, мелкие чистые функции, решения
  саммари). Обёртки-однострочники оставлены сознательно — коллапс имён
  `formatTimestamp`/`stamp`/`timecode` в одно это отдельный, чисто
  косметический диф. `gallery-build-broken` — чужой дефект, не лечил.

## 2026-08-20 · Пять форматтеров одной метки времени записаны как дефект

- **Что:** строка `timecode-copies` в `STATE.md` §5 — пять форматтеров и четыре
  парсера одного таймкода, с картой мест и разбором того, чем именно они
  расходятся.
- **Зачем:** находка ревизии, которую нельзя было оставить в переписке: сегодня
  копии согласны случайно (производитель часов не пишет), и первый, кто
  «починит» `[90:12]` на `[1:30:12]`, молча потеряет метку спикера за первым
  часом. Дефект компонента — слой `STATE.md`, а не хвост.
- **Проверено:** прочитаны все пять форматтеров и все четыре парсера, каждый
  file:line в строке — из этого чтения. Кода не менял.
- **Осталось:** лечение — шаг 2.1 блока 2 ревизии (единый `Timecode`
  в `PropellerPure`).

## 2026-08-20 · Блок 1 ревизии: мёртвое из ядра вынесено, покрытие выросло раньше удаления

- **Что:** удалены таргеты `Experiments` (303 строки) и `SpeakerMatchingCoreChecks`
  (116) вместе с их записями в `Package.swift`; из `SpeakerMatchingCore.swift`
  вырезаны `SourceAwareScoring` + `SourceAwareScoringConfig` (50),
  `AudioEnergySummary` + оба `summarize` (80) и `AudioCaptureSource.label` (8) —
  файл 337 → 197. Перед удалением пять утверждений про живой код
  (`classify` по `dominanceRatio`, имена стемов) переехали из самодельного
  харнесса в `Tests/.../SystemWindowShiftTests.swift`. Строка о закрытии —
  `STATE.md` §3, `~~smc-voicelib~~`.
- **Зачем:** `SourceAwareScoring` правил косинусную близость под библиотеку
  голосов, которой в продукте нет; оба харнесса не звались ниоткуда, кроме
  замороженного `archive/`, а `Experiments` вдобавок читал
  `~/.meeting-recorder/test/test-5min.wav`, которого на машине нет. Удалять
  харнесс, который один проверял живой `classify`, без переноса — значило бы
  разменять 550 мёртвых строк на дырку в покрытии.
- **Проверено:** `swift test` — **929, 5 пропущено, 0 падений** (было 927;
  в блоке 0 я назвал базлайн 926 — ошибка чтения, счёт стабилен на трёх
  прогонах). `swift build` — все оставшиеся таргеты линкуются.
  `./build.sh` → `/Applications/Propeller.app`, подпись `valid on disk`;
  прежний экземпляр закрыт, новый поднялся (PID 56100, аптайм 49 с),
  свежих `.ips` нет.
- **Осталось:** блок 1 закрыт. `SourceAwareSpeaker` в `PropellerPure` —
  другое и живое, не тронуто (совпадение имён проверено). Планы блока 2
  (единый таймкод, чистая половина `MarkdownWriter`) ждут решения владельца
  про формат метки на диске.

## 2026-08-20 · Контрольная трасса и залипшая запись TCC

- **Что:** снята третья трасса §11.1 (Zoom без встречи): 132 опроса, ноль
  плиток, журнал пуст (`noTiles`) — отрицательный случай подтверждён живьём.
  В план §6 вписана ловушка TCC с лечением (`tccutil reset Accessibility …`):
  тумблер включён, `AXIsProcessTrusted()` false после пересборки бандла под
  работающим процессом + смены entitlements в тот же день.
- **Зачем:** последний недоснятый вход §11.1; ловушка воспроизведётся у
  тестировщиков после обновлений с новой подписью.
- **Проверено:** `CallWindowJournalLab` по трассе — 0 пролётов; право у
  владельца после сброса встало (галочка живая).
- **Осталось:** первый боевой журнал — встречи владельца сегодня; гейт
  полноты recap-lab по их лентам.

## 2026-08-20 · Саммари-часть чек-листа: состав в промпте, чужие имена ловятся

- **Что:** `RecapDocument.participants(fromTranscript:)` (состав из меток ленты,
  плейсхолдеры не люди) + строка «Участники» с правилом в `userMessage`;
  `RecapLint.assigneeOutsideRoster` — исполнитель-имя вне состава как находка
  для редактора (не удаление — урок `ghostOwnerHead`), совпадение по токенам
  с общим префиксом ≥4 (падежи). Подключено в `RecapService` (одиночный путь
  и редактура; путь нарезки — не тронут, свод идёт через ту же редактуру).
- **Зачем:** с настоящими именами регексы «Speaker N» слепнут; жёсткий фильтр
  по составу — требование plan-people.md §6, вписан в чек-лист шага 4 ревизией.
- **Проверено:** `swift test` — 997, 5 пропущено, 0 падений (6 новых тестов).
- **Осталось:** стендовый гейт полноты recap-lab на именованных лентах;
  кросс-скрипт «Арина/Arina» ловится как находка намеренно — редактор
  нормализует написание по списку.

## 2026-08-20 · Шаг 4: журнал в приложении — слои до подключения к записи

- **Что:** Г0 закрыт решением владельца; три коммита шага 4: чистый слой
  (`SpeakerAttribution.callWindow` + disclosure, `ownerZoomName`/`remoteLabel`,
  `MeetingTrace.callWindowTrace` — стирание видит трассу), оболочка и шов
  (`Sources/CallWindowObserver.swift`, чтение `<id>.calltrace.jsonl` в
  `TranscriptionService.assembleFromStems`: журнал выше диаризации, только для
  чужих реплик, телеметрия `CallJournal.applied`), двери разрешения
  (плита онбординга: строка «Доступ к приложениям» выше пушей, пуши «Только
  важное», высота 526→582; та же дверь в настройках, «Приватность»).
- **Зачем:** чек-лист §11.1 после гейта; двери — чтобы фича была достижима и
  для уже онбордившихся.
- **Проверено:** `swift build` чисто; `swift test` — 984, 5 пропущено,
  0 падений. Живьём наблюдатель не гонялся: он ещё не подключён к записи.
- **Осталось:** ~~подключение в AppState~~ (a6845b7, зелено в чистом worktree:
  979/5/0), ~~`./build.sh`~~ (установлен из чистого worktree, подпись 15:33,
  строки и `calltrace` в бинарнике проверены raw-grep). Ещё нет: саммари-часть
  чек-листа (состав в промпт, фильтр исполнителей), гейт полноты recap-lab,
  догфуд владельца, вопрос обязательности строки онбординга.

## 2026-08-20 · Вторая живая трасса: шер экрана методу не мешает, мьют замерен

- **Что:** разбор трассы встречи с демонстрацией (`20260820_140110`): Н5 закрыт
  (678 опросов подряд — три плитки полосой 160×80, метка без пропусков, решение
  назвало 299 с из 300 при нуле молчания), Н8 сужен (строка «Звук компьютера
  выключен» → дефолт `Tuning.mutedMarkers`, +тест), метка ни разу не стояла на
  заглушённом (0/678). Правки плана §2/§8, DIARIZATION.md, TAILS.
- **Зачем:** Н5 был последним замером до гейта Г0 и главным риском метода.
- **Проверено:** `swift test` — 980, 5 пропущено, 0 падений; числа — прогоном
  `CallWindowJournalLab` по трассе.
- **Осталось:** контрольная трасса «Zoom без встречи»; решение владельца по Г0;
  Н9/Н10 — телеметрией и будущими трассами.

## 2026-08-20 · Ревизия свежими глазами отработана: линейка стала честной

- **Что:** по 20 находкам Opus-ревизии — код: `Silence`-причины молчания вместо
  голого nil, конец пролёта +полшага, `startedUnix` в метазаписи трассы,
  `--names` (объявленное сопоставление) в `saer-journal.py`; докам: чек-лист
  шага 4 в плане (7 пунктов с file:line), Н9–Н11, решение владельца «AX-запрос —
  в онбординг», пересчёт чисел в DIARIZATION.md; TAILS: два новых хвоста.
- **Зачем:** прежний «SAER 0 %» не проверял ось «имя верное» и прятал границы
  реплик в «молчание»; честные числа: **9,4 % названного (подмен людей 0 с,
  вся ошибка — правило середины на репликах через смену говорящего), покрытие
  88,5 %, сквозной 19,8 %**, плато чувствительности к сдвигу 23–26 с.
- **Проверено:** `swift test` — 927, 5 пропущено, 0 падений; пересчёт SAER
  и свип сдвига — команды и числа в DIARIZATION.md; make-app.sh собирает.
- **Осталось:** хвосты Н8–Н11 в TAILS (замеры на живых встречах); шаг 4 — по
  обновлённому чек-листу после Г0.

## 2026-08-20 · Первый SAER журнала: 0 % на названном против 49,6 % у диаризации

- **Что:** эталон из разметки владельца (`mkref/20260820_100315.py`, 23 реплики,
  136 с, 8 спорных исключены), счёт `saer-journal.py --offset 24.2`.
- **Зачем:** число, решающее «выпускать ли» (п.2 релизного плана). Встреча —
  класс «слипание» (1 кластер на двух собеседниц): диаризация 49,6 %, журнал +
  фолбэк 21,3 % (вся ошибка — свёрнутое окно), названные секунды — 0,0 %.
- **Проверено:** сам счёт и есть замер; эталон **по памяти, не по прослушке** —
  0 % читать как «журнал и память не разошлись», оговорка в DIARIZATION.md.
- **Осталось:** Г0 формально (демонстрация экрана — следующий созвон), мьют (Н8),
  шаг 4 после гейта.

## 2026-08-20 · Всё для первого SAER готово, не хватает только разметки

- **Что:** стемы `20260820_100315` отложены в корпус; сдвиг трасса↔запись
  откалиброван по репликам владельца — **24,2 с** (не 26 по часам), согласие
  журнала с микрофонным стемом 94,6 % на 221 с; `--offset` в `saer-journal.py`;
  `utts/20260820_100315.sys.utts.json` (31 дальняя реплика, 152 с, только
  таймкоды); диаризация в `out/…base.json` — **один кластер на двух собеседниц**.
- **Зачем:** п.2 плана релиза — число, решающее «выпускать ли»; калибровка
  сдвига заодно первая независимая проверка журнала против второго сенсора.
- **Проверено:** `--offset` регрессией на синтетике (сдвиг+офсет = тот же счёт);
  пик калибровки острый (91 % → 94,6 % → 91 % при ±0,5 с).
- **Осталось:** разметка `~/diarize-lab-corpus/20260820_100315.markup-draft.txt`
  (владелец, 31 строка) → `mkref/` → счёт. Трассы демонстрации/контрольная — TAILS.

## 2026-08-20 · Первая живая трасса: у Zoom есть метка говорящего

- **Что:** разобрана трасса с рабочей встречи (галерея, трое, владелец — гость);
  решение переписано под метку `active speaker` (главный сигнал), площадь —
  запасной, «первое место» удалено; фикстура `synthetic-gallery-marker.jsonl`;
  правки plan-speaker-tags.md §1/§2/§4 и DIARIZATION.md H12.
- **Зачем:** метка — явный сигнал самого Zoom; «первое место» совпало с ней в
  0 из 626 опросов и подписало бы встречу чужими именами.
- **Проверено:** `swift test` — 926, 5 пропущено, 0 падений; журнал по живой
  трассе воспроизводит серии метки один в один (32 пролёта, 265 с из 300).
- **Осталось:** трассы «демонстрация» и «без встречи» (PRO-9), эталон к
  `20260820_100315` и SAER (PRO-12, сдвиг ≈ 26 с), мьют (Н8), Г0 (PRO-10).

## 2026-08-20 · Подготовка релиза 1.16.6: дыры в CHANGELOG и версия

- **Что:** дописал в `CHANGELOG.md` шесть пунктов, которых там не было (новый
  знак, цвет вместо жирности, ресайз окна, окно без пустой рамы, чистка лексикона
  телеметрии, `Ollama.setup{reason}` + `Recap.finished{first}`), бампнул версию в
  `meeting-recorder/swift/build.sh` (288, 290) 1.16.5 → 1.16.6, снял два хвоста в
  `TAILS.md`. Кода не касался.
- **Зачем:** правило «поменял поведение — строка в CHANGELOG в том же коммите»
  было нарушено шесть раз, и заметки к релизу пришлось бы восстанавливать по
  `git log`. Версия важнее: без бампа новые сигналы приехали бы на дашборд под
  номером старой сборки, и разрез по версии — единственное, что доказывает
  «стало ли хуже после релиза» — их не отделил бы.
- **Проверено:** `swift test` — 922 теста, 0 падений, 5 пропущено; `swift build`
  — Build complete. Разбор дерева: 48 коммитов после `v1.16.5`, ни одного merge,
  один автор, секретов в диффе нет.
- **Не сделано / известное:** `origin/main` отстаёт на 39 коммитов — не пушено.
  В публичном репозитории остаётся личный путь в тесте
  (`CodexConfigMergeTests.swift:35,50`), автор коммитов —
  `levonlobanov@MacBook-Pro.local`, GitHub их не привяжет к аккаунту. Числа
  тестов в `STATE.md` (615) и `NEXT-STEPS.md` (692/718) устарели.

## 2026-08-20 · Причина отказа движка и флаг первой выдачи конспекта

- **Что:** `Ollama.setup` получил `reason` — грубую корзину отказа
  (`OllamaSidecar.SidecarError.signalReason`: `binary`, `unpack`, `download`,
  `health`, `pull`, плюс `disk` на дисковом гварде, который до сих пор не
  считался вовсе и выглядел как «настройку не начинали»). `Recap.finished{ok}`
  получил `first` — первый ли это конспект для встречи
  (`AppState.isFirstRecap`, половина прежнего `isNews`). Файлы:
  `Analytics.swift`, `AppState.swift`, `OllamaSidecar.swift`.
- **Зачем:** движок саммари не встаёт у 18 человек из 41, и по одному `fail` без
  причины было не понять, что чинить. А доля встреч, дошедших до саммари, не
  считалась вовсе: `Recap.finished` уходит на каждой попытке (встреча без
  провайдера паркуется, лестница возвращается) и ещё раз при перегенерации —
  отсюда 395 `ok` на 395 записей при 19 людях без конспекта. Встречи теперь
  считаются как `Recap.finished{result=ok, first=1}`.
- **Проверено:** `swift test` — 922 теста, 5 пропущено, 0 падений. `./build.sh` →
  `/Applications/Propeller.app`, приложение запускается (pid живой). Сами
  параметры живьём **не проверены**: `reason` виден только на упавшей настройке,
  `first` — на сгенерированном конспекте; подтвердятся на дашборде после
  релиза. `strings` по бинарю для этого не годится — короткие строковые литералы
  Swift не попадают в таблицу строк.
- **Осталось:** версия в `build.sh` зашита как 1.16.5 (строки 288–290) — без
  бампа новые параметры приедут под тем же номером, и разрез «стало ли хуже
  после релиза» их не отделит. Правило «доля встреч = два `eventCount`» в
  `STATE.md` §12 всё ещё стоит и теперь неверно — правка в незакоммиченном файле
  другой сессии.

## 2026-08-19 · Ресайз окна: setFrameAutosaveName откатывал каждый тик драга

- **Что:** guard в `AppWindowRegistry.rememberFrame` — `setFrameAutosaveName`
  ставится один раз, а не на каждом layout-тике (`SceneWindowChrome.swift`).
  Коммит 2453d65.
- **Зачем:** `rememberFrame` зовётся из `ChromeHostView.layout()`, то есть на
  каждом тике живого ресайза, а `setFrameAutosaveName` перечитывает сохранённый
  кадр и применяет его — раньше, чем observer didResize записал новую ширину.
  Каждый тик драга откатывался к сохранённым 797×760; окно не ресайзилось вовсе.
- **Проверено:** событийная трасса (willStartLiveResize шёл, каждый выросший
  кадр тут же снапался назад), headless-воспроизведение программным
  `setFrame(+120)` — до guard'а снап, после guard'а 917 держится. Не мой коммит
  9b37559: ресайз мёртв и на 9b37559^ (проверено сборкой). `swift test` 922/922.
  Живой драг после фикса — ждёт подтверждения Левона.
- **Замечание:** когда именно сломалось — не установлено; layout-путь и
  `setFrameAutosaveName` в этом виде существуют давно, подозрение на изменение
  поведения AppKit в macOS 26 — не проверено.

## 2026-08-19 · Первая продуктовая аналитика: саммари не доезжает до половины людей

- **Что:** прогнал TQL живьём через MCP-коннектор `telemetrydeck` (окно 30 дней,
  Live) и поправил `STATE.md` §12 в шести местах — таблица пяти чисел с
  измеренными значениями, живое ожидание саммари рядом с лабораторным, доступ к
  Query API, доказанная привязка `defaultParameters` к `App.opened`, закрытая
  гипотеза про `Diarize.retryAlternateEngine` и снятое правило «доля встреч =
  два `eventCount` на `{result=ok}`» (считало выдачи, а не встречи; честный
  счёт — `{result=ok, first=1}` начиная с 1.16.6). Кода не касался.
- **Зачем:** продуктовой аналитики не проводили ни разу, и §12 содержал четыре
  утверждения, которые данные либо опровергли, либо доказали.
- **Проверено:** числа из запросов, а не из памяти. Ключевые: 19 из 41
  записавшего не получили саммари ни разу (`thetaSketchSetOp NOT`); пересечение
  `Ollama.setup.fail` × `Recap.finished{skip=no_provider}` — 18 человек
  (`INTERSECT`); `Summary.waited{awaited=1}` на 15–60 мин — медиана 279 с, p90
  3347 с против лабораторных 82 с; `Transcription.finished{reason=error}` 58 % на
  1.15 → 8 % на 1.16.5; `Capture.levelRate` — 24 726 из 29 180 событий месяца
  (85 %); `Recording.cancelled{source=auto}` — ноль.
- **Не сделано / известное:** гипотеза «сигналы перед смертью процесса теряются»
  по данным не проверяема — прежнее «доказательство» оказалось арифметикой
  лестницы `DiarizerAttempts`, а других сигналов у смерти процесса нет. 51
  `clientUser` против 42 живых людей не объяснён (переустановки? вторые машины?).
  Дашборд `Users` в TelemetryDeck с этими запросами не сверен.

## 2026-08-19 · Запуск: окно показывается готовым, а не рамой

- **Что:** окно держится на `alphaValue = 0`, пока `showMain` не выведет его с
  содержимым (`SceneWindowChrome.swift`: конceal в `configure`, реестр
  `AppWindowRegistry.revealed`, `layoutSubtreeIfNeeded`+`displayIfNeeded` перед
  показом); `bootstrap()` ушёл из view-update-транзакции `onAppear` в `Task`
  (`MeetingRecorderApp.swift`). Коммит 9b37559.
- **Зачем:** AppKit восстанавливает окно раньше первого SwiftUI-коммита, и
  прозрачное окно стояло ~0.4–0.5 с голой рамой, в которую «прогружалось»
  приложение.
- **Проверено:** покадровый замер (`CGWindowList` alpha + `screencapture -l`
  каждые ~200 мс при перезапуске): фаза alpha=0 есть, первый видимый кадр —
  полный архив; без `displayIfNeeded` первый кадр был пустой тёмной плитой.
  Build `./build.sh` зелёный, `swift test` 922/922.
- **Не сделано / известное:** «Publishing changes from within view updates»
  (2 шт. на запуске) остаётся; источник НЕ `bootstrap`, НЕ
  `MainView.onAppear` и НЕ `selectNewestIfNothingChosen` — каждый переносился
  в `Task`, счётчик не менялся, правки откатаны. lldb-брейкпоинты на
  `_os_log_fault_impl` / `_os_log_impl(type=fault)` не срабатывают на этой
  macOS; ловить — Xcode runtime-issue breakpoint. Замеры запуска: индекс
  7.4 МБ / 66 встреч декодируется за 11–18 мс — не горлышко; декод растёт
  линейно с архивом (транскрипты внутри индекса, P6).

## 2026-08-19 · Чистка телеметрии: минус трафик, минус имена

- **Что:** снят `Capture.levelRate` (`AudioRecorder.swift`, замер остался в
  `debugLog`); имена, собиравшиеся интерполяцией, сведены в параметры —
  `Notice.shown{kind,surface}`, `Pipeline.failed{phase,kind}`,
  `Invariant.broken{name}`, `Model.fetch{reason}`, `Ollama.setup{result}`,
  `Diarize.fallback{stage}`, `Claude.setup{step,result,client,reason,again}`
  (новая секция `Families` в `Analytics.swift`, вызовы в `AppState.swift`,
  `MCPConnector.swift`, `SettingsPane.swift`, `TranscriptionService.swift`);
  `Recap.generated` сужен с восьми осей до `collapsed`+`author`+секунды. Доки:
  §12 `STATE.md` (чистка, правила чтения под 42 пользователя, пять чисел для
  продуктовой аналитики), таблица всех сигналов в `COLLEAGUES.md`,
  `plan-claude-mcp.md`.
- **Зачем:** `levelRate` уходил шесть раз в минуту записи — 180 сигналов на
  получасовую встречу против семи на всю её обработку, почти весь трафик ради
  вопроса про ход лопасти, на который ответ давно получен. Интерполяция в имени
  давала на дашборде декартово произведение (пять поводов × четыре поверхности =
  до двадцати имён), между частями которого не сложить и не сравнить. 31
  фиксированное имя плюс неограниченные семейства → 24 фиксированных имени и ни
  одного семейства (число 14 в первой версии этой записи — ошибка счёта,
  пересчитано по коду; в §12 `STATE.md` исправлено там же).
- **Проверено:** `swift test` — **922 теста, 5 пропущено, 0 падений**;
  `./build.sh` — «Build complete», установлено в `/Applications/Propeller.app`
  21:28:35. Живой процесс (21:31:38, уже новый бинарь) держит соединение с
  ингестом и пишет `[TelemetryDeck:LogHandler]`, но payload в логе `<private>`,
  поэтому **новые имена данными не проверены** — доступа к дашборду в этой
  сессии нет (MCP `telemetrydeck` требует OAuth, сессия неинтерактивная).
- **Осталось:** сверить лексикон с дашбордом, когда будет доступ — история
  обрывается, старые имена останутся со своими данными, новые начинают с нуля.
  `design/notifications.md` §7 всё ещё называет несуществующие
  `Notice.emitted`/`Notice.suppressed` — дрейф не мой, не трогал. Тестов на
  телеметрию нет вовсе (`Sources/` не импортируется тестами), так что от
  опечатки в имени сигнала не страхует ничто.

## 2026-08-19 · Linear закрыт, хвосты переехали в TAILS.md

- **Что:** новый файл [TAILS.md](TAILS.md) — четырнадцать хвостов плоским списком
  с галочками, без проектов, циклов и приоритетов. Слой «Хвосты» внесён в
  `DOCS.md` и в карту доков `STATE.md` Часть VI; правило 6 Части IV переписано
  под решение владельца; правило 5 `DOCS.md` дополнено: из блока «Осталось»
  чужая будущая работа переезжает строкой в `TAILS.md`. Сообщение хука говорит то
  же. Из `STATE.md` §13 убраны номера `PRO-8`/`PRO-11` — они стали мёртвыми
  ссылками. В Linear отменены 39 задач и 5 проектов; воркспейс остался пустым.
- **Зачем:** решение владельца — трекер не нужен. Почти вся работа уже описана в
  `ROADMAP.md`, и задачи `PRO-29`…`PRO-39` были его второй копией, то есть прямым
  нарушением правила 3 `DOCS.md`; сам стол требовал ритуала (циклы, разбор в
  пятницу, привязка к горизонту), которого в формате «сейчас хочу заняться этим»
  нет. Терялись не цели, а хвосты — им и завели место.
- **Проверено:** `list_issues(team=Propeller)` — все 39 задач в статусе Canceled,
  `list_projects` — все пять проектов Canceled. Перед отменой сверил, не пропадёт
  ли конкретика: содержание `PRO-9`/`PRO-12`/`PRO-13` стоит в
  `plan-speaker-tags.md` §11.1 вместе с командами и статусами, а `PRO-14` (Толк:
  `--process`, `AXManualAccessibility` для Electron) в плане описан не был —
  grep по «толк» находит только открытый вопрос §10.1, поэтому этот хвост и
  отметка `claude-mcp-seen` дописаны в `TAILS.md`. Хук после правки сообщения:
  `bash -n` чисто, блокировка на тестовом репозитории по-прежнему exit 2.
- **Осталось:** воркспейс `propellerapp` живой, но пустой — удалять его не мне.
  Ритуал перечитывания `TAILS.md` не задан намеренно: список смотрится, когда
  выбирается следующее дело, а не по расписанию. Три трассы Zoom (`plan-speaker-tags.md`
  §11.1, шаг 1) ждут утренней встречи 20 августа — это календарь, а не список хвостов.

## 2026-08-19 · Стол в Linear собран по одной оси, правило перестало наказывать за порядок

- **Что:** цели H0 стали проектами — «H0 · Находимость» (`PRO-29`…`PRO-33`),
  «H0 · Бренд» (`PRO-34`…`PRO-36`), «H0 · Пережить приток» (`PRO-37`…`PRO-39`,
  туда же переехали `PRO-8` и `PRO-11`); пункты целей, ранее лежавшие абзацами в
  описании одной задачи, стали отдельными задачами с зависимостями (`PRO-34`
  блокирует `PRO-29`). «Спикер-теги» переименован в «H1 · Спикер-теги», старый
  проект «H0 · Эта неделя» отменён с пометкой, куда разложен; `PRO-5`/`6`/`7`
  отменены со ссылками на свои проекты, а не удалены. Правило 6 Части IV
  `STATE.md` переписано целиком.
- **Зачем:** на полке стояли два проекта разной природы — «H0 · Эта неделя» по
  оси времени и «Спикер-теги» по оси темы, — и стол перестал читаться. Вторая
  причина важнее: прежняя формулировка правила («только фронты сейчас/следующий»)
  наказывала владельца за то, что он поработал над H1 раньше плана, и толкала
  прятать начатое. Теперь правило говорит: заводить впрок нельзя, прятать
  начатое — тоже; если работа пошла раньше горизонта, правится `ROADMAP.md`, а не
  трекер.
- **Проверено:** `list_projects` отдаёт четыре живых проекта с префиксами
  горизонтов и критериями «готово, когда» в summary, плюс «H0 · Эта неделя» в
  статусе Canceled. Задачи разложены по проектам через `save_issue`, каждый ответ
  подтвердил `project` и лейблы.
- **Осталось:** ни одна задача ещё не поставлена в цикл — первый стартует
  2026-08-24, и до этого стол весь в Backlog. `ROADMAP.md` пока не знает, что H1
  начат: по новому правилу это его правка, а не трекера — не делал, потому что
  переставлять горизонты может только владелец. Инбокс `PRO-15`…`PRO-28` ждёт
  первого разбора 28 августа.

## 2026-08-19 · Выгребка хвостов: 62 «Осталось» разобраны, 14 стали задачами

- **Что:** прочёсаны все 62 блока «Осталось» в `WORKLOG.md` (58 записей плюс
  четыре варианта формулировки «Осталось в Г3»). Заведены `PRO-15`…`PRO-28` —
  без проекта, лейбл `инбокс` плюс тип работы, в каждой ссылка на дату записи, из
  которой хвост вынут. Правило 6 Части IV `STATE.md` уточнено: привязка к фронту
  требуется только от задач проекта, хвост живёт в инбоксе. Замер пика памяти на
  8 ГБ Mac владелец проводить не будет — строка в «Отложено» `product-ideas.md`,
  задачей не заведён.
- **Зачем:** хвосты писались исправно (58 записей из 62 их несут), но лежали в
  историческом логе, где у них нет состояния: «растры сайта разошлись» от
  17 августа соседствовало с давно закрытым. Трекер даёт им состояние, лог
  остаётся историей.
- **Проверено:** подозрительные хвосты сверены с диском, а не с памятью. `ls
  site/assets` — `og.png`/`preview.png` от 10 августа, то есть старее знака
  (17-го); grep по `*.swift` — `Heading.md`, `Heading.sm`, `Typo.note` не
  встречаются вовсе, `ToastBar`, `notesHeader`, `notePlaceholder`, `pageHeader`
  живут одной строкой определения, а `Heading.lg` жив в `MeetingsTitleBlock`;
  grep по `design/state-gallery.md` — retention там не снят; `find . -name
  "*.wav"` — двух wav из `phase0/` больше нет; в Application Support нет
  `chatgpt-mcp-seen`, а `claude-mcp-seen` датирован сегодня 19:00, то есть похож
  на живое подключение, а не на мусор проб. `list_issues` отдаёт все четырнадцать
  новых задач с лейблом `инбокс`.
- **Осталось:** 39 хвостов признаны закрытыми — почти весь пласт Г0/A5.2/стенда
  умер заморозкой механики саммари 15 августа и релизом 1.16.5 16-го; вычеркнуты
  по документам, кода под каждый не открывал. Восемь оставшихся — ограничения, а
  не работа, и живут текстом (тестам недоступен `Sources/`, гвард пишет `NSLog`,
  «Саболь» неизвестен, `out/` в gitignore). Triage у команды не включён, лейбл
  `инбокс` его заменяет. Разбор инбокса в конце цикла — ритуал, который ещё ни
  разу не проходил; первый раз 28 августа.

## 2026-08-19 · Остаточная работа по MCP заведена в Linear

- **Что:** `PRO-8` «Переснять Г3: в бандле второй подписанный бинарь» — подзадача
  `PRO-7`, лейблы `проба`+`люди` (чистой машины нет, зависимость та же, что у
  родителя); `PRO-11` «Подключить ChatGPT живьём — путь ни разу не пройден
  целиком», лейбл `проба`, с заранее названным исходом и попутным вопросом про
  Developer Mode. Оба в проекте «H0 · Эта неделя», приоритет High. В §13
  `STATE.md` строки `mcp-gate` и `mcp-chatgpt-live` получили номера задач —
  чтобы на то же не завели вторую.
- **Зачем:** правило 6 Части IV — в Linear заводится только то, чей фронт стоит
  в «сейчас», и туда идёт ссылка, а не копия. Эти два куска блокируют релиз
  1.16.5, то есть цель H0; остальное по MCP — не работа: имена событий
  `Claude.*` и тип `ClaudeMCP` неточны, но это состояние кода (§13 `mcp-names`),
  `mcp-broken-commit` — факт истории, а C4 «MCP отдаёт атомы» живёт в H1
  `ROADMAP.md` и в трекер по правилу не идёт.
- **Проверено:** `list_issues(team=Propeller)` отдаёт `PRO-8` с `parentId
  PRO-7` и `PRO-11`, оба в проекте «H0 · Эта неделя» со статусом Backlog и
  лейблами, как заведены.
- **Осталось:** ничего по учёту. Заглушки Linear `PRO-1`…`PRO-4` («Get familiar
  with Linear» и прочие) лежат без проекта и лейблов — трогать не стал, это
  твой стол.

## 2026-08-19 · Заслонка: коммит без записи в WORKLOG не проходит

- **Что:** новый проектный хук `.claude/hooks/require-worklog.sh` и
  `.claude/settings.json`, регистрирующий его на `PreToolUse(Bash)`. Хук ловит
  `git commit` (включая цепочки `git add -A && git commit`, `git -C dir commit`,
  `-am`) и возвращает exit 2, если коммит что-то меняет, а записи за сегодняшнюю
  дату в `WORKLOG.md` нет ни в коммите, ни в истории. Пропускает `--amend`,
  `--dry-run`, пустой стейдж, не-репозиторий, коммит одного `WORKLOG.md` и любые
  шаги того же дня после уже закоммиченной записи. Всё неожиданное (нет jq,
  ошибка git) — exit 0: сломанная заслонка не должна вешать каждый Bash-вызов.
- **Зачем:** правило «document the work» есть в CLAUDE.md и всё равно было
  пропущено четыре коммита подряд (см. запись ниже про CHANGELOG). Коммит — тот
  момент, где проверка однозначна и дешева. Ключ по дате записи, а не по
  «предыдущий коммит трогал файл»: второе дарит бесплатный пропуск первому
  коммиту новой работы.
- **Проверено:** формат payload снят живым пробником (есть `cwd`,
  `tool_input.command`, pwd хука = корень проекта). Матрица 18/18 PASS на
  выброшенном репозитории — блокировки и пропуски перечислены выше, плюс
  ложноположительные `git log --grep commit` и `echo "time to commit"`. Живой
  выстрел: `git -C <тестовый репо> commit -m "live probe"` через Bash — вызов
  заблокирован хуком, коммит не создан.
- **Где записано:** правило 5 в `DOCS.md` («Запись в лог — не по памяти»).
- **Осталось:** хук проектный, в других чекаутах не сработает (глобализация —
  одна запись в `~/.claude/settings.json`). CHANGELOG.md он не проверяет:
  «поменял поведение» из shell не определить. Правило «стейджить только свои
  файлы» тоже не проверяет.

## 2026-08-19 · Рабочий стол в Linear заведён по linear-setup.md

- **Что:** в воркспейсе `propellerapp`, команда Propeller (PRO): семь лейблов
  графа типов работы (`работа`, `ресёрч`, `проба`, `решение`, `люди`, `туман`,
  `инбокс`) с описаниями из плана; проект «H0 · Эта неделя» с описанием дословно
  из `linear-setup.md` §2, лид — владелец; три задачи PRO-5 «A · Находимость»,
  PRO-6 «A2 · Бренд», PRO-7 «B · Пережить приток» с текстами и лейблами из
  плана. В репозитории — только эта запись и пункт 6 в Части IV `STATE.md`.
- **Зачем:** `linear-setup.md` — одноразовый файл: состояние работы должен
  хранить трекер, а не репозиторий. H0 стоит в «сейчас», значит его задачи
  существуют в Linear.
- **Проверено:** `list_issues(team=Propeller)` возвращает PRO-5/6/7 в проекте
  «H0 · Эта неделя» с лейблами `работа`+`люди`, `работа`+`решение`,
  `работа`+`люди`; `get_project` отдаёт описание целиком; `list_issue_labels` —
  все семь лейблов.
- **Где записано:** Часть IV пункт 6 `STATE.md` — решение владельца «где живёт
  состояние работы», с правилом, что попадает в трекер, а что остаётся в
  `ROADMAP.md`.
- **Осталось:** §3 плана (цикл) выставлен вручную владельцем — у авторизованного
  коннектора нет мутации настроек команды. `list_cycles` теперь отдаёт два
  недельных цикла подряд без зазора (24–31 авг, 31 авг–7 сен), то есть длина 1
  неделя и cooldown 0 подтверждены; автостарт через API не читается — **не
  проверен**. Файл `linear-setup.md` удалён по своему же правилу (он был
  untracked, в истории его нет). Статус проекта оставлен Backlog, четыре
  онбординговые задачи Linear PRO-1…PRO-4 не тронуты.

## 2026-08-19 · Доки догнали код: CHANGELOG, STATE §13, CLAUDE.md

- **Что:** пункт в `CHANGELOG.md` («Новое») — впервые за всю фичу; новый
  компонент **§13 «Подключение к ассистентам (MCP)»** в `STATE.md` с таблицей
  открытого (Г3 не переснят, живой ChatGPT не подключали, имена событий
  `Claude.*` стали неточными, невоспроизводимый коммит `9ffdee1`); раздел
  «Assistant connections (MCP)» в `meeting-recorder/CLAUDE.md` — второй бинарь в
  бандле, оба мёржа, выводимое состояние, ловушка с подстрокой «claude»;
  `plan-claude-mcp.md` дописан Claude Code и решением по подписи. Поправлены две
  дрейфующие строки: список групп настроек и число кадров галереи.
- **Зачем:** правило `CHANGELOG.md` — «поменял поведение, строка сюда в том же
  коммите» — я нарушил четырежды подряд, а `STATE.md` объявлен источником правды
  по компонентам, и целого компонента в нём не было. Фича при этом уже стоит у
  людей на машинах.
- **Проверено:** `swift test` — **922 теста, 5 пропущено, 0 падений** (доки кода
  не трогают, прогон ради уверенности). Утверждения §13 сверены с кодом и с
  живыми проверками этой сессии, а не с планом.
- **Осталось:** ничего по документации. По фиче — шаг 7, пересъёмка Г3 со вторым
  клиентским путём.

## 2026-08-19 · «Подключён» снят: галочка уже это сказала

- **Что:** `MCPCellState.connected.subtitle` → nil. Молчащих состояний стало
  два, и оба молчат по одной причине — за них говорит то, что справа: у отказа
  записи кнопка «Попробовать снова», у подключённого галочка. Ярлык кадра в
  галерее для него стал «галочка» вместо латинского `rawValue`.
- **Зачем:** решение владельца 2026-08-19 по снятым состояниям. «Подключён» под
  заголовком и галочка справа — одно утверждение дважды; из двух одинаковых
  лишним оказывается слово. Остальные подписи оставлены: они делают разную
  работу — предложение, инструкция, объяснение пустоты справа.
- **Проверено:** `swift test` — **922 теста, 5 пропущено, 0 падений**; тест
  `testEveryStateEitherSaysSomethingOfItsOwnOrSaysNothing` поймал изменение и
  обновлён вместе с причиной. Добавлен
  `testМолчащаяСтрокаВсегдаЧтоТоПоказываетСправа` — снятая подпись однажды
  оставила бы заголовок в одиночестве. `./build.sh` установлен.
- **Осталось:** отметку `chatgpt-mcp-seen` от моих проб удаляет владелец —
  хук не пускает агента в каталог Application Support ни при каком согласии.

## 2026-08-19 · Claude Code третьим клиентом, командой вместо кнопки

- **Что:** `MCPClient.claudeCode` — клиент без конфига: `configLocation` и
  `configFormat` стали опциональными, кнопка ходит только по
  `MCPClient.connectable`. В настройках у него своя строка `MCPCommandRow` с
  новым компонентом `SettingsCommand` (поле с командой и кнопкой копирования,
  глиф на полторы секунды становится галочкой). Команда — `MCPClient
  .claudeCodeCommand(binaryPath:)`, путь берётся из бандла. В галерее у него
  один кадр вместо шести: состояний нет, есть команда.
- **Зачем:** попутно закрыт баг, который иначе доехал бы до людей: `resolve`
  сопоставлял имя клиента по подстроке «claude», и **Claude Code ставил бы
  отметку Claude Desktop** — галочка «подключён» встала бы у приложения, которое
  человек мог и не ставить. Теперь `claude-code` проверяется раньше.
- **Проверено:** `swift test` — **921 тест, 5 пропущено, 0 падений**;
  `./build.sh` установлен. Кадры галереи снять не удалось: штатный экспортёр
  требует разрешения «Запись экрана», которого у сборки нет, — состояния
  собраны отрисовкой по строкам кода и отданы владельцу на разбор.
- **Осталось:** решение по подписям в строках (владелец спросил, нужны ли они
  вовсе); отметка `chatgpt-mcp-seen` от моих проб лежит в Application Support и
  даёт строке ChatGPT ложное «Больше не видит Propeller» — удалить может только
  владелец.

## 2026-08-19 · Лабораторная часть спикер-тегов — в локальной сборке и в доках

- **Что:** пересобран и установлен `/Applications/Propeller.app` (из чистого
  worktree от HEAD `1888786`, `./build.sh`); правки STATE.md (карта доков) и
  `tools/diarize-lab/DIARIZATION.md` (H12: инструменты построены).
- **Зачем:** владелец попросил занести всё, что можно до гейта Г0; живой
  наблюдатель в приложение не занесён — это шаг 4, за гейтом.
- **Проверено:** `swift test` на HEAD — 922, 5 пропущено, 0 падений; подпись
  `valid on disk`; в установленном бинарнике 80 символов `CallWindowJournal`
  (`nm`). Запущенный Propeller — старый процесс, перезапуск за владельцем.
- **Осталось:** три живые трассы §11.1, эталон к одной из них, Г0.

## 2026-08-19 · Решение «кто говорит» построено над снимками, до гейта

- **Что:** §11 плана спикер-тегов, шаги 1–3. `axprobe trace` (JSONL-трасса),
  `PropellerPure/CallWindowJournal.swift` (трасса → пролёты, 13 тестов,
  синтетические фикстуры в `Tests/Fixtures/ax-traces/`),
  `Checks/CallWindowJournalLab` + `tools/diarize-lab/saer-journal.py` (SAER
  журнала: покрытие, честность, сквозной счёт с фолбэком на диаризацию).
- **Зачем:** гейт Г0 переехал с «можно ли писать код» на «с какими порогами
  выпускать»; логика решается над записанными снимками и проверяется без
  живой встречи. В приложение не уехало ничего (шаг 4 — за гейтом).
- **Проверено:** `swift test` в чистом worktree от HEAD — 895 (882 базовых
  + 13 новых), 5 пропущено, 0 падений; `make-app.sh` собирает пробу;
  `saer-journal.py` на ручной синтетике сходится с ручной арифметикой.
  **Не проверено живьём:** ни одной живой трассы; пороги `Tuning` (разброс
  2×, два опроса) выбраны, а не замерены; маркеры мьюта пусты (Н8).
- **Осталось:** снять три трассы §11.1 (нужна встреча), эталон к трассе —
  иначе SAER не посчитать; Г0; шаг 4. Коммиты 2d1cb48, c1d98e7, 9427a50.

## 2026-08-19 · ChatGPT подключается той же кнопкой

- **Что:** второй MCP-клиент. Новое в `PropellerPure`: `MCPClient` (пути,
  форматы конфигов, имена отметок, `resolve` по `env`/`clientInfo`),
  `CodexConfigMerge` (дописывание секции в TOML). Переименованы и обобщены:
  `ClaudeCellState` → `MCPCellState` (подпись стала `subtitle(for:)`),
  `ClaudeConnector` → `MCPConnector` (два клиента, два формата конфига).
  Сервер ставит **свою отметку на клиента** и отдаёт ChatGPT два лишних
  инструмента — `search` и `fetch` по контракту OpenAI, с `structuredContent`.
  Группа «MCP» в настройках стала списком строк по клиенту; рельс называет того
  клиента, который стоит на машине.
- **Зачем:** `~/.codex/config.toml` один на приложение ChatGPT, Codex CLI и
  расширение IDE — второй клиент стоил перечисления, а не второго сервера.
  Дописывание, а не разбор: файл правят руками, там комментарии, а Foundation
  TOML не читает. `required = true` сознательно не берём — обязательный сервер
  утащил бы за собой запуск чужого Codex, если наш бинарь не поднимется.
- **Проверено:** `swift test` — **905 тестов, 5 пропущено, 0 падений** (было
  882). `./build.sh` — установлен, подпись прошла. На живом бинаре из бандла:
  Клод получает 5 инструментов, ChatGPT — 7; отметки лежат раздельно
  (`claude-mcp-seen` / `chatgpt-mcp-seen`); `search` на настоящем архиве вернул
  20 встреч с полями `id`/`title`/`url` и `content`, совпадающим со
  `structuredContent`; `fetch` — `id`/`title`/`text`/`url`/`metadata`, 8127
  знаков конспекта.
- **Осталось (и одна оговорка по истории):** коммит `9ffdee1` **сам по себе не
  собирается** — `git add` был написан с путями до переименования, git отверг
  pathspec целиком, и правки трекнутых файлов уехали следующим коммитом
  `8185538`. Вершина зелёная; переписывать историю не стал — сверху уже лёг
  коммит параллельной сессии. Записано в `STATE.md` §13 как `mcp-broken-commit`.
  Живой ChatGPT ещё не подключали — кнопку на его строке никто не
  нажимал, `~/.codex/config.toml` не тронут. Имена событий телеметрии остались
  `Claude.*` ради непрерывности дашбордов, хотя теперь несут параметр `client`
  и относятся к обоим. Тип `ClaudeMCP` тоже сохранил имя, хотя описывает общий
  сервер. Шаг 7 (пересъёмка Г3) по-прежнему открыт и теперь включает второй
  клиентский путь.

## 2026-08-19 · ChatGPT спроектирован и вписан в план (не построен)

- **Что:** раздел «ChatGPT — второй клиент» в `plan-claude-mcp.md` плюс правки
  рельса, «Веб-версии», порядка работ (шаг 8) и открытых вопросов. Сервер,
  бинарь и инструменты те же; отличий три: пишем в `~/.codex/config.toml`
  дописыванием секции (не разбором — там комментарии человека, а Foundation
  TOML не читает); Codex поднимает сервер **лениво**, поэтому третье состояние
  строки другое по словам («Готов — возьмёт при первом вопросе» вместо
  «перезапустите»); в deep research нас не видно без инструментов `search` и
  `fetch`, которые предлагается отдавать по `clientInfo` только ChatGPT.
- **Зачем:** одна запись в `~/.codex/config.toml` закрывает сразу три клиента
  (приложение, Codex CLI, расширение IDE) — дешевле, чем вышло с Клодом. И
  `instructions` там документирован, с бюджетом в 512 знаков.
- **Проверено:** факты собраны по `learn.chatgpt.com/docs/extend/mcp` и
  документации OpenAI: путь и схема секции, разделяемость конфига между тремя
  клиентами, ленивый старт и `required = true`, чтение `instructions`,
  требование `search`/`fetch` для deep research. **Не проверено:** нужен ли
  Developer Mode и платный тариф (страница помощи OpenAI отдала 403) — записано
  открытым вопросом.
- **Осталось:** `required = true` сознательно не берём — обязательный сервер
  утащит за собой запуск чужого Codex, если наш бинарь не поднимется. Сверить
  Developer Mode и схему `search`/`fetch` на живой машине до кода.

## 2026-08-19 · Типографика: минус медиум, минус дубль 12 pt, плюс девять стилей в Фигме

- **Что:** из шкалы удалены `Label.mdMedium / smMedium / xsMedium`, `Setup.Typo.action`
  больше не `cell.weight(.medium)`, а `cell`; `Label.smRegular` переведён с 12/14 на
  12/16. Затронуты `PropellerUI/Tokens.swift`, `Sources/SearchPalette.swift` (13 мест),
  `Sources/MainView.swift`, `Sources/SidebarGallery.swift`. В Фигме (файл
  `td7G5O2KhREcABF2nZ2ORx`) заведено девять текстовых стилей SF Pro: `Display/2xl`,
  `Body/Lead|Section|Text`, `Interface/lg|md|sm|xs|2xs`.
- **Зачем:** медиум нёс четыре ступени шкалы и ничего не говорил сверх цветовой
  рампы — палитра ⌘K весила им поле ввода, заголовки результатов, даты и «Ничего не
  найдено» разом, то есть вес был применён к экрану, а не внутри него. 12/14 и 12/16
  были одним кеглем с двумя line box'ами, который выбирался тем, до какого токена
  дотянулось место вызова. Фигма нужна была универсальная (Interface/Body), а не
  контекстная: до этого в файле не было ни одного текстового стиля.
- **Как проверено:** `./build.sh` — зелено, приложение установлено в
  `/Applications/Propeller.app`. `swift test` — 882 теста, 5 пропущено, 0 падений.
  Глазами: `./build.sh --gallery` + `--gallery-export`, 42 кадра; смотрел
  `lib-search` (палитра целиком на regular, ничего не поехало), `onb-01-setup-all`
  («Дальше» без медиума читается за счёт плашки), `lib-empty`. После съёмки
  пересобрано обычной сборкой — в `/Applications` не галерейный бандл (`strings` не
  находит `gallery-export`).
- **Осталось:** 10/12 оставлен как regular, а не переселён на 11/14 — минимальное
  изменение, но если в ⌘K он окажется блёклым, это одна строка. Мёртвые токены не
  тронуты и в Фигму не поехали: `Heading.lg|md|sm` (36/34 живёт только в снапшот-тесте
  и в невызываемом `Theme.pageHeader`), `Tokens.ToastBar` целиком, `Pane.Typo.note`,
  `notesHeader`, `notePlaceholder`. Стили в Фигме не разложены на образец-простыню и
  сортируются по алфавиту (`2xs` идёт первым внутри `Interface/`).

## 2026-08-19 · Имена из окна звонилки: чужой опыт, проба AX, план

- **Что:** обзор того, как атрибуцию решают другие записывальщики, и проба
  `tools/diarize-lab/axprobe` — читается ли клиент Zoom через «Универсальный
  доступ». Разделы «чужой опыт», H12 и H13 в `DIARIZATION.md`; план реализации
  `plan-speaker-tags.md`; строки в карту доков (STATE, Часть VI) и в
  `product-ideas.md` (H12 — за гейтом, H13 — убито). Кода приложения не тронуто.
- **Зачем:** диаризация закрыта 2026-08-18 выводом «не трогать», и болит не она,
  а имена. Обзор показал, что Granola берёт имена из окна звонилки, а не из
  звука; это единственный найденный путь, который бьёт мимо диаризации.
- **Как проверено:** живая встреча Zoom 7.0.6, двое, вид «Динамик», микрофоны
  включены. Имя участника — в `AXDescription` видеоплитки (панель открывать не
  надо), активный спикер — в геометрии: 1080×600 у говорящего против 160×80 у
  молчащего, шесть изменений раскладки за 20 с и две передачи слова. Проба
  собирается и подписывается (`make-app.sh`), `swift build` чист. H13 проверен по
  документации Zoom и Google, не замером — так и помечен.
- **Три ложных отрицания по моей вине, записаны в доску:** ключ по позиции узла
  выдаёт перестановку за речь; заглушённые микрофоны делают замер пустым;
  `AXFrame`/`AXSize`, выброшенные как «дрожь анимаций», — и есть искомый сигнал.
  Плюс два факта про TCC: `AXIsProcessTrusted()` не спрашивает и не логируется, а
  shell-обёртка главным файлом бандла ломает привязку права.
- **Осталось:** гейт Г0 — один прогон на настоящей рабочей встрече (владелец
  гость, участников больше двух, кто-то показывает экран). Семь непроверенных
  пунктов в `plan-speaker-tags.md` §2. До гейта реализация не начинается.
  Спека людей (`plan-people.md`) на паузе по решению владельца.

## 2026-08-18 · Инкремент диаризации: минус опровергнутое правило, плюс снимок корпуса

- **Что:** удалена мёртвая цепочка `reprocess()` → `repairSpeakerAttribution` →
  `relabelSegmentsFromStems` — 114 строк в трёх файлах (`AppState.swift`,
  `PipelineBoundaries.swift`, `TranscriptionService.swift`), включая требование
  протокола `Transcriber`. Добавлен `tools/diarize-lab/snapshot.py`: откладывает
  стемы в `~/diarize-lab-corpus` и ведёт манифест.
- **Зачем:** функция расставляла спикеров по энергии дорожек — правило,
  опровергнутое замером 11.08 (свой голос в своём микрофоне на 4–6 дБ **ниже**
  эха дальней стороны). На ленте из стемов она перепишет верные метки, то есть
  это была мина в ожидании вызывающего. Вызывающего нет: `reprocess()` не зовётся
  ниоткуда, тестов на цепочку нет. Закрывает 🟠 `relabel-stems` в STATE,
  компонент 3. Снимок — потому что корпус для замеров скоропортящийся: retention
  съел звук 53 встреч из 55, и переспросить их уже нечем.
- **Как проверено:** `swift build` чисто, `swift test` — 882, 5 пропущено,
  0 падений. Радиус сверен grep до правки: `SourceAwareSpeaker.resolve`,
  `captureSource`, `loadPersistedSegments`, `encodePersistedSegments`,
  `MarkdownWriter.extractSpeakers` имеют других вызывающих и остались;
  дублёров `Transcriber` в тестах нет. `snapshot.py` прогнан живьём: отложено
  **7 встреч, 0,92 ГБ** (13–68 мин), архив только читался.
  **`./build.sh` не запускался:** основное приложение работает и автозаписывает
  встречи, замена бандла под ним — решение владельца, а видимого поведения у
  удаления недостижимого кода нет.
- **Осталось:** третий пункт первого блока — отдавать наверх `(id, вес)` кластера
  — **не делается**: его единственный потребитель (спека людей, `plan-people.md`)
  на паузе по решению владельца. В снятом корпусе появился второй кандидат на
  слипание, `20260818_140424` (50,8 мин, один кластер на дальней стороне);
  не размечен, вопрос слипания не переоткрыт.

## 2026-08-18 · Спека слоя людей и проектов

- **Что:** `plan-people.md` — спецификация людей и проектов как атрибутов
  встречи. Собрана по трём кругам интервью с владельцем поверх замеров
  диаризации. Плюс строка в карту доков (STATE, Часть VI) и две записи в
  `product-ideas.md`: решение завести людей и отдельная строка о том, что
  опознание по голосу остаётся убитым. Кода не тронуто.
- **Зачем:** наименование, а не разметка, оказалось местом продуктовой боли
  (57 % реплик архива под `Speaker N`, и при DER = 0 цифра та же). Спека нужна
  была до стройки: владелец выбрал порядок «сначала цельная спека, потом потолок,
  потом слой связей».
- **Главное, что спека установила:** заявленные четыре ценности разваливаются на
  два слоя, и **половина не требует наименования вообще** — поиск по человеку и
  контекст перед встречей стоят на связи «встреча ↔ человек», которая уже лежит
  на диске у 66 % архива (`CalendarMeta.attendees`). Второе: определение
  участника, выбранное владельцем («все, кто был, даже если молчали»),
  **ненаблюдаемо** — у молчавшего нет следа в звуке, списка участников звонка
  неоткуда взять, поэтому состав это данные пользователя с машинным черновиком.
  Третье: «приглашён», «был» и «говорил» — три разных факта (замер 315 против
  79), и смешивать их в одно поле нельзя.
- **Как проверено:** спека опирается на замеры этой сессии (числа в §3, каждый
  воспроизводим по индексу архива) и на сверенные по коду факты: `Participants:`
  в markdown собирается из спикеров, а не из состава
  (`MarkdownWriter.swift:138`); контрол фильтра по человеку уже существует и ни к
  чему не подключён (`MeetingsTitleBlock.swift:19`); `PersonErasure` построен на
  допущении «у человека нет объекта», которое спека отменяет. Тесты 882, 5
  пропущено, 0 падений — код не менялся, прогон для протокола.
- **Осталось:** дизайн и вайрфреймы (решение владельца), пять открытых вопросов
  в §12, проекты отдельным разговором. Ближайший шаг по §10 — замер потолка
  наименования одним прогоном оракула; не начат.

## 2026-08-18 · Диаризация: эталон, метрика, вердикт «не трогать»

- **Что:** новая лаборатория `tools/diarize-lab` — `probe/` (SPM-проба:
  `run` гоняет только диаризатор FluidAudio с любой ручкой, `sortformer` —
  сквозную модель NVIDIA, `der` считает DER реализацией самой FluidAudio),
  `saer.py` (главная метрика), `ref/` + `mkref/` + `utts/` (эталон двух встреч,
  пересобирается байт в байт), `out/` (разметки, на которых стоят числа),
  доска `DIARIZATION.md`. Кода приложения не тронуто ни строки.
- **Зачем:** качество диаризации нечем было измерить — как WER до golden.
  Вопрос стоял «есть ли что улучшать и стоит ли цена». Ответ: **не стоит.**
  На встрече, которую владелец назвал средней по сложности, SAER 2,7 % (97,3 %
  времени дальней речи подписано верным кластером); одиннадцать гипотез по
  ручкам дали лучшее 2,5 %; менять движок некуда — мы уже на pyannote
  community-1 через FluidAudio 0.15.5, последний релиз, а оба замеренных
  кандидата хуже (gigastt 51,9 %, Sortformer 24,1 %); RTF 0,002–0,004, то есть
  оптимизировать нечего. Главное: 57 % реплик архива без имени — это дальняя
  сторона, у которой имени негде взять, и **при DER = 0 цифра та же**. Боль
  лежит в наименовании, не в разметке.
- **Как проверено:** эталон размечен руками по расшифровке (85 % и 92 % дальней
  речи, остальное `?` и вне зачёта), пересобирается из файлов репозитория байт
  в байт (`md5` сверен). Числа доски воспроизводятся без звука:
  `python3 saer.py --ref ref/20260817_165425.sys.ref.json --hyp
  out/20260817_165425.base.json --utts utts/20260817_165425.sys.utts.json`
  → `SAER 2.7 %`; `probe der` на тех же файлах → `DER 7.9 %`. Тесты
  `swift test` — 882, 5 пропущено, 0 падений (до работы и после, код не менялся).
  Сборка приложения не запускалась: нечего собирать.
- **Осталось:** архив скоропортящийся — retention съел звук 53 встреч из 55,
  перед следующим замером надо начать откладывать стемы. Встреча
  `20260817_160113` (двое в одном кластере, 7,8 %) не починена: Sortformer её
  частично чинит и одновременно рушит среднюю встречу, подпорка «Sortformer,
  когда кластер один» стоит второй модели в бандле при базе из одной встречи —
  не делаем. Порог кластеризации 0,6 стоит в 0,06 от обрыва, где встреча
  сваливается в один кластер — записано как риск, не крутим. `macos14-fbank`
  этой работой не затронут.

## 2026-08-17 · Цель D: сквозное удаление, стирание человека, retention

- **Что:** пять новых файлов в `PropellerPure` — `MeetingErasure` (карта следов
  и правило принадлежности файла встрече), `ArchiveEraser` (стирание на диске +
  `IndexFile`/`TombstoneFile`), `PersonErasure` + `ArchivePersonEraser`,
  `AudioRetention`. `RecordingStore.remove` перестал сам удалять три wav и
  отдаёт встречу целиком стиранию; появились `erasePerson(named:)` и
  `applyAudioRetention()`. `OrphanAdoption.pruned` удалён — дочистку надгробий
  делает `ArchiveEraser.finishPendingErasures`, то есть то же стирание, что и
  кнопка. В настройках: режим и срок хранения аудио, поле «Стереть человека».
  Три файла тестов, 33 теста.
- **Зачем:** цель D роадмапа. Удаление было половинчатым: уходили аудио и строка
  индекса, а расшифровка и конспект в `meetings/` оставались навсегда (STATE,
  компонент 5, rev-9). Два следа не были найдены ни одним прежним разбором —
  `recordings.json.bak` держит целую копию удалённой записи с транскриптом, а
  карантинные `recordings.json.corrupt-*` держат её вечно.
- **Главное решение — форма, а не объём.** Модель, перечисляющая виды
  производных, ломается в день появления атомов. Поэтому стирание спрашивает
  «несёт ли имя файла id встречи», и `<id>-atoms.json` уйдёт кодом, который про
  атомы не знает (проверено тестом). `MeetingTrace` остался как обязанность
  фикстуры: исчерпывающий `switch`, поэтому новый вид следа **не соберётся**,
  пока не сказано, где он лежит. Чистка индекса при стирании человека обходит
  JSON целиком, а не список полей, — поле, добавленное позже, чистится само.
- **Копии индекса чистятся поэлементно, а не удаляются:** карантин — единственный
  путь восстановления **всех остальных** встреч. Снимок, не разбирающийся в
  массив объектов, докладывается как оставшийся след, надгробие тогда остаётся.
- **Retention по умолчанию выключен** (`keep`). У обновления нет способа
  спросить, а любой N в первый же запуск прошёлся бы по уже лежащему архиву.
  Число ждёт включения: `AudioRetention.defaultDays` = 30. **Включать по
  умолчанию — решение владельца.**
- **Проверено:** `cd meeting-recorder/swift && swift test` → `Executed 882
  tests, with 5 tests skipped and 0 failures` (до работы — 851, минус два теста
  удалённого `pruned`, плюс 33 новых). Проверка на зубы: временно сузил стирание
  до известных видов файлов — 5 падений из 12 в `MeetingErasureTests`, включая
  оба содержательных обхода; правка откатана. `swift build -c release` — чисто.
  Ни одна фикстура не выходит за свой временный каталог; архив владельца не
  тронут, читался только `ls`.
- **Что осталось:** `build.sh` не запускал — он удаляет и переустанавливает
  `/Applications/Propeller.app`, а владелец за машиной; проверена только сборка
  релизной конфигурации. UI (пикер срока, поле стирания человека) тестами не
  покрыт — снимков настроек в наборе нет. Дефолт retention ждёт решения. Замена
  имени не согласуется по падежу («с Иваном» → «с Участник»). Двое стёртых
  становятся одним `Участник`. `Preferences.userName` стиранием не трогается.
  Ручной снимок `recordings.json.pre-restore-20260805_192338` в архиве владельца
  теперь чистится стиранием как любой другой снимок — но он там лежит, и это
  стоит знать.

## 2026-08-17 · Сайт: в шапке голый знак, иконки сайта пересобраны

- **Что:** `site/assets/mark.svg` — знак без подписи, вьюбокс по собственным
  габаритам рисунка (623.6 × 695.4), заливка белая прописью. В шапке
  `index.html` он вместо локапа с надписью, высота 2rem, ширина по рисунку;
  класс `.wordmark` → `.mark`. `favicon.png` (64) и `apple-touch-icon.png` (180)
  перерисованы из того же SVG через CGImageDestination, добавлен
  `favicon.svg` и ссылка на него перед PNG.
- **Зачем:** просил владелец. Иконки сайта были не пропеллером, а микрофоном
  из более раннего брендинга — то есть меняются не «на новый знак», а впервые
  приводятся к нему.
- **Домен:** сайт поднимается на Netlify из `main` — Base directory и Build
  command пустые, Publish directory `site`, собирать нечего. Плейсхолдер
  `propeller.example` в `og:url` и `og:image` заменён на
  `https://propellerapp.netlify.app/`: без абсолютного адреса телеграм не
  разворачивает карточку, о чём в файле и стояла пометка «заменить перед
  публикацией».
- **Решения по композиции:** плита чёрная, знак белый — как в иконке приложения,
  а не прежняя синяя `#162850`. Отступ 3 px и радиус 13 px у 64-го сохранены от
  старого файла. `apple-touch-icon` теперь во весь квадрат и непрозрачный: iOS
  кладёт свою маску, а старый файл был уже скруглён и скруглялся второй раз.
- **Замерено, а не прикинуто:** на 16 px знак читается плохо — шесть тонких
  лепестков сливаются. Пробовал увеличить его внутри плиты (×1.12 и ×1.25) —
  хуже, пропадает плита и остаётся каша. Поэтому оставил кадрирование как в
  иконке приложения и добавил SVG-фавиконку: на 32 px (ретина-вкладка) знак
  уже чистый.
- **Проверено:** `qlmanage -t` по `site/index.html` — шапка со знаком, ассеты
  подгрузились; тот же рендер версии из HEAD для сравнения — посадка и
  выравнивание совпадают. Фавиконки смотрел на 64, 32 и 16 px. В браузерной
  панели проверить не вышло: она снимает страницу в `data:`, относительные
  ссылки не резолвятся, а локальный сервер закрыт политикой.
- **После деплоя:** `.container { gap }` 0.25rem → 0.5rem. Четыре пикселя между
  «macOS 14.4 и новее» и верхом окна читались как слипшиеся; это единственный
  зазор в этом месте, снизу у `.terms` отступа нет намеренно. Замерено
  разметочно-нейтральными `outline` на двух сборках страницы: верх `.preview`
  уехал на 5 px изображения при масштабе рендера 1.25 — ровно 4 CSS-пикселя,
  то есть окно сдвинулось, а не ужалось (оно flex-элемент с `overflow: hidden`
  и могло бы просто съёжиться).
- **Фавиконка без подложки:** `favicon.svg` переписан — плиты нет, вьюбокс
  квадратный по высоте рисунка (695.38), знак отцентрован по горизонтали,
  цвет ведёт `@media (prefers-color-scheme)`: светлая тема — чёрный, тёмная —
  белый. Проверено в браузерной панели на `colorScheme: light` и `dark`: тот же
  файл, вставленный через `<img>`, перекрашивается. Это документный контекст,
  тот же, в котором браузер рисует фавиконку, но **саму вкладку я так не
  проверял** — панель не показывает свой фавикон.
  `favicon.png` и `apple-touch-icon.png` оставлены с плитой намеренно: PNG не
  умеет тему, а прозрачный знак пропадает либо на светлой вкладке, либо на
  тёмной; PNG вообще достаётся только браузерам без поддержки SVG-иконок, у
  остальных выигрывает SVG. Иконке для домашнего экрана прозрачность
  противопоказана отдельно — iOS кладёт её на свой фон.
- **Осталось:** `wordmark.svg` (локап с надписью) обновлён новым знаком, но
  сайтом больше не используется — не удалял, это решение владельца.
  `og.png` и `preview.png` — скриншоты окна со старым знаком в рельсе,
  не пересобирал.

## 2026-08-17 · Новый знак доведён до интерфейса, вордмарка и меню-бара

- **Что:** три оставшиеся поверхности переведены с Union на новый рисунок.
  `PropellerUI/PropellerMark.swift` — шесть лепестков перевыпущены из
  `propellericon.icon/Assets/propeller.svg` (было по 4 кривые на лепесток, стало 6),
  плюс переписаны два комментария: откуда взят рисунок и почему `symmetryPeriod`
  теперь точен. `site/assets/wordmark.svg` — те же шесть путей подменены во всех
  трёх блоках фигмовского экспорта (маска, заливка, внутренняя обводка).
  `swift/Resources/MenuBarIcon.pdf` пересобран из того же SVG через CGPDFContext
  (скрипт одноразовый, в репо не клал; в PDF теперь чёрная заливка вместо белой —
  значок всё равно `isTemplate`, но белый на прозрачном не виден в Preview).
- **Зачем:** иконка уже несла новый знак, интерфейс — старый; в рельсе и в чёлке
  это два разных логотипа в одном окне.
- **Как масштабировал:** равномерно по высоте и по центру — 794-квадрат ужимался
  бы в бокс с лишними полями (радиус −17 %), а неравномерная посадка в квадрат
  убила бы симметрию. Рисунок 623.6 × 695.4, поэтому по бокам остаётся его
  собственный зазор: 2.48 в 48-боксе, 1.11 в вордмарке, 1.65 в PDF. Радиус знака
  25.25 → 24.00 в 48-боксе (−5 %), высота как была.
- **Замерено:** новый рисунок симметричен ровно на 60° — поворот любого лепестка
  на соседний оставляет 0.001 единицы на 794-холсте (0.0001 в 48-боксе). Старый
  стоял на 59.70 / 62.18 / 58.12, и эта погрешность была вписана в комментарий
  как сознательный размен; размена больше нет.
- **Проверено:** `swift test` — 851 тест, 0 падений, 5 пропущено. `./build.sh` —
  зелёная, подпись валидна. Снял с живого приложения: значок в меню-баре
  (шаблон, белый на синей полосе) и знак в рельсе рядом с шестерёнкой — новый
  рисунок, размер как у соседей. Вордмарку рендерил на тёмном фоне рядом со
  старой: посадка и кегль совпадают. Чёлку и SetupView не открывал — они берут
  тот же `PropellerMark`, отдельной геометрии у них нет.
- **Осталось:** не коммичено. Растры на сайте не трогал и они разошлись
  по-разному: `og.png` и `preview.png` — скриншоты окна, в рельсе там виден
  старый знак; `favicon.png` и `apple-touch-icon.png` вообще не пропеллер, а
  микрофон из ещё более раннего брендинга — они были устаревшими и до сегодня.
  Собранный `dist/Propeller.dmg` несёт иконку версии 1.16.5.

## 2026-08-17 · Знак в иконке приложения обновлён, интерфейс на старом рисунке

- **Что:** `propellericon.icon/Assets/propeller.svg` заменён новой отрисовкой
  с рабочего стола (`logoprop.svg`): та же геометрия лопастей, но холст квадратный
  794×794 и знак в нём отцентрован (поля 73/72 по X, 43/40 по Y) вместо прежнего
  624×696 впритык к краям. `icon.json` уже указывал на `propeller.svg` — не тронут.
- **Зачем:** в несквадратном холсте Icon Composer тянул знак в квадрат иконки без
  полей; поля нужны, чтобы знак сел в сетку macOS 26.
- **Проверено:** `cd meeting-recorder/swift && ./build.sh` — зелёная, «Compiling
  icon from ../../propellericon.icon», подпись валидна. `iconutil -c iconset`
  над `/Applications/Propeller.app/Contents/Resources/propellericon.icns` →
  `icon_128x128@2x.png`: новый знак, белый на чёрном, с полями. Тесты не гонял —
  правка только в ассете, Swift не менялся.
- **Осталось:** три поверхности всё ещё несут старый рисунок (Union):
  `PropellerUI/PropellerMark.swift` (рельс, чёлка, SetupView),
  `site/assets/wordmark.svg`, `swift/Resources/MenuBarIcon.pdf`. Не коммичено:
  в дереве лежит чужая правка `icon.json` и удаление `Union 2.svg`.

## 2026-08-16 · Linear: содержание рабочего стола собрано, завести нечем

- **Что:** `linear-setup.md` — лейблы (работа · ресёрч · проба · решение · люди ·
  туман + инбокс), проект «H0 · Эта неделя» с описанием-правилами, три задачи
  (A · Находимость, A2 · Бренд, B · Пережить приток) с текстами дословно из
  ROADMAP.md вместе со строками «Невкусный кусок» и «Готово, когда», настройка
  недельного цикла. Файл одноразовый: удалить, когда заведено.
- **Зачем:** MCP `linear-server` в этой сессии не авторизован — инструменты не
  подгружены, создать в Linear ничего нельзя. Чтобы разбор роадмапа не пропал,
  он сложен в исполняемый вид для следующей сессии.
- **Проверено:** не проверено — в Linear ничего не создано. `claude mcp list`
  показывает linear-server Connected, но `ToolSearch` по «linear» не находит ни
  одного инструмента; харнесс сообщает, что серверу нужна авторизация.
- **Осталось:** авторизовать linear-server и выполнить `linear-setup.md`.
  Открыто: встраивание Linear в начало/конец сессии — предложено, не внедрено.

## 2026-08-16 · 1.16.5 выпущен; «пропавший штамп» оказался не багом

- **Что:** релиз опубликован — тег `v1.16.5`, GitHub Release с тремя ассетами
  (версионный DMG, `Propeller.dmg`, `appcast.xml`), заметки из CHANGELOG +
  Claude-подключение и фиксы вечера (`dist/RELEASE-1.16.5-NOTES.md`). После:
  CHANGELOG опустошён до «Не выпущено (после 1.16.5)», план релиза →
  `archive/RELEASE-1.16.5.md`, живые ссылки поправлены (4 файла). Оговорка Г6
  про штамп версии снята с разбором.
- **Зачем:** решение владельца «Релизь»; штамп — задача-чип 16.08.
- **Проверено:** `releases/latest/download/appcast.xml` отдаёт 1.16.5 (curl);
  Sparkle-цепочка живьём проверена ещё до публикации (локальный фид). Штамп:
  индекс ночного стенда несёт `recapGeneratorVersion: 2` у 4 из 4
  сгенерированных (бэкфилл и живая запись) — путь записи работает; в реальном
  индексе штампов нет потому, что единственная v2-встреча реального архива
  (тестовая 14.08 20:19) удалена — запись ушла из индекса (аудио нет,
  надгробий нет, markdown остался; таких встреч-без-строки в архиве 58 — норма
  жизненного цикла).
- **Осталось:** телеметрия первых дней (`collapsed` по встречам от 10 мин,
  доля `author=assembly`), разговоры с юзерами; «8 ГБ» и сравнение пика с
  1.16.4 — ждут чужого Mac (в архивном плане так и записано).

## 2026-08-17 · Аудио уходит после расшифровки, плеер удалён

- **Что:** дефолт retention `afterDays` → **`afterTranscript`** (`Preferences.swift`,
  `@AppStorage` в `SettingsPane.swift`, док `AudioRetention` переписан). Удалены
  `Sources/AudioPlayer.swift` и `Sources/WaveformScrubber.swift` (~410 строк),
  `@Published var player` и единственный `player.stop()` в
  `deleteAllReclaimableAudio`. Строки в `CHANGELOG.md`, правки `STATE.md`
  (компонент 5, дефект `waveform-dead` закрыт, «шесть функций без интерфейса» → пять).
- **Зачем:** решение владельца. Слушать записи продукт не даёт и не планирует, а в
  то, что человек сам найдёт wav на диске, верить нельзя — значит звук, переживший
  расшифровку, не хранится ни для чего. Мои доводы за окно (переразбор архива
  движком получше, повторная диаризация слипшегося `Speaker S1`, паралингвистика
  §3.2 вижена) предъявлены и отклонены; цена записана в доке `AudioRetention`,
  чтобы через полгода её не пришлось выяснять заново. Плеер удалён тем же
  решением: код без двери выглядел отложенной фичей.
- **Проверено:** `cd meeting-recorder/swift && swift test` → 882 теста, 5
  пропущено, 0 падений (первый прогон после удаления упал на `player.stop()` —
  единственная ссылка, которую не показал первый греп). `./build.sh` →
  `/Applications/Propeller.app`. **Глазами в приложении не смотрел.**
- **Осталось:** совпадение двух дефолтов тестом не ловится; снимков retention нет
  в галерее состояний.

## 2026-08-17 · Retention включён по умолчанию, стирание человека без двери

- **Что:** дефолт `audioRetentionMode` → `afterDays` = 30 (`Preferences.swift`,
  `@AppStorage` в `SettingsPane.swift` — дефолт живёт в двух местах и обязан
  совпадать). Из настроек убрана поверхность «Стереть человека» (поле, диалог,
  `erasePerson()`); ядро и тесты остались. Док `AudioRetention` переписан:
  водораздела и одноразовой миграции нет намеренно. Строки в `CHANGELOG.md`,
  правки `STATE.md` компонент 5 (два дефекта закрыты, один заведён).
- **Зачем:** решение владельца. Опасение «апдейт унесёт уже накопленный архив»
  снято замером: приложение вышло 10 августа, в архиве владельца **ноль** wav
  старше 30 дней (162 файла, самый ранний 27 июля) — водораздел решал бы задачу,
  которой нет. Стирание человека убрано с глаз, потому что постоянных спикеров в
  продукте пока нет, а замена по имени не согласуется по падежу.
- **Проверено:** `cd meeting-recorder/swift && swift test` → 882 теста, 5
  пропущено, 0 падений. `./build.sh` → `/Applications/Propeller.app`, подпись
  Developer ID, `valid on disk`, `satisfies its Designated Requirement`. Замер
  архива: `find ~/.meeting-recorder/recordings -name "*.wav" -mtime +30 | wc -l`
  → 0. **Глазами в приложении не смотрел** — владелец за машиной.
- **Осталось:** совпадение двух дефолтов тестом не ловится (`Sources/` тестам
  недоступен); UI retention без снимков в галерее состояний.

## 2026-08-16 · design/brand.md — образ бренда

- **Что:** новый файл `design/brand.md` (слой «Канон»): архетип Слуга +
  Правитель вместо волшебника/мудреца, корень визуального языка «честность
  механизма» (общее у Vercel, Nothing, Dyson, Herman Miller), собственная нота —
  приборная эстетика со шкалой времени и одним благородным металлом на тёмном,
  антиреференсы (тепло Granola, дефолтный тёмный AI-стиль, B2B SaaS). Строка в
  `STATE.md` §VI.
- **Зачем:** цель A2 роадмапа — лендинг без зафиксированного образа не
  собирается. Волшебник как архетип противоречил §3.6 конституции (дворецкий
  незаметен, а волшебник обязан поражать) и вёл прямо к причине смерти §12.2.
- **Проверено:** документ решений, кодом не верифицируется.
- **Осталось:** имя-обещание одной фразой, типографическая пара, лого — и
  решение, нужно ли лого до сигнала H1.

## 2026-08-16 · ROADMAP.md — план от вижена, порядок осей закрыт замерами

- **Что:** `ROADMAP.md` — цели по горизонтам H0 (эта неделя: находимость,
  пережить приток), H1 (атомы и атрибуция, приватность-минимум), H2 (проектный
  слой, петля, первая продажа), H3 (общее, покрытие), плюс сквозные дорожки и
  раздел «чего не делаем в этом году». У каждой цели назван невкусный
  блокирующий кусок. Строки в `DOCS.md` (слой «План») и `STATE.md` §VI.
- **Зачем:** §14.1 вижена закрыт не рассуждением, а замерами: полнота конспекта
  — свойство модели (4b 3 / 9b 7 / Sonnet 10 из 14), починка входа локальным
  моделям не помогает, и только атрибуция (58 % реплик без имени) чинится
  входом. Значит атомы идут первыми, а рост качества текста упирается в облако.
- **Проверено:** документ решений, кодом не верифицируется. Числа взяты из
  `STATE.md` компонент 6 и `tools/recap-lab/OPTIMIZATION.md` (разделы «Никакого
  потолка нет» и «Финал»), прочитанных в этой сессии.
- **Осталось:** §14.2 вижена (облако и аккаунты для общего) блокирует H3;
  гипотезы петли §16 не проверены; цена 2 500 ₽/место не проверена ни одним
  разговором.

## 2026-08-16 · VISION.md — точка Б, собранная в интервью

- **Что:** новый слой документации — `VISION.md` (обещание, конституция из
  шести правил, домен «речь, а не знание», два слоя продукта, экономика,
  рынок и три отличия, семь осей развития, отложенное с условиями возврата,
  четыре открытых вопроса). Строка в карту доков `STATE.md` §VI и слой
  «Вижен» в `DOCS.md`. Память: `propeller-vision`,
  `propeller-people-attribution`.
- **Зачем:** первый дофаминовый прилив кончился; владельцу нужен роадмап под
  режим «полчаса-час в день», а роадмап не строится без точки Б. Ключевой
  сдвиг: главный читатель архива — агент, человеческий интерфейс вторичен;
  коммерческая цель (500k–1M ₽/мес) объявлена обязательным топливом.
- **Проверено:** не верифицируется кодом — документ решений, не поведения.
  Рынок сверен веб-поиском 2026-08-16 (локальность больше не дифференциатор:
  Granola bot-free + локальная запись, Meetily MIT бесплатен).
- **Осталось:** роадмап отдельным документом; открытые вопросы §14.
- **Дополнено тем же вечером (интервью продолжено, 21 вопрос):** §6 «Общее» —
  модель Figma (личное бесплатно, деньги за общее), линия «делится содержание,
  не наблюдения», видимость вместо записи (аудитория атома = комната, ручное
  только расширение), оптики встреча → проект → компания, полигон Прагматика.
  §10 платформы: захват — сменная деталь, память — ядро; Mac пишет, общим
  пользуются все. §11 ритм (час/день, 1–2 релиза и 2–3 поста в неделю) и
  сигналы (100 пользователей и органика — 2–3 недели; продажа в компанию —
  3–4 месяца). §12 предмортем: обе причины смерти внутренние.
- **Правки после внешнего разбора (три агента: позиционирование/GTM, стратегия,
  приватность):** §3.2 ярлыки эмоций убраны, остаётся сигнал без интерпретации
  (EU AI Act Art. 5(1)(f) запрещает вывод эмоций из голоса на рабочем месте —
  первоисточник не читан, помечено). §8.2 «прокладка к агенту почти ни у кого»
  зачёркнуто как неверное: Granola $125M / $1.5B в марте 2026, Spaces +
  enterprise API + MCP; MCP есть у Otter, Fireflies, Fellow; Контур.Толк и МТС
  Линк идут в проектную память. §7: арифметика мест была завышена (3–5 мест, а
  не 15 ⇒ ~40 компаний), добавлена якорная альтернатива 3–4 контракта по
  150–200 тыс. ₽/мес. §12.2 новая причина смерти — отсутствие петли
  распространения. §14.2 противоречие «общее ⇄ отложенное облако» переведено в
  явное решение, §14.5 — развилка «временный ров на русском против общего».

## 2026-08-16 · Финальный образ, Sparkle со сменой подписанта, Г6 — все гейты закрыты

- **Что:** финальная цепочка релиза: `swift test` (851/5/0) → `./build.sh` →
  `./notarize.sh` (.app `accepted`) → `./make-appcast.sh` → `./package-dmg.sh`
  (`dist/Propeller-1.16.5-20260815.dmg`, `accepted`, тикет внутри). Затем
  живой Г6+Sparkle: v1.16.4 из тега (ad-hoc) поверх новых данных, локальный
  фид (127.0.0.1:8642), апдейт Sparkle → 1.16.5 Developer ID. Галочки — в
  `RELEASE-1.16.5.md`; стенд прибран (override снят, сервер погашен, worktree
  удалён), бэкап старого appcast — `dist/appcast-1.16.4-backup.xml`.
- **Зачем:** последние два пункта Г3 (Sparkle) и Г6 закрываются только живым
  переходом ad-hoc → Developer ID — риск №3 был единственным, что могло
  оставить людей без пути наверх.
- **Проверено:** после апдейта `TeamIdentifier=9T455555U3`, `flags=runtime`,
  `spctl accepted`; конспекты нетронуты (0 файлов с новым mtime в окно
  апдейта, 106 счётом); версия 1.16.5. Владелец глазами: старый код читает
  новый архив, ничего не пересчитывает.
- **Осталось:** публикация DMG+appcast на GitHub Release (руки владельца);
  телеметрия первых дней (`collapsed` по встречам от 10 мин, доля `author=
  assembly`); находка «штамп версии не доезжает до индекса» — отдана задачей.

## 2026-08-15 · Таймкод с часом больше не переносится на две строки

- **Что:** `PropellerUI/Tokens.swift:880` (`Tokens.Pane.transcriptTimeWidth`
  40 → `Space.s48`, комментарий объясняет расчёт от `H:MM:SS`); `.lineLimit(1)`
  добавлен рядом с `.frame(width: transcriptTimeWidth)` в трёх местах, которые
  делят этот токен: `PropellerUI/MeetingPane.swift:674-677` (готовая встреча),
  `PropellerUI/RecordingPane.swift:190-193` (живой транскрипт),
  `PropellerUI/TranscriptNote.swift:40-42` (заметка в ленте).
- **Зачем:** токен был сайзен под `MM:SS` (40 pt); `TranscriptPresentation.formatTimestamp`
  на встрече длиннее часа отдаёт `H:MM:SS`, и таймкод переносился на две строки
  рядом с именем спикера (баг владельца, `1:23:12` → `1:23:1`/`2`) — во всех трёх
  местах разом, потому что все три читают один токен.
- **Проверено:** `swift /tmp/measure.swift` — в `Tokens.Pane.Typo.transcriptMeta`
  (11pt, railWeightTrim −0.08, `monospacedDigit`) любая цифровая комбинация
  `H:MM:SS` даёт одну и ту же ширину ~41.5 pt (табличные цифры), `MM:SS` — ~31.2
  pt; 48 pt даёт тот же запас, что 40 pt давал `MM:SS`. `swift test`: 851/851,
  5 пропущено, 0 падений. `./build.sh`: собрался, `/Applications/Propeller.app`
  переустановлен (проверено `ls -la` на бинарнике). Сам экран не смотрел (нет
  доступа к скриншотам) — визуальную проверку оставляю владельцу.
- **Осталось:** ничего не осталось в этой задаче.

## 2026-08-15 · Г3: живая прогонка владельцем — два последних сценария

- **Что:** стенд g3-final (копия архива, 3 встречи без конспектов, из них
  длинная), владелец за машиной: ⌘R посреди бэкфилла и клик по считающейся
  встрече. Галочки и наблюдения — `RELEASE-1.16.5.md` (Г3); «прогресс по
  чанкам» вычеркнут из пункта решением владельца.
- **Зачем:** два сценария Г3 требовали событий ввода, которых у сессий нет.
- **Проверено:** запись при живом бэкфилле получила транскрипт+конспект вне
  очереди за 38 с, бэкфилл догнался (`stopped: finished`, 3/3); счётчик
  «фрагмент N из M» в панели не виден — потребность закрыта индикацией
  активности. Наблюдение: гвард на «Агентном дизайне» на новом билде отдал
  сборку (12 против 44) — днём был свод (25): автор недетерминирован между
  билдами, смотреть телеметрию `author` в проде. Окружение возвращено, архив
  цел (106 конспектов). Ошибка сессии по ходу: фильтр удаления в стенд-копии
  зацепил 6 файлов вместо 3 — восстановлено из оригинала там же, оригинал не
  затрагивался.
- **Осталось в Г3:** Sparkle-апдейт обычный и со старой ad-hoc установки
  (совместимо с Г6). Найден визуальный баг: таймкод с часом (1:23:12) в
  транскрипте переносится на две строки — отдан агенту.

## 2026-08-15 · Г3 добит до двух человеческих действий

- **Что:** в `RELEASE-1.16.5.md` (Г3): «Агентный дизайн» прочитан владельцем —
  ок; пункт «модель не скачана» переписан с протухшего «honest empty state» на
  «находит или скачивает сама» и закрыт живым тестом; «чистый Mac» закрыт
  заменителем (слово владельца: чистых Mac ни у кого нет).
- **Зачем:** протухшая формулировка старше ModelProvisioning и проверяла бы
  состояние, которое дизайн объявил незаконным (невыразимые состояния).
- **Проверено:** `ollama rm qwen3.5:4b` при закрытом приложении → `open -a
  Propeller` → модель снова в сторе (3,4 ГБ, ~6 мин) без действий человека,
  9b/2.5:7b не тронуты. Провижининг не пишет лог-строк — отмечено в плане.
- **Осталось в Г3:** клик по считающейся встрече + ⌘R (совместить, один
  прогон); Sparkle-апдейт обычный и со старой ad-hoc установки (совместимо
  с Г6-даунгрейдом: поставить старую, обновиться Sparkle'ом на новую).

## 2026-08-15 · Г3: kill посреди генерации, notes, тишина — три галочки стендом

- **Что:** три сценария Г3 закрыты живым приложением на стенд-копии
  (`/Users/Shared/propeller-stand/g3-scenarios/`, `stand-build.sh` + возврат
  конспектов кроме сценарных); галочки с доказательствами — `RELEASE-1.16.5.md`.
- **Зачем:** гейт требовал живого приложения, а не кода: kill проверяет
  бэкфилл после смерти процесса, notes — дословность на настоящих заметках,
  тишина — что отказ честный и без зацикливания.
- **Проверено:** kill −9 на 57 с генерации → рестарт «4 owed» → 4/4 за 5,5 мин,
  `stopped: finished`, дублей 0, битых 0, индекс чист. Notes: две настоящие
  встречи, блок «## Заметки» байт-в-байт (110 и 331 байт). Тишина
  (синтетическая): terminal `noSpeech` за 67 мс, одна попытка, без конспекта;
  «Салам»-встреча получила честный «встреча не состоялась», не выдумку.
  Настоящий архив после прогонов: 106 конспектов, свежайший 08:59 — не тронут;
  пути и `analyticsEnabled` возвращены (ключ удалён, как было).
- **Осталось:** в Г3 ещё пять пунктов (длинная встреча глазами владельца, живая
  запись во время бэкфилла, модель не скачана, Sparkle-апдейты, чистый Mac).

## 2026-08-15 · Телеметрия использования Claude-подключения

- **Что:** `PropellerPure/ClaudeUsage.swift` (формат журнала и сводка),
  `MCPServer/UsageLog.swift` (сервер дописывает строку на состоявшийся вызов),
  `Analytics.reportClaudeUsage()` (забирает переименованием, шлёт `Claude.used`
  с параметрами `tool` / `frequency` / `days` и числом вызовов в значении,
  стирает). Зовётся на старте приложения и на смене суток. Плюс
  `ClaudeUsageTests` (9 шт.).
- **Зачем:** отметка `claude-mcp-seen` на вопрос «сколько людей пользуются» не
  отвечает и не сможет: stdio-серверы поднимаются вместе с Claude Desktop, то
  есть каждый день у всех, кто подключил. `Claude.connected` говорит только про
  нажатие кнопки. Считать надо вызовы, а они в чужом процессе — отсюда
  разделение «сервер пишет, приложение отправляет»: у сервера не появляется ни
  сети, ни SDK, ни очереди отправки.
- **Проверено:** `swift test` — **847 тестов, 5 пропущено, 0 падений**.
  На бандловом бинаре: `initialize` и `tools/list` в журнал не попадают, три
  вызова дали ровно три строки. Два сервера сразу (как Desktop и Code) — 60
  вызовов, 60 целых строк, ни одной рваной. Забор журнала переименованием на
  ходу — 0 потерь из 9: каждая запись открывает путь заново, поэтому сервер
  продолжает в новый файл. Выключенная телеметрия — сервер не пишет вовсе.
- **Осталось:** отправку самого сигнала в TelemetryDeck живьём не смотрел —
  Propeller запущен, `bootstrap()` уже прошёл; журнал уедет при следующем
  запуске приложения или на смене суток. Дашборд под `Claude.used` не заведён.

## 2026-08-15 · Технический аудит подключения после живого коннекта

- **Что:** пять правок по итогам первого настоящего соединения с Claude
  Desktop. `MCPServer/main.swift` — `autoreleasepool` на сообщение;
  `MCPServer/Archive.swift` — настройки читаются из своего домена
  (`UserDefaults.standard`), в карточку приехали приглашённые из календаря,
  выброшено неиспользуемое поле `status`; `PropellerPure/MeetingLookup.swift` —
  `people` ищет по говорящим, приглашённым и заголовку, сравнивая основы;
  `Sources/ClaudeConnector.swift` — «подключён» требует, чтобы бинарь по
  записанному пути существовал; `package-dmg.sh` — отказ паковать сборку без
  `PropellerMCP`. Плюс флаг `--paths`.
- **Зачем:** утечка (0,2 МБ на вызов) в процессе, который живёт днями;
  `UserDefaults(suiteName:)` со своим bundle id не работает вовсе, то есть
  перенесённый архив читался бы по умолчанию; `people` на живых данных не
  находил никого, кроме владельца микрофона, а «ничего не нашлось» читается как
  «такой встречи не было».
- **Проверено:** `swift test` — **838 тестов, 5 пропущено, 0 падений**.
  Замер памяти на бандловом бинаре: до правки RSS 8 → 87 МБ за 300 вызовов без
  плато, после — плато 27–29 МБ на тех же 300 (54,6 с, ~180 мс на вызов).
  `--paths` из бандла отдаёт настоящие пути и 52 встречи, stderr на обычном
  прогоне пуст (0 байт). `search_meetings(people: ["Даня"])` находит «1х1 с
  Даней». Подпись вложенного бинаря: Developer ID + hardened runtime +
  timestamp; `spctl` пока `rejected — Unnotarized`, это и есть гейт Г3.
- **Осталось:** Г3 (нотаризация, чистый Mac, Sparkle со старой сборки).
  Кеша чтения нет намеренно: 180 мс на вызов не заметны, а протухший кеш дал бы
  «такой встречи нет». `instructions` сервера Claude **доносит** — видно в
  системном промпте живой сессии; в плане это стояло как сомнительное.

## 2026-08-15 · Подключение к Claude (MCP, read-only) — шаги 1–6 плана

- **Что:** реализован `plan-claude-mcp.md`, шесть коммитов. Новый SPM-таргет
  `MCPServer/` → бинарь `PropellerMCP` в `Contents/MacOS` (stdio-MCP, четыре
  инструмента, только чтение архива; единственная запись — отметка
  `claude-mcp-seen` в Application Support). Чистый слой в `PropellerPure`:
  `ClaudeMCP` (каталог инструментов), `RecapDigest`, `MeetingLookup`,
  `TranscriptSlice`, `ClaudeCellState`, `ClaudeConfigMerge`, `ClaudeConnection`.
  Приложение: `Sources/ClaudeConnector.swift`, ячейка в `SettingsPane`,
  `SettingsCheck` в `SettingsKit`, третий кейс `SetupPrompt` (счётчик стал
  1/3 · 2/3 · 3/3), семь кадров галереи, `build.sh` кладёт и подписывает бинарь.
- **Зачем:** сегодня показать Клоду `~/.meeting-recorder/meetings` можно и это
  не работает — путь надо знать. Фича живёт на двух вещах: одна кнопка вместо
  конфига и описания инструментов, за которые модель берётся сама. Поэтому
  описания лежат в типе, где их видит тест, а состояние ячейки выводится из
  чужого файла и отметки, а не хранится.
- **Проверено:** `swift test` — **831 тест, 5 пропущено, 0 падений** (было 757).
  `./build.sh` — бинарь в бандле, `codesign --verify --strict` проходит, версия
  сервера читается из Info.plist (`1.16.5`). Живой прогон бандлового бинаря на
  настоящем архиве (52 встречи): `initialize` → `tools/list` → четыре вызова,
  `search_meetings("найм")` дал 2 встречи со сниппетами, `find_decisions` — 7
  пунктов из 2 встреч, `get_recap`/`get_transcript` — по делу, несуществующий id
  вернул `isError` с текстом. Экспорт галереи — 42 кадра, дублей среди новых нет
  (первый прогон дал шесть одинаковых, из-за чего кадры ячейки стали доской).
- **Осталось:** шаг 7 плана — **пересъёмка Г3** (подпись изнутри наружу,
  нотаризация обоих артефактов, двойной клик на чистом Mac, Sparkle с реально
  установленной старой сборки): нужен релизный прогон, здесь не делался.
  Живьём с Claude Desktop **не проверено**: это запись в чужой конфиг и его
  перезапуск, нужно решение владельца. В Application Support лежит отметка
  `claude-mcp-seen` от дымового прогона — удалить (хук на пользовательские пути
  не пустил). Телеметрия: шлём `Claude.connected` / `Claude.connectFailed`
  (`reason`) и `Setup.claudeAsked`; сигнала «какое состояние ячейки видят люди»
  нет — если нужен, это одна строка.

## 2026-08-15 · Г4: рост памяти бэкфилла объяснён и закрыт фиксом

- **Что:** причина роста 5,0 → 12,3 ГБ — prompt cache llama-server
  (`--cache-ram`, деф. 8192 MiB), копивший KV-стейты отработанных промптов
  (354 MiB на 8107 токенов). Фикс — `LLAMA_ARG_CACHE_RAM=0` при спауне серва:
  `PropellerPure/OllamaServeTuning.swift` + правка
  `Sources/OllamaSidecar.swift` + `OllamaServeTuningTests` (3 шт.). Числа и
  приёмка — в `RELEASE-1.16.5.md`, Г4; новый сэмплер по PID —
  `/Users/Shared/propeller-stand/stand-watch-pids.sh`.
- **Зачем:** решение владельца — фоновому бэкфиллу разгоняться до 12 ГБ
  нельзя; кэш не окупался ни разу (конвейер не повторяет промпты).
- **Проверено:** `swift test` — 834, 5 пропущено, 0 падений; `./build.sh` —
  Installed. Живой лог раннера: «prompt cache is disabled». Полный стенд 52
  встреч на фиксе: пик рабочей фазы 5 910 МБ (база 12 349), выше 8 ГБ — 0
  сэмплов (база 20 %), старт 5 321 ≈ финал 5 316, 75 мин, 52/52, ноль failed.
  Настоящий архив после прогонов — 106 конспектов, свежайший от 08:59, пути и
  `analyticsEnabled` возвращены, homebrew-серв перезапущен.
- **Осталось:** сырой пик серии 17,2 ГБ — один тик GPU-пробы при старте
  свежего серва, до первой встречи (см. оговорку в Г4); фикс не достаёт чужой
  адоптированный Ollama; 8 ГБ-Mac тестера и сравнение с 1.16.4 — как и были.

## 2026-08-15 · Гвард длинной встречи виден `log stream`

- **Что:** `meeting-recorder/swift/Sources/RecapService.swift` — четыре решения
  пути длинной встречи (фрагмент не разобран ~471, встреча не влезла в окно
  ~480, свод не удался ~514, решение `RecapDigestGuard` ~524/526) переведены с
  `NSLog` на `debugLog` (`Logger(subsystem: "app.propeller")`). Остальные
  `NSLog` в файле не тронуты.
- **Зачем:** Г3-чек-лист велит смотреть решение гварда через
  `log stream --predicate 'subsystem == "app.propeller"'`, а `NSLog` пишет без
  подсистемы — предикат его не видит (см. запись Г4 выше, строки 26-29).
- **Проверено:** `swift test` — 825 тестов, 5 пропущено, 0 падений.
  `./build.sh` — `=== Installed: /Applications/Propeller.app ===`. Байт-поиском
  по бандлу (`grep -a`) все четыре русские строки, включая «документ пишет
  свод модели», найдены в установленном бинарнике.
- **Осталось:** живой прогон `log stream` с реальной длинной встречей не
  делал — строки должны появиться, но подтверждение на живом трафике за
  тестером.

## 2026-08-15 · Г4: нагрузочный прогон бэкфилла, числа в плане

- **Что:** стенд-копия архива и прогон бэкфилла 52 встреч на установленном
  1.16.5. Числа записаны в `RELEASE-1.16.5.md` (Г4, плюс длинные встречи в Г3
  и `author` в Г5). Скрипты стенда — `/Users/Shared/propeller-stand/`:
  `stand-build.sh`, `stand-watch.sh`, `stand-report.sh`. Выдача по длинным
  встречам — `/Users/Shared/propeller-stand/g3-long-meetings/`.
- **Зачем:** Г4 требовал живых чисел, а не оценок; на 16 ГБ пик оказался
  12,4 ГБ, и это меняет вопрос про 8 ГБ с «не вырос ли» на «поместится ли».
  Отдельного macOS-пользователя не заводили: изоляцию дают `recordingsPath` /
  `meetingsPath`, а учётка потребовала бы системных настроек и пароля.
- **Проверено:** `stand-build.sh` — 110 транскриптов, 106 конспектов удалены
  **в копии**; бэкфилл 16:56:07 → 18:18:17 (`[pipeline] stopped: finished`),
  52 из 52, ноль `failed`. `stand-report.sh`: 82 мин, медиана 90 с/встречу;
  ollama пик 12 349 МБ, суммарный 12 448 МБ, 20 % сэмплов выше 8 ГБ; батарея
  100 → 32 % за 81 мин; троттлинга ноль. Настоящий архив до и после — 106
  конспектов, свежайший файл от 08:59, то есть не тронут; пути в настройках
  возвращены, `diff` с бэкапом даёт только `SULastCheckTime`.
- **Осталось:** «8 ГБ» — нужен Mac тестера. Сравнение с 1.16.4 — кита нет,
  числа записаны как база. Прогон от сети — нужен человек с зарядкой. Живая
  запись во время бэкфилла и скриншот «фрагмент N из M» — нужен ⌘R и клик
  человеком: `osascript` не имеет права слать события ввода (1002). Строки
  лога про гвард недостижимы в принципе — они `NSLog`, а подсистемы
  `com.apple.defaultSubsystem` на машине нет.

## 2026-08-15 · План подключения к Claude (MCP, read-only) — документ

- **Что:** новый `plan-claude-mcp.md`. Одна ячейка в настройках с кнопкой,
  четыре выводимых состояния, третий кейс `SetupPrompt` в рельсе, набор
  MCP-инструментов с прогрессивным раскрытием, отказ от веб-версии с причиной.
  Код не начат.
- **Зачем:** решение владельца — делать только Claude и только чтение; путь
  человека надо было зафиксировать до кода, потому что фича живёт или умирает
  на двух вещах (одна кнопка вместо конфига; описания инструментов, за которые
  модель сама берётся).
- **Проверено:** не проверено — документ. Механика рельса сверена с кодом
  (`PropellerPure/SetupPrompt.swift`, `PropellerUI/SidebarPrompt.swift`),
  формат `.mcpb` и состояние MCP в ChatGPT — по документации Anthropic и
  OpenAI. Схема `claude_desktop_config.json` с живым файлом **не сверена**.
- **Осталось:** письмо в Anthropic за разрешением на знак; сверка схемы
  конфига с живым файлом; формат отметки. Раздел «Открытые вопросы» в плане.

## 2026-08-15 · План Claude сверен с документацией, восемь правок

- **Что:** агент проверил десять фактических утверждений `plan-claude-mcp.md`
  по докам Anthropic, спеке MCP и репозиторию `modelcontextprotocol/mcpb`.
  Внесено: точная схема конфига (`mcpServers`, абсолютный `command`); риск
  того, что Claude Desktop сам стирает `mcpServers`; отметка привязана к
  `initialize` и означает «Клод запустил процесс», не «человек пользуется»;
  ставка снята с `instructions` (Desktop его, судя по всему, не доносит) и
  перенесена на описания инструментов; ограничение на имена названо формально
  (`[a-z0-9_]`, ≤64); аргумент против `.mcpb` переписан честно — это один
  двойной клик, а не три; раздел «Лого» переписан под брендгайд.
- **Зачем:** план писался по памяти о том, как устроен MCP; половина
  утверждений была правдоподобной и непроверенной. Две ошибки были дорогими:
  ставка на `instructions` и «три клика» у `.mcpb`.
- **Проверено:** десять пунктов с URL официальных источников; где источника нет
  (наследование PATH, перезапись конфига на macOS) — записано как непроверенное.
- **Осталось:** знак Claude требует **предварительного письменного разрешения**
  Anthropic и запрещает перекраску — нужен запасной вариант кнопки без знака,
  иначе релиз ждёт чужой ответ.

## 2026-08-15 · Знак Claude снят, план готов к реализации

- **Что:** кнопка «Подключить» везде текстовая, без логотипа (решение владельца
  2026-08-15); раздел «Лого» заменён на «Знак Claude — снят из плана» с
  сохранённой причиной, чтобы к вопросу не возвращались вслепую. Добавлен
  раздел «Порядок работ» — семь кусков от сервера до пересъёмки Г3, с указанием,
  что состояние ячейки и форма мёржа конфига живут в `PropellerPure`.
- **Зачем:** брендгайд Anthropic требует предварительного письменного
  одобрения; ставить релиз в зависимость от чужого ответа не за что, а знак
  фичу не держит. План отдаётся отдельной сессии на реализацию, поэтому нужен
  порядок работ и адреса, а не только описание поведения.
- **Проверено:** не проверено — документ.
- **Осталось:** реализация. Открытые вопросы — раздел в плане: формат отметки,
  имя события телеметрии, сверка схемы конфига с живым файлом на машине
  владельца.

## 2026-08-15 · Г2 закрыт: маршрутизация вынута в тестируемое, бэкфилл сверен

- **Что:** решение «резать или один проход» целиком (включая «облако не режется
  никогда») вынуто из `RecapService` в
  `TranscriptChunking.needed(backend:promptCharacters:)` +
  `testНарезкаТолькоДляЛокальногоПути`; галочки Г2 в `RELEASE-1.16.5.md`
  закрыты с адресами доказательств. Порт свода перепроверен независимо:
  промпт дословный из 26e0504^ (сборка parts «Ниже — факты…»), сьют
  перепрогнан отдельно от порт-сессии.
- **Зачем:** последние два пункта Г2; строка `backend == "ollama" &&` жила в
  Sources, где тест её не доставал, — «облако не режется» держалось на честном
  слове.
- **Проверено:** `swift test` — **757 тестов, 5 пропущено, 0 падений**;
  «бэкфилл пропускает конспект любой версии» — цепочка из трёх покрытых
  звеньев (`SummaryStageReconciler` → `RecordingStageTests`;
  `testAFinishedArchiveSchedulesNothing`; версия не входит в решения по коду).
  `./build.sh` — установлен.
- **Осталось:** Г3 — руками владельца (чек-лист с командами готов); Г4 —
  сессией на собранном ките; Г6 — даунгрейд руками; Г5 — критерий отката
  смотреть по встречам от ~10 минут (ложные collapsed на огрызках).

## 2026-08-15 · Конспект длинной встречи снова пишет автор, сборка ушла в страховку

- **Что:** `recapByChunks` (`Sources/RecapService.swift`) снова зовёт свод — тот же
  конспектный промпт над фактами, вернулся дословно из `26e0504^`, t=0 + ретрай, в
  окне фрагментов. `RecapAssembly.assemble` считается на тех же фактах, но читателю
  не показывается: выбор делает новый `PropellerPure/RecapDigestGuard` (+7 тестов,
  фикстура границы `chat-collapsed-digest.json`), порог доли пунктов —
  `RecapGenerationPolicy.digestMinBulletShare`. Решение уходит в `NSLog` и в
  параметр `author` сигнала `Recap.generated` (`Analytics.swift`, `AppState.swift`);
  `draftStats` свода больше не `nil`. Доки: `RELEASE-1.16.5.md` (Что едет,
  маршрутизация, Г1/Г2/Г3), `CHANGELOG.md` (обещание «нет Итога» снято).
- **Зачем:** решение владельца 2026-08-15 (`tools/recap-lab/OPTIMIZATION.md`,
  «Финал»): связность — свойство единственного автора, и сборка из фрагментов
  проиграла ему по читаемости на всех трёх замеренных жанрах. Но режим схлопывания
  свода никуда не делся (A5.1: 4 пункта против 9), поэтому сборка остаётся линейкой
  и запасным выходом, а не основным путём.
- **Проверено:** `swift test` — 756 тестов, 5 пропущено, 0 падений (дважды подряд;
  первый прогон дал 755/1 — в дерево в ту же секунду писала параллельная сессия по
  `TermCanon`). `./build.sh` — exit 0, установлен в `/Applications/Propeller.app`,
  подпись `valid on disk`.
- **Осталось:** живой прогон длинной встречи не делался — это Г3, руками владельца.
  Порог `digestMinBulletShare` = 0,5 выбран от A5.1 и от 14 буллетов сборки на живой
  фикстуре; сколько пунктов держит **здоровый** свод на тех же фактах, не замерено —
  поэтому порог мягкий, и калибровать его будет доля `author=assembly` в проде.

## 2026-08-15 · Три ложных «человека» из словаря и Ильяс в ядро

- **Что:** «Саболь» убран целиком (владелец: не фамилия, а неизвестно что);
  «Медловский» → `мидл` и «Бладос» → `Владос` перенесены в `TermCanon` (таблица имён
  и основ соответственно); в ядро добавлены `Ильяс`, `Ильяса` и формы
  `мидла/мидлы/мидлов`. Ядро 393 → 398, полный список 786 → 791, та же правка в
  `BuiltinHotwords.swift`, разбор — в `vocab/slang-vocabulary.md`.
- **Зачем:** разбор владельца по вчерашнему списку. Три «фамилии» оказались не
  людьми: «Медловский (мидл)» — должность, услышанная конспектом как человек,
  «Бладос» — Владос (в расшифровках верен 13 раз из 13). «Ильяса» распознавание
  стачивает до «Ильи» — 27 верных форм против 25 «Илья» по архиву.
- **Проверено:** `swift test` — 756 тестов, 0 падений, 5 пропущено (новый
  `testВыдуманныеЛюдиВозвращаютсяКСказанному` проверяет и то, что «медленно» и
  «медлил» не задеты). `python3 mine.py --audit` — список перетягивающих не вырос.
- **Осталось:** обратной замены «Илья» → «Ильяс» нет и не будет — Илья обычное имя,
  правило сломало бы встречу, где он настоящий. Что такое «Саболь» — неизвестно.

## 2026-08-15 · Г3-чеклист с командами, Г5 подтверждён живым сигналом

- **Что:** в `dogfood-checklist.md` — раздел «Релиз 1.16.5 · Г3»: слепок хэшей
  конспектов до/после апдейта, log stream для наблюдения за фрагментами и
  гвардом свода, сценарии руками; примечание к Г5 про ложные `collapsed=1` на
  крошечных записях. Коммит 9221640.
- **Зачем:** план релиза перечисляет «что проверить», прогонке нужны «как»;
  главный непросмотренный пункт — авторский свод на живой длинной встрече.
- **Проверено:** Г5.1 — живой сигнал `Propeller.Recap.generated` виден в
  TelemetryDeck (запись 14.08 20:19: reply_tokens 190, retried 1, collapsed 1,
  bullets 2, generator 2, ram 16, 20,7 с) — все обещанные поля доехали.
  Урок: двухминутная запись честно даёт collapsed=1 — долю схлопываний в
  проде считать только по встречам от ~10 минут.
- **Осталось:** Г3 — руками владельца на живом приложении; Г4/Г6 — после
  посадки свода со страховкой; Г2 — тест маршрутизации и «бэкфилл пропускает
  встречу с конспектом».

## 2026-08-15 · Словарь пополнен по 20 последним встречам

- **Что:** `vocab/hotwords-core.txt` 349 → 393 и та же правка в
  `PropellerPure/BuiltinHotwords.swift`, `vocab/hotwords-full.txt` 672 → 786, раздел
  «Пополнение по 20 последним встречам» в `vocab/slang-vocabulary.md`. Восемь новых
  основ в `PropellerPure/TermCanon.swift` (лондос/лундос → лендос, реч-текст,
  мадборд, лок-шоп, классер, сториеллинг, вейпкод) + `TermCanonTests`.
- **Зачем:** словарь собран по переписке в июле и не знает ни текущих проектов, ни
  того жаргона, который проговаривают, но не пишут; «фастплей» звучит семь раз и ни
  разу не выходит целым словом, «варик» звучит 21 раз и не покрыт вовсе.
- **Проверено:** `swift test` — 748 тестов, 0 падений, 5 пропущено (включая
  `testBuiltinHotwordsMatchVocabFile`, который ловит расхождение .txt и Swift).
  `python3 mine.py --audit` — новых слов в перетягивающих нет; «преза» сталкивалась
  с «пресса» ×5 и оставлена только в полном списке.
- **Осталось:** «deact» → The Act не внесено — единственное вхождение внутри адреса
  `deact.ru/assistant`, правило дало бы «The Act.ru». Прирост точности на живой
  встрече не замерен.

## 2026-08-15 · Эксперименты по саммари закрыты: v2 едет, механика заморожена

- **Что:** доска `tools/recap-lab/OPTIMIZATION.md` закрыта шапкой-решением и
  разделом «Финал»: сравнение v1 → v2 (`out/v2-compare/`, обе встречи — v2
  точнее в главном), итог проб 15.08 (голосование веток по якорям, макеты
  «опорных точек» — `out/*/recap-form-mock.md`), условия возврата гипотез.
- **Зачем:** решение владельца 2026-08-15 — финализировать эксперименты и
  готовить релиз 1.16.5 без новых изменений механики; три сигнала сошлись
  (суд, релизное решение, глаза владельца): связность — свойство одного автора.
- **Проверено:** v2-прогоны — `bench_ensemble.whole_pass` t=0, reply_tokens
  1169/863, retried false; пробы — ноль вызовов Ollama, числа в «Финале»
  воспроизводимы скриптами скретчпада. Рекапы приложения не тронуты.
- **Осталось:** релиз — гейты Г3–Г6 по `RELEASE-1.16.5.md`; Г3 включает первый
  живой прогон длинной встречи (сборка кодом, без «Итога») — глазами владельца.

## 2026-08-15 · Developer ID: подпись, hardened runtime, нотаризация

- **Что:** `build.sh` подписывает изнутри наружу под Developer ID Application
  (hardened runtime + secure timestamp), `--deep` убран, `xattr -cr` перенесён
  до подписи; каждый вложенный бинарь Sparkle (Downloader.xpc, Installer.xpc,
  Autoupdate, Updater.app, сам фреймворк) подписывается отдельно. Новый
  `notarize.sh` — отправка, ожидание, staple, `spctl` для `.app` или `.dmg`.
  `package-dmg.sh` отказывается паковать `.app` без тикета, подписывает и
  нотаризует образ, хардлинк `Propeller.dmg` делает после staple.
- **Зачем:** ad-hoc/self-signed подпись требовала «ПКМ → Открыть» на каждом
  новом Mac. `--deep` навешивал entitlements приложения на XPC-сервисы Sparkle
  и не умел ставить runtime — ровно то, что notary отклоняет. `.app`
  нотаризуется отдельно от образа, потому что Sparkle ставит обновление без
  DMG, и без тикета внутри бандла первый запуск без сети упёрся бы в Gatekeeper.
- **Проверено:** `./build.sh` → `flags=0x10000(runtime)`, `TeamIdentifier=9T455555U3`,
  `codesign --verify --strict` — `satisfies its Designated Requirement`;
  notary `Accepted` дважды (`0405713c…` для .app, `1956b4ca…` для .dmg);
  копия образа с карантином Safari смонтирована →
  `/Volumes/Propeller/Propeller.app: accepted, source=Notarized Developer ID`;
  `swift test` — 747 тестов, 0 падений, 5 пропущено.
- **Осталось:** не проверено обновление Sparkle с установленной ad-hoc 1.16.5 на
  Developer ID билд (смена подписанта) и запуск Ollama под hardened runtime —
  и то и другое видно только на живой встрече.

## 2026-08-15 · Документы: «ПКМ → Открыть» убран из раздачи

- **Что:** `COLLEAGUES.md` (установка + раздел для разработчиков с порядком
  `build → notarize → package-dmg → make-appcast`), `STATE.md` (строки «Подпись»
  и «Авто-обновления»), `dogfood-checklist.md`, `ARCHITECTURE.md` (строка
  «Дистрибуция v1»), `CHANGELOG.md` («Как готовить релиз» + блок про заметки),
  `RELEASE-1.16.5.md` (строка в «Что едет», риск №3 про смену подписанта,
  четыре пункта в Г3).
- **Зачем:** инструкция коллегам велела обходить Gatekeeper вручную — шаг,
  которого больше нет, и на котором раздача теряла людей молча. Порядок скриптов
  записан там же, потому что переставить `make-appcast.sh` перед staple значит
  подписать EdDSA по байтам, которых в релизе не будет.
- **Проверено:** `grep` по «ПКМ→Открыть» вне `archive/` — живых упоминаний не
  осталось, кроме отрицающих формулировок и чек-листа. Кода не касалось.
- **Осталось:** ничего. `product-ideas.md` дозаполнен следом: «Developer ID +
  нотаризация» переехал из «Отложено» в сделанное v1, строка «DMG для коллег»
  больше не обещает ПКМ→Открыть.

## 2026-08-14 · Стенд: эксперимент «только буллеты, бюджет вверх»

- **Что:** флаг `--no-narrative` в `bench_ensemble.py` (параметр `narrative` у
  `render`), проброс через `replay_asym.build/replay` + флаги `--no-narrative`
  и `--draft-from-stats` в его CLI, `--budget/--no-narrative` в
  `check_defects.py`, три фикстуры в `test_parse.py`. Замер и вывод — новый
  подраздел A5.2 в `tools/recap-lab/OPTIMIZATION.md`. Матчер и методика не
  тронуты.
- **Зачем:** проверить гипотезу «выбросить „Ход обсуждения“ и поднять бюджет».
  Подтвердилась частично: прирост метрики читателя (+1,00/+1,25/+0,88 на
  бюджете 18) даёт **бюджет**, а не смерть прозы — контрольная строка «проза
  жива» совпадает по буллетам на каждом бюджете. Выброс прозы покупает только
  выдумки (5,12 → 1,88 на m3) и стоит 2,76 пункта покрытия на m2, из них 3,00
  на ячейку невозвратимы бюджетом. Рекомендация: бюджет 18, прозу оставить;
  `--draft-from-stats` добавлен потому, что старый CLI `replay_asym` всегда брал
  черновиком t=0 и ячейку гейта №2 воспроизвести не мог.
- **Проверено:** `python3 test_parse.py` — 29 до, **32 после**, 0 падений.
  Текущая точка не сдвинута: `python3 check_defects.py --batch gate2-fixed` —
  покрытие 9,75 → 9,75 · 12,88 → 12,88 · 8,25 → 8,25, дефектов 0 → 0.
  Предзамер владельца воспроизведён поячеечно (9,75/12,88/8,25 · 8,62/8,88/6,88 ·
  11,62/10,88/9,50). Ноль вызовов Ollama.
- **Осталось:** ячейки конструкции «только буллеты» не материализованы — для
  JUDGE-4 их надо будет собрать. Читаемость стендом не мерится: размен
  «−2,76 покрытия на m2 против читаемости» решается судом, не здесь.

## 2026-08-14 · Порт lite, кусок 2: механическая сборка переполняющих встреч

- **Что:** `RecapAssembly` (PropellerPure) — порт items_from_facts / merge /
  merge_prose / render со стенда: четыре формы ярлыка + пятая неизвестная,
  дедуп 0,55 «длинный побеждает», одна хронология прозы, стабильная сортировка
  как в Python. `recapByChunks` больше не зовёт модель для свода — документ
  собирает код; у такого конспекта нет «Итога» (цена названа в CHANGELOG).
  Прогресс по фрагментам — в деталях активности (`progress` в `RecapBackend`).
  Коммиты 26e0504 + следующий.
- **Зачем:** свободный свод схлопывался и терял до 5 найденных пунктов (A5.1);
  «Что едет» RELEASE-1.16.5.md закрыт целиком.
- **Проверено:** `swift test` — 747, 5 пропущено, 0 падений. Паритет Г1 двумя
  замками: юнит-фикстуры из `test_parse.py` + живой вход стенда
  (gate2-fixed m3/code-1) собирается **байт в байт** как python-эталон
  (`Tests/Fixtures/recap-assembly/`, README с командой снятия эталона).
  `./build.sh` установлен, литералы в бандле байт-поиском. **Живой прогон
  длинной встречи через приложение не делался** — это Г3.
- **Осталось:** Г2-тест маршрутизации «влезает/не влезает» на уровне
  RecapService не написан (порог решает `TranscriptChunking.needed` — он
  покрыт своими тестами); Г3–Г6 целиком; notes на пути нарезки теперь идут
  только блоком `RecapNotes.placed` — дефолт из плана, подтвердить глазами на Г3.

## 2026-08-14 · Порт lite, кусок 1: t=0 + ретрай, версия генератора, телеметрия

- **Что:** `RecapGenerationPolicy` (PropellerPure, 13 тестов) и `chatReplyTokens`
  в `BoundaryResponses`; `RecapService.callOllamaTracked` — t=0 на всех локальных
  вызовах, при ответе короче порога (800 конспект / 600 извлечение) один повтор
  на t=0,3, дальше уходит длинный из двух; `RecordingEntry.recapGeneratorVersion`
  (= 2) пишется при успехе локального конспекта; сигнал `Recap.generated`
  (reply_tokens, retried/collapsed, пункты, chunked, версия, RAM-когорта,
  секунды в value). Облачные пути не тронуты. Коммиты f890756 + следующий.
- **Зачем:** RELEASE-1.16.5.md, «Что едет»; семантика повтора — перенос
  `promptlib.call_ollama` шаг в шаг (Г1), пороги и «длинный из двух» оттуда же.
- **Проверено:** `swift test` — 733, 5 пропущено, 0 падений; `./build.sh` —
  установлен; литерал «ответ схлопнулся» в бандле байт-поиском (1). Урок:
  «Recap.generated» байт-поиском не находится — ASCII ≤ 15 байт Swift пакует
  как small string в код; метод в STATE.md дополнен. **Живой прогон генерации
  не делался** — это Г3.
- **Осталось:** кусок 2 (механическая сборка переполняющих) не начат; Г5 —
  увидеть сигнал в TelemetryDeck с этой машины; Г6 — даунгрейд руками.

## 2026-08-14 · Релиз саммари сокращён до lite, едет как 1.16.5

- **Что:** `RELEASE-1.17.md` → `RELEASE-1.16.5.md` (git mv + переписан): едут
  t=0 + один ретрай, нарезка + механическая сборка, версия генератора,
  телеметрия; ансамбль срезан и вернулся на доску с записанным условием
  возврата — покрытие выше базы **и** читаемость у независимого судьи не ниже
  базы. Маршрутизация по RAM и порог 15 минут удалены. Живые ссылки поправлены:
  `STATE.md` (карта доков), `product-ideas.md`.
- **Зачем:** решение владельца 2026-08-14 после JUDGE-3 (ансамбль читателю хуже
  базы: 2,10 против 3,25) и B0 (v3 отвергнута): шипим бесспорные победы, спорную
  конструкцию не шипим. Номер 1.16.5 — следующий регулярный, `build.sh` уже на нём.
- **Проверено:** правка доков, кода нет; `git mv` подтверждён `ls`; ссылок на
  старое имя вне исторических логов не осталось (`grep -rn RELEASE-1.17 *.md`).
- **Осталось:** сам порт (Г1–Г6) не начат — бриф готов к раздаче сессиям;
  решение про облачный opt-in не принято; notes на пути нарезки — подтвердить
  дефолт при порте.

## 2026-08-14 · П0: парадигма v3 отвергнута — окна работают, схема нет

- **Что:** новый `tools/recap-lab/bench_extract.py` (окна 13k по репликам с
  перекрытием 20 %, два прохода, JSON по контракту через `format`, спан- и
  enum-проверки кодом, рендер без генерации, режимы `--free` и `--lean`, отчёт
  батча); `promptlib.call_ollama` получил `fmt`, `chunked.turns` вынесен как общая
  резка; 11 фикстур в `test_parse.py`. Числа и разбор — на доске, раздел **B0**;
  статусы — в `plan-recap-v3.md`.
- **Зачем:** П0 — фальсификация парадигмы до любого порта. Ответ отрицательный:
  требование «каждое утверждение несёт спан» делает извлечение локальным, а
  полнота встречи живёт в длинных дугах спора; заодно поля схемы съедают
  внимание 4B, а enum защищает поле, но не документ.
- **Проверено:** `python3 test_parse.py` — 29/29. Чередованные батчи n=5 на обеих
  размеченных встречах: v3 **5,6 и 2,4** против базы 7,8 и 10,4 и ансамбля 10,8 и
  14,0 (ансамбль в том же батче +3,30 к базе, p = 0,0 % — батч здоров). Контроль
  формы теми же окнами свободным промптом — **11,3 и 11,0**. Заход на упрощение
  схемы (`--lean`, разрешён стоп-условием один раз) — 2,6 и 5,4. Детекторы: 0
  находок на всех 10 ячейках v3. 84 с/ячейка, пик RSS ollama 4,45 ГБ.
- **Осталось:** П1–П5 не начинаются; пауза порта RELEASE-1.17 снимается (точка
  решения 1). Лид на доске: окна с перекрытием + сборка кодом дают 11,3/11,0 за
  4 вызова против 6 у ансамбля — мерить отдельной гипотезой, n=3 и без ячеек
  ансамбля рядом это ещё не замер. Ручную сверку матчера делала та же сессия, что
  строила стенд, — правило «судит не тот, кто строил» здесь не соблюдено.

## 2026-08-14 · План конвейера v3 «поиск → проверка → вёрстка» — на раздачу сессиям

- **Что:** `plan-recap-v3.md` в корне — пакеты П0–П5 с критериями, записанными до
  прогонов, контрактом утверждения (JSON-схема со спаном и enum имён), картой
  распараллеливания и точками решения владельца.
- **Зачем:** контур «свободное порождение → починка» упёрся структурно (суды
  находят новый класс дефектов каждым заходом); v3 делает дефекты непорождаемыми
  вместо вычищаемых. П0 — дешёвая фальсификация за день стенда, всё остальное
  условно от неё.
- **Проверено:** не проверено — это план, кода нет; числа-ориентиры сверены с
  OPTIMIZATION.md (A5, A5.2, JUDGE-3) построчно.
- **Осталось:** П0 запущена отдельной сессией 2026-08-14; пауза порта 1.17 и
  заморозка итерации Г0 приняты владельцем и вписаны в план и RELEASE-1.17.md.
  П1–П5 ждут вердикта П0.

## 2026-08-14 · Синтез пере-суда №2 записан на доску

- **Что:** в `tools/recap-lab/OPTIMIZATION.md` (раздел A5.2) — сводка `judge/JUDGE-3.md`:
  трёхсторонняя таблица base/gate2/gate2-fixed, три находки мимо детекторов (атрибуция
  улучшена снятием, дыры от вырезания, двойной пересказ прозы) и чтение Г0 с планом
  следующей итерации (одна нить прозы, стемы имён, срез хвостовых огрызков).
- **Зачем:** вердикт судьи лежал только в judge/, решение по Г0 принимается по доске.
- **Проверено:** числа сверены с `JUDGE-3.md` построчно; кода не меняли,
  `test_parse.py` 18/18 в этой сессии.
- **Осталось:** сама итерация (zero-generation, на replay) и пере-суд №3 (JUDGE-4)
  независимой сессией; Г0 не решён.

## 2026-08-14 · Пересборка легла на диск, четвёртый дефект — артефакты генерации

- **Что:** новый `tools/recap-lab/materialize.py` кладёт пересборку гейта №2 в
  `out/gate2-fixed/{m1,m2,m3}/{base,code}-{1..8}/recap.md` — раскладка гейта, ноль
  вызовов модели, тот же черновик, что в живом прогоне; `base`-ячейки копируются
  дословно (они не проходят через `render`), об этом сказано в README папки. В
  `bench_ensemble` добавлен фильтр артефактов генерации (`strip_artifacts`,
  `artifact_findings`, `fused_word`) и включён в `render(..., transcript=)`, то есть
  одинаково в живом прогоне и в пересборке; `replay_asym.build/by_metric/replay`
  прокинули транскрипт. В `check_defects` — четвёртый детектор «г·артефакт генерации»
  с разбивкой по трём классам. `test_parse.py` 11 → 18 фикстур.
- **Зачем:** пере-суд (`judge/JUDGE-2.md`) промахнулся мимо корпуса — починенные
  ячейки существовали только в памяти `check_defects`, файлов не было, и судья взял
  `out/gate2`, живые прогоны **до** починки, измерив старую сборку второй раз. Плюс он
  нашёл класс дефектов, которого в первом суде не было: иероглифы посреди русской
  фразы, сросшиеся письменности внутри слова и дословное эхо промпта экстрактора.
- **Проверено:** `python3 test_parse.py` — 18/18. `python3 materialize.py` —
  пересобрано 24, скопировано 24. `python3 check_defects.py --batch gate2-fixed` — на
  code-ячейках **0 → 0** по всем четырём детекторам (было 121 на тех же ячейках в
  `gate2`), пересборка починенного совпадает с ним байт в байт на всех 24 —
  рендер неподвижная точка. Артефакты на code-ячейках: иероглиф 2 → 0, сращение
  16 → 0, эхо промпта 6 → 0. Контроль: попарное покрытие не просело ни на одной
  встрече — 9,75 → 9,75 · 13,12 → 12,88 · 8,38 → 8,25, те же числа, что до фильтра.
  Первая версия фильтра резала по пробелу и дефису и давала 14 ложных находок из 31
  («Радио/VK», «сбербанка.ru»); правило переписано на прогон букв. `python3
  gate_score.py` — вердикт как был, не пройден.
- **Осталось:** судить ничего не стал — это работа независимого судьи, теперь у него
  есть явный путь `out/gate2-fixed`. В бэклоге A5.2 с числами, не чинилось: кентавр
  «Саша Шукурова» в **прозе** буллета (фильтр видит только слот; жив в 5 ячейках m3);
  перестановка сроков моделью (4 m2-ячейки из 5 — это не сборка); «Саболь» как
  исполнитель (11 задач из 22 на m2 — токен дословный, нужен вокатив-контекст).
  `out/` в `.gitignore`, то есть `gate2-fixed` живёт только локально.

## 2026-08-14 · Три дефекта сборки по судейскому аудиту: 80 → 0, покрытие на месте

- **Что:** починены три дефекта, названные независимым судейским аудитом
  (`tools/recap-lab/judge/JUDGE.md`). (а) Проза «Хода обсуждения» больше не
  конкатенируется по ветвям: новый `bench_ensemble.merge_prose` разбирает её на блоки
  (строка без заголовочного таймкода продолжает предыдущий) и ставит в одну хронологию;
  `replay_asym.candidates_of` и оба пути `main` переведены на него. (б)
  `bench_ensemble.items_from_facts`: метка ищется после снятия маркера буллета и болда
  (форма `- ДОГОВОРИЛИСЬ: …` уезжала в конспект дословно), неизвестная метка теряет
  ярлык, но не текст. (в) Новый `owners.py`: слот исполнителя обязан совпасть со
  словарём имён встречи, составное имя — парой имя+фамилия; фильтр включается в
  `render(..., names=)`, то есть одинаково в живом прогоне и в пересборке. Новое:
  `test_parse.py` (11 фикстур, все четыре формы экстрактора + пятая неизвестная),
  `check_defects.py` (три детектора + попарный замер до/после). `replay_asym`
  научился воспроизводить политику черновика `clean` по `stats.json`.
- **Зачем:** аудит подтвердил знак гейта по полноте на всех трёх встречах, но нашёл
  два регресса ансамбля против базы на осях, которых стенд не мерил: читаемость 2,17
  против 3,38 (две ячейки на 1 — «пользоваться нельзя») и неверный исполнитель у
  44,7 % задач против 37,5 %. Причины оказались в сборке, а не в модели, то есть
  чинятся кодом и без генераций.
- **Проверено:** `python3 test_parse.py` — 11/11. `python3 check_defects.py` и
  `--batch gate2` — дефекты на пересобранных ячейках **80 → 0** (gate) и **97 → 0**
  (gate2); покрытие попарно m1 10,12 → 10,12, m2 13,12 → 12,88, m3 8,00 → 8,00 (gate2:
  9,75 → 9,75 · 13,12 → 12,88 · 8,38 → 8,25), буллетов 14,0 → 14,0, выдумки линта
  m1 1,25 → 0,25 и m3 14,88 → 12,88. Все шесть разошедшихся зачётов матчера стоят на
  самих дефектных строках (Q1 и T2 — внутри сырых `- ОТКРЫТО:`/`- ЗАДАЧА:`, D8 — на
  фамилии внутри слота «Команда (Саша Шукурова и др.)»). Порог фильтра подобран на
  размеченном сете пробы C-2: 248 слотов, **0 из 12** fabricated/wrongname прошло,
  честных сохранено 159/165 вхождений и 16/17 форм при θ = 0,34, плато [0,25; 0,58);
  `python3 owners.py` печатает свип и матрицу. Разово на пересборке правило гейта:
  выдумки **2,29 против 2,67 у базы** (те самые 0,12 закрыты), но по встречам
  +2,75 · +1,25 · **−0,12**, то есть гейт не проходит уже другим условием.
  `python3 gate_score.py` на сохранённых файлах — вердикт как был (не пройден).
- **Осталось:** пере-суд читаемости и атрибуции **независимым судьёй** — эта сессия
  свою работу не судит; содержательный повтор в «Ходе обсуждения» остался (убрать
  механически стоит 0,87 пункта на m2 — числа в A5.2, решение владельца); «Саболь» и
  «Агась» как исполнители фильтр проходят (токены дословные); ячейки базы не
  пересобираются, у них остаётся 21 и 23 неподтверждённых исполнителя. Живой гейт после
  починки не прогонялся.

## 2026-08-13 · Гейт №2: покрытие взято, выдумки мимо на 0,12 — решение за владельцем

- **Что:** в методику доски добавлены два правила (приёмка стратума по сверке ≥90 %
  без перекоса; протокол калибровки новой встречи на held-out ячейках). Якоря m3
  калиброваны по четырём отдельным конспектам `out/calib/m3` и заморожены. В
  конструкцию ансамбля добавлена политика выбора черновика `clean`
  (`bench_ensemble.choose_draft_index` — ветка с меньшей плотностью выдумок на
  буллет), проза «Итога» теперь берётся из выбранного черновика, в `stats.json`
  пишется `draft_branch`. Прогнан гейт №2: свежие батчи ABAB, n = 8 × 3 встречи.
- **Зачем:** гейт №1 провалился на −1,62 на третьей встрече, но её линейка была
  негодной (сверка 67 %, перекос 10 против 2 в пользу базы). Владелец выбрал чинить
  замер; починка линейки и конструкции требовала перезапуска гейта.
- **Проверено:** сверка после калибровки — **100 % · 96 % · 95 %**, перекос ушёл.
  Пересчёт гейта №1 нетронутыми ячейками: третья встреча **−1,62 → +0,25**, то есть
  знак переворачивала линейка, а не конструкция. Поиск политики черновика на пяти
  сохранённых батчах: `clean` режет выдумки m3 **14,9 → 7,2** (ниже базы 7,9) при том
  же покрытии и поднимает m1 10,12 → 10,50; по длине — провал, как и ожидалось.
  **Гейт №2 (`python3 gate_score.py`): покрытие +2,75 · +1,50 · +0,00, пулом +1,42
  при p = 0,61 %** — пройдено; буллетов 14,0 — пройдено; **выдумки 2,79 против 2,67 —
  не пройдено** (в гейте №1 разрыв был 1,92). Старый матчер даёт тот же вердикт
  (+2,62 · +1,62 · +0,00, p = 0,50 %). Ретрай черновика вытащил 5/8 и 3/8
  схлопываний, политика на m2 не вмешалась ни разу, на m1/m3 поделила выбор 4/4.
- **Осталось:** Г0 открыт, нужно слово владельца — дефолт по записанному правилу
  вариант 1 (принять точку, снизить критерий до фактического). Отдельная находка,
  которую надо учесть при решении: **на планёрке ансамбль не даёт ничего** (+0,00 за
  7 вызовов против 1), то есть его ценность зависит от жанра; строка «+3–4 пункта» в
  `RELEASE-1.17.md` заменена фактическими числами. Политика черновика в «Что едет» не
  вносилась: гейт не закрыт.

## 2026-08-13 · Гейт №1 по записанному правилу: не пройден, третья встреча нашла дыру

- **Что:** правило решения записано в `OPTIMIZATION.md` **до** прогонов (условия,
  ячейки, n = 8, стратифицированный перестановочный тест, 100 000 жребиев, seed
  20260813) и исполняется `gate_score.py` без ручных шагов. Добавлена третья
  размеченная встреча `20260810_094722` («Тактика | Лиды», планёрка, шкала 21):
  слепой golden от отдельного агента, якоря по нему — до генерации любых конспектов.
  Починен матчер (`normalize` сворачивает `й` → `и`). Изменена конструкция ансамбля:
  ветка-черновик получила `retry_temperature` = 0,3 (`promptlib`, `bench_ensemble`).
  `replay_asym` умеет подменять черновик (`draft_from`) для бесплатного превью.
- **Зачем:** прежний критерий не проходил и оракул (p = 10,3 % при n = 5) — владелец
  выбрал чинить замер, а не конструкцию. Третья встреча нужна была как проверка на
  оверфит, и она сработала.
- **Проверено:** `gate_score.py` — разницы **+2,38 · +1,25 · −1,62**, пулом +0,67 при
  **p = 23,7 %**; тем же вердиктом и старым матчером (+0,50, p = 39,4 %) — починка
  матчера вердикт не решала. Условие «выдумки» тоже не выполнено (5,46 против 3,54).
  Диагноз: на третьей встрече ветка t=0 схлопывается **детерминированно 8/8**
  (684 токена против порога 800), теряет «Ход обсуждения» целиком и даёт черновик
  **6/21** — ниже худшего из восьми сэмплов базы (10–16); неприкосновенный черновик
  съедает 11 буллетов из 14, добавкам остаётся 3 слота. Превью на пересборке со
  здоровым черновиком выправляет знак (−1,62 → +0,62), но оптимистично по
  построению. **Ретрай при 0,3 измерен и его мало:** пять сэмплов дают 694 · 705 ·
  1224 · 623 · 1152 токена, порог берут два из пяти; живая проверка — ретрай
  сработал, вторая попытка не длиннее, черновик тот же. Сверка матчера на третьей
  встрече — **42 из 63 (67 %)** против 84/84 и 46/48, промахи асимметричны (10 лишних
  на конспекте базы против 2 на конспектах ансамбля), по руке `code14` там базу
  **обыгрывает** (9 и 13 против 6). Находки линта на m3 разобраны глазами: половина —
  варианты имени участника («Льон», «Льва»), это шум линта; «Сафронов» — настоящая
  выдумка, и ансамбль множит её (7 вхождений на прогон против 1,3).
- **Осталось:** гейт №2 **не запускался** по стоп-условию — починка недостаточна,
  третий стратум негоден, условие выдумок провалено по существу. Г0 открыт, дефолт
  прежний (вариант 1, решение владельцу). Три предложения в конце A5.2: черновику до
  трёх попыток вместо одной (гигиена замера ≠ правило продукта), третий стратум
  мерить рукой вместо матчера, и перепроверить якоря двух старых встреч слепой
  разметкой. Линт и якоря под увиденные данные не подгонялись.

## 2026-08-13 · A5.2: три сигнала в отборе — все в шуме, критерий упёрся в мощность

- **Что:** в `tools/recap-lab/replay_asym.py` добавлены складываемые признаки отбора
  — регион транскрипта (`Regions`: реплики, взвешенные IDF; дедуп и приоритет по
  месту пункта), IDF-новизна с нормировкой на длину, канонизация пунктов одним
  детерминированным вызовом с кэшем `canon.json`; варианты `idf · region · canon` и
  комбинации. В `OPTIMIZATION.md` — два новых подраздела A5.2 и переписанное решение
  владельцу, в Г0 `RELEASE-1.17.md` уточнена причина блокировки.
- **Зачем:** промахи отбора разные в каждом прогоне, то есть шум ранжирования, а не
  слепое пятно ветвей — по постановке чинится сигналом. Проверялось на сохранённых
  ветках `asym1`/`asym2` попарно против действующей точки `code14`.
- **Проверено:** `golden_match.py --calibrate` — 84/84 и 46/48; пересборка
  по-прежнему воспроизводит живой батч (`code14` 10,8 / `oracle14` 11,8). Средние:
  `idf` 11,2 / 12,6 · `region` 11,2 / 12,4 · `canon` 11,0 / 12,6 против `code14`
  10,8 / 12,4 при оракуле 11,8 / 13,0. **Попарный знаковый тест по всем десяти
  прогонам: `idf` +0,30 (p = 50 %), `region` +0,20 (p = 80 %), `canon` +0,20
  (p = 50 %), `canon+idf` +0,10 (p = 100 %)** — ни один не отличим от нуля; у `idf`
  прирост на второй встрече ещё и не выживает при смене длины корня (4 → 12,2;
  5 → 12,6; 6 → 12,4). Живой батч не запускался: подтверждать нечего. Главное
  измерение захода: **на второй встрече критерий гейта не проходит и оракул** —
  13,0 против базы 11,4, p = 10,3 % (механика 13,2, p = 4,0 %, но при 26 буллетах);
  бутстрап-оценка мощности — оракул прошёл бы p < 5 % в 45 % случаев при n = 5 и
  77 % при n = 8.
- **Осталось:** Г0 открыт, решение владельца из трёх вариантов в конце A5.2
  (снизить критерий · починить замер: n = 8–10 плюс слепая разметка третьей встречи ·
  шипнуть механику с 26 буллетами). Резервные направления зафиксированы, но не
  начаты: `llama-server --embeddings` на GGUF-блобе из хранилища ollama и
  эмбеддинг-модель на 300–500 МБ. Починена ловушка разбора канона (модель не пишет
  номера; позиционная склейка теперь только при точном совпадении числа строк).

## 2026-08-13 · A5.2: отбор добавок упёрся, решение о шипе — за владельцем

- **Что:** новый `tools/recap-lab/replay_asym.py` — пересборка конспекта из
  сохранённых веток прогона без единой генерации (варианты `draft · code14 ·
  quota14 · oracle14 · mech`). На нём проверены и отвергнуты секционные квоты в
  жадном отборе; измерена недоступность эмбеддингов; зафиксирована лучшая точка
  кода без вызова дедупа. Раздел A5.2 в `OPTIMIZATION.md` дополнен четырьмя
  подразделами, строка A5.2 в таблице состояния переписана, в гейте Г0
  `RELEASE-1.17.md` пункт слияния помечен как требующий решения, два других пункта
  закрыты.
- **Зачем:** дефицит покрытия лежит в отборе семи добавок из двадцати (оракул
  доказал, что 14 буллетов вмещают всё). Отбор — код, значит его варианты меряются
  на уже снятых ветках попарно и бесплатно, до любого живого батча.
- **Проверено:** `python3 golden_match.py --calibrate [--meeting 20260812_153107]`
  — 84/84 и 46/48, exit 0. Пересборка воспроизводит живой батч попунктно (draft
  8,0 · code14 10 11 10 12 11 → 10,8 · oracle 11,8 — те же значения и порядок
  прогонов, что в `out/asym1`). Квоты: 10,8 → 10,8 / 10,6 / 11,2 при 1+1 / 2+2 /
  3+2 слотах и 10,6 пропорционально, на второй встрече 12,4 **без изменений на все
  пять вариантов** — живой батч не запускался, проверять нечего. Диагностика: без
  квот жадность и так берёт 1–2 в каждую секцию, промахи внутри секции и разные в
  каждом прогоне. Эмбеддинги: `/api/embed` отвечает
  `does not support embeddings` на всех трёх локальных моделях — ollama 0.32.1, а
  не архитектура; сигнал согласия ветвей пуст лексически (0–1 пара на прогон при
  пороге 0,55–0,45). Лучшая точка `--dedup budget --budget 14`: 10,8 (p = 0,8 % к
  базе 7,2) и 12,4 (p = 41 % к базе 11,4), 14,0 буллета, выдумок 1,2 и 0,2 против
  1,2 и 1,8 — **два критерия из трёх**, покрытие не проходит ни на одной встрече.
- **Осталось:** Г0 открыт, нужно решение владельца из трёх вариантов в конце A5.2
  (принять точку и снизить критерий · шипнуть механику с 26–28 буллетами · вложить
  в семантический отбор отдельную эмбеддинг-модель). Живых генераций в этой работе
  не было вовсе.

## 2026-08-13 · A5.2: асимметричное слияние конспекта (не решено, дефицит найден)

- **Что:** в `tools/recap-lab/bench_ensemble.py` добавлена конструкция «база +
  добавки» (`--dedup asym|asym-one|budget`, `--budget`): ветка t=0 — черновик и не
  редактируется, остальные ветки только предлагают, модель отвечает буквой пункта
  или нулём, добавки фильтруются проверкой заземления `lint` и режутся бюджетом
  буллетов. Раздел A5.2 в `OPTIMIZATION.md` перезаписан.
- **Зачем:** ансамбль даёт +3 пункта покрытия, но 26–28 буллетов — свалка, а
  симметричный дедуп (порог, кластеризация моделью, вето кода) не сходился.
- **Проверено:** живой чередованный батч n=5 на обеих встречах (`out/asym1`,
  `out/asym2`): краткость и выдумки прошли (12,4 и 14,0 буллета; 1,2 против 1,2 и
  0,2 против 1,8), покрытие нет — 10,0 против порога 11 и против 12,0 у механики
  (p = 2,4 %), на второй встрече 12,4 неотличимо от базы (p = 41 %). Оракул при 14
  буллетах даёт 11,8 и 13,2, то есть столько же, сколько механика при 28: дефицит
  в отборе добавок, а не в краткости. `golden_match.py --calibrate` — 84/84 и
  46/48.
- **Осталось:** гейт Г0 в `RELEASE-1.17.md` не закрыт (слияние). Дальше — догонять
  оракула кодом либо эмбеддинги (`/api/embed`) вместо лексического пересечения;
  вызов дедупа не окупается и в шип не идёт.

## 2026-08-13 · Инвентаризация и чистка документации

- **Что:** удалены phase0/2/4/5/6 (покрыты `archive/phaseN-STATUS.md`) и 596 МБ
  build-кэша; 3 историчных дока переехали в `archive/`; починены 5 мест, где доки
  врали (CLAUDE.md, NEXT-STEPS, CHANGELOG, product-ideas, dogfood-checklist);
  созданы `DOCS.md` (слои и правила) и `RELEASE-1.17.md` (план релиза с гейтами).
- **Зачем:** три агента-инвентаризатора нашли мёртвые папки, четыре точки лжи и
  отсутствие правил «что куда пишем» — до релиза 1.17 это источник ошибок.
- **Проверено:** `swift test` — 720 тестов, 0 падений; `./build.sh` — установлен
  /Applications/Propeller.app. Ссылки на перенесённые доки обновлены (grep чистый).
- **Осталось:** два wav в `phase0/` удалить руками (последняя копия записи,
  автоматике нельзя); решить судьбу авто-снапшотов `swift/benchmarks/report-*.md`.
